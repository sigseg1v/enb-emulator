# Phase: Player-reported live bugs

Running log of bugs reported by the project owner from real in-game play
(real client.exe under WINE against the live server). Distinct from the
phase checklists: these are observed runtime defects, triaged here, fixed
under whichever phase/rule applies. Newest at top.

Status key: `[ ]` open `[~]` in progress `[x]` fixed `[!]` blocked `[?]` needs repro/capture

| # | Status | Bug | Root cause / notes |
|---|--------|-----|--------------------|
| PB-31 | `[x]` | **Auction House: cannot bid/buy listings while playing a non-first character; works only on the first character selected** (Minor). Buyer has sufficient credits but the bid/buy fails on the 2nd+ avatar of the account. | **SOLVED 2026-06-20.** Root cause confirmed exactly as suspected: `store_ah_write.go` `PlaceBid`/`Buyout` resolved the wallet via `primaryAvatar(accountID)` = the lowest-slot character, ignoring the character the player was acting as. On a 2nd avatar the empty slot-0 wallet was debited -> `errInsufficient`, even though the playing char had the credits. (`PostListing` already took an explicit `avatarName` via `ownedAvatarByName` -- bid/buyout were the inconsistent pair.) Fix: thread the SPA's selected topbar character through `placeBid`/`buyout` (web `api.ts` + `AuctionHouse.tsx`/`BuyView`), read `body.Avatar` in `api.go` `handleBid`/`handleBuyout`, and resolve the wallet via `ownedAvatarByName` in the store -- no fallback to slot-0. `primaryAvatar` now only serves the mail-loot fallback. Regression tests: `TestIT_Buyout_ChargesActingCharacter_NotPrimarySlot` + `TestIT_Bid_ChargesActingCharacter_NotPrimarySlot` (store_integration_test.go) seed a 2-char account with slot-0 broke + slot-1 funded; proven to fail (`insufficient credits`) on the old code and pass on the fix. NOT a wire bug; no CV entry. (Note: AH is web-only freya/online SPA, no in-game native bridge -- reporter's "in-game AH" was loose framing.) |
| PB-30 | `[?]` | **Skill (Terran Tradesman): Katlyn Walsh at Somerled Station (New Edinburgh) will not train Build Components at lvl 10 -- it is gated behind Reconfiguration of Shields (PB-29) which is itself untrainable** (Medium). | **AUDIT DONE 2026-06-20 -- the reporter's stated cause is FACTUALLY WRONG, and the content is internally coherent. Needs a real-client repro of the reporter's character state; do NOT guess-fix.** Findings (verified against the live `net7` content DB): Build Components is skill 4; its trainer mission is mission 117 "Tinkering with Parts", given by Katlyn Walsh (`starbase_npcs.npc_Id` 306, location 7), `<Reward ID="9">4`. Its mission prereqs are `<Condition ID="10">` (MISSION_REQUIRED) on missions **6 and 116** -- **NOT mission 347 "Reconfiguration of Shields"**. So PB-30 is NOT downstream of PB-29: the two are unrelated chains. The real prereq chain is coherent: mission 6 "Supply Run" (Louden) which itself requires mission 5; and mission 116 "Build Chemical Resistance Device" (Maks Babin, npc 225). CONCLUSION: there is no content defect -- Katlyn correctly refuses to train Build Components until missions 6+116 are complete. The reporter either hasn't completed those prereqs, or (like PB-29) has a character-state mismatch. The "gated behind Reconfiguration of Shields" premise is simply incorrect per the data. Same as PB-29: needs the live client + the reporter's actual completed-missions set to diagnose; see plans/29 CV entry. No content change is justified. |
| PB-29 | `[?]` | **Skill (Terran Tradesman): Louden MacEwen at Loki Station (High Earth) does not offer the "Reconfiguration of Shields" quest, blocking training of Shield Charging** (Medium). Primary source: net7wiki Reconfiguration_of_Shields. | **AUDIT DONE 2026-06-20 -- content is CORRECT; this is NOT a content-data bug. Needs a real-client repro of the reporter's character state; do NOT guess-fix.** Findings (all verified against the live `net7` content DB + server source): (1) the mission EXISTS -- `missions` id 347 "Reconfiguration of Shields", well-formed XML (2921 chars, properly `<Mission>..</Mission>`-wrapped), within the loaded id range (3..424). (2) It is CORRECTLY wired to Louden: Stage-0 `<Completion ID="1">41</Completion>` and Louden's `starbase_npcs.npc_Id` = 41 (the only Louden MacEwen row). (3) The id spaces MATCH: the server's offer scan (`Player::CheckForNewMissions`, PlayerMissions.cpp) compares the mission giver value against `m_StarbaseTargetID` = `pkt->StarbaseID` = the NPC's `npc_Id` (resolved via `StationLoader::GetNPC`, keyed by `npc_Id` -- StationLoader.cpp:346-363,549), so giver 41 == Louden 41. (4) The ONLY offer gates are `<Condition ID="5">0` = Race==Terran(0) and `<Condition ID="6">1` = Profession==Trader(1) (restrict_type enum TalkTree.h:78-90; eval PlayerMissions.cpp:594-601,627-628) -- exactly a "Terran Tradesman", and there is NO level/prereq gate. (5) Reward is correct: `<Reward ID="9">51` = skill 51 "Shield Charging". CONCLUSION: a Terran-Trader who has not already started/completed 347 WILL be offered it. The report is therefore explained by RUNTIME/character state, not data: most likely (a) the reporter already completed or started 347 (the `m_CompletedMissions`/`m_CurrentMissions` skips at PlayerMissions.cpp:542-546 suppress re-offer), or (b) the character's stored `ship_data.race`/`profession` isn't exactly Terran(0)/Trader(1). Both need the live client + the reporter's actual character to confirm -- see plans/29 CV entry. The net7wiki page is behind a Cloudflare JS challenge (not machine-fetchable here), so retail-fidelity of the prereqs couldn't be cross-checked; our content is internally self-consistent and correctly wired, so there is no primary-source basis to CHANGE it. |
| PB-28 | `[ ]` | **Quest (Terran Tradesman): "Survival of the Fittest" class quest step 2/5 (Hull Patch) awards "The Cygni Express" (a lvl-4 engine) as a step item -- the WRONG item** (Minor). It is consumed at the next-step turn-in at Zweihander Planet, so do not use/vendor it. Per reporter, Cygni Express is actually meant to be the reward of "Terran Promotion Notification 2" (a lvl-30 class hull quest). | Reported 2026-06-19. Quest CONTENT-data bug -- a mission step is configured to grant the wrong item template (the lvl-30 hull-quest reward "The Cygni Express" instead of whatever the Hull Patch step should hand over). NOT a wire bug. **CAUTION / NEEDS A PRIMARY SOURCE for the CORRECT item:** the reporter pins WHAT is wrong (Cygni Express shouldn't be here) and WHERE it belongs (Terran Promotion Notification 2), but does NOT give the item the Hull Patch step is SUPPOSED to grant -- so the audit can confirm the defect but the fix needs the authoritative correct item before changing the grant. **Solo-doable audit (no client):** (1) find the "Survival of the Fittest" mission in mission content, locate step 2/5 (Hull Patch) and read its reward/grant item id; (2) confirm it points at the Cygni Express template; (3) find "Terran Promotion Notification 2" and confirm Cygni Express is (or should be) ITS reward; (4) source the correct Hull Patch step item (wiki/retail) before editing. Don't guess-swap the item. Quest/content data. |
| PB-27 | `[ ]` | **Quest (Terran Tradesman): "Exterminate Scuttle Nymphs" class quest -- the target Scuttle Nymphs are missing from north of EarthCorps Sector Command in High Earth** (Minor). Reporter notes Scuttle Nymphs do spawn in Saturn at nav "Abandoned Pathway" beside the Accelerator. | Reported 2026-06-19. Content/spawn-data bug -- the mission expects Scuttle Nymph mobs at a High Earth location (north of EarthCorps Sector Command) where none currently spawn; the mob type DOES exist in content (it spawns in Saturn). NOT a wire bug. Likely a missing/mis-placed spawn group in the High Earth sector content, OR the mission targets the wrong sector/spawn. **Solo-doable audit (no client):** (1) confirm the "Scuttle Nymph" mob template exists and find its spawn rows (`spawn`/mob-placement content) -- present in Saturn, check High Earth; (2) read the "Exterminate Scuttle Nymphs" mission to see what sector/spawn/objective it keys on (does it require a High Earth spawn that's absent, or is the mission objective itself pointing at the wrong place?); (3) determine whether retail had Scuttle Nymphs in High Earth (primary source) before adding a spawn -- adding a spawn that retail didn't have would DIVERGE from preservation. Author content only with a source; do not invent a spawn to satisfy the quest. Quest/spawn content data. |
| PB-26 | `[!]` | **Progen Sentinel: Mordanite for the "Gravity Link" quest (Bring Mordanite to Jenya Erelius) is not obtainable by mining.** | **AUDIT DONE 2026-06-17 -- BLOCKED on a primary source; do NOT guess-fix.** Findings: Mordanite (item 3945) and Mercury Chloride (item 1225) both exist. Our content has the relationship the OPPOSITE way from the reporter's described retail mechanism: `item_manufacture` (schema.sql:22519) says Mercury Chloride is MADE FROM Mordanite (`'1225','3974','3945',...`), and Mordanite is also an ingredient of item 1091. The `item_refine` table (the analyze/rip table) is **completely EMPTY** in both Postgres and MySQL -- no rip recipes exist at all. Mordanite is NOT mineable, NOT a manufacture OUTPUT, and is currently obtainable only as the Mission-17 reward (which is circular for the quest) or by being consumed as a manu ingredient. Mercury Chloride is also not mineable. So the player's path (mine Mercury Chloride -> analyze/rip -> Mordanite) is unsupported, AND our manufacture data contradicts it. **Why this is blocked, not solo-fixable:** "correct" requires the authoritative retail recipe -- inventing a Mercury-Chloride->Mordanite refine recipe would be content FABRICATION that also contradicts existing `item_manufacture` data. Needs a primary source (retail recipe/skill docs, a capture, or owner confirmation of the exact retail chain) before any content change. Reported 2026-06-17 (Veret). The reporter's OWN note describes the intended retail mechanism: Mordanite is NOT mined -- it is RIPPED from **Mercury Chloride** (a PW flies to a Deep Tartarus-style hostile zone, mines Mercury Chloride, a Trader **analyzes** it and rips Mordanite out). So this is very likely NOT "make Mordanite mineable" (that would DIVERGE from retail) -- the real question is whether our content lets you obtain it the retail way. Both `mordanite` and `mercury chloride` exist in `db/postgres/seed_phase_y*.sql`. **Solo-doable audit (no client):** (1) confirm Mercury Chloride is a mineable/prospectable resource present in the right zone(s); (2) confirm the analyze/rip recipe (manufacturing `m_RefinesInto`) for Mercury Chloride yields Mordanite -- cross-check the refine table / `seed_phase_y_drops.sql`. If the rip recipe is missing or Mercury Chloride is absent from the source zone, THAT is the bug (content/data), not the mining. Do NOT add Mordanite to mining drop tables -- that contradicts the reporter's described retail behaviour. Ties to PB-14 (analyze/dismantle path now fixed -- the rip uses it). |
| PB-25 | `[~]` | **Mining: resource nodes are stacked on top of waypoints instead of spread across the zone, and many are missing from zones.** | **(a) STACKED-ON-WAYPOINTS FIXED 2026-06-17 (commit ce867062); (b) MISSING-IN-ZONES still blocked (no per-zone source).** Fix (a): `generate_seed_prospecting.py` now offsets each field centre off its nav anchor by a bounded, fully deterministic scatter (`scatter_offset()`, SHA-256 of the stable `sector|nav` key -> uniform-on-sphere direction, magnitude 0.35..0.75 x the field's own `max_field_radius`, vertical/Y component scaled 0.25x to avoid burial). The nav stays INSIDE the field footprint (offset < radius) so it's still reachable, but the centre no longer coincides with the marker. Deterministic => the committed `db/postgres/seed_phase_y_prospecting.sql` regenerates byte-stable; regenerated against the seeded content DB + reconstruct dataset (529 nodes, position-only diff vs the prior seed, applies clean `ON_ERROR_STOP=1`). No wire/server change (pure content seed). (b) remains OPEN: widening coverage to under-populated zones needs an authoritative resource-per-zone source we do not load (the reconstruct dataset is nav-centric; 76 sector/nav pairs don't resolve to an object and sectors without resolvable navs get no nodes) -- not solo-fixable without that source. Original root-cause notes below. **ROOT-CAUSED 2026-06-17.** `tools/dataimport-jsonl/generate_seed_prospecting.py` emits ONE harvestable field (`sector_objects` type 38) per (sector, nav) pair and places it at the EXACT runtime nav-point coordinates (`prow['x'/'y'/'z']`, the nav object's own position) -- so every field sits on its waypoint marker = "stacked on waypoints." Output is the committed `db/postgres/seed_phase_y_prospecting.sql` (type-38 ids >= 200000), applied at boot. "Many missing" = coverage is gated on a (sector, nav, resource) triple resolving by name to a runtime nav anchor + being a reconstruct gap; sectors whose dataset navs don't resolve get NO nodes. **Two sub-problems with different difficulty:** (a) "stacked on waypoints" -- fixable by scattering each field center off its nav anchor; BUT we have NO sector-bounds data, so a blind offset risks placing a field inside a planet/station/wall. A small bounded deterministic offset is low-risk (navs are usually open space) but still *invents* positions. (b) "missing in zones" -- needs an authoritative per-zone resource source we don't currently load (the reconstruct dataset is nav-centric; sectors without resolvable navs are simply absent). Regenerating also requires running the generator against the reconstruct dataset (`ENB_RECONSTRUCT_DB`) + a live content DB. **DECISION NEEDED (see report):** is a bounded deterministic scatter off the nav point acceptable for (a), and is there a resource-per-zone source for (b)? Reported 2026-06-17 (Veret). **Direct consequence of the Phase Y9 prospecting import.** `tools/dataimport-jsonl/generate_seed_prospecting.py` placed each prospecting node AT a nav-point position (it had no per-node coordinate source), so every node lands exactly on its waypoint -- hence "stacked on waypoints." "Many missing in zones" = the importer only emitted nodes for sectors/resources it had a nav-point anchor + reconstruct gap for (see the Y9 memory: import ONLY the by-name+by-position diff gaps). **Solo-doable (content/data, no client):** rework the prospecting seed to (a) scatter nodes within a radius of their anchor instead of co-locating on the nav point, and (b) widen coverage so under-populated zones get the expected resource set. Needs the authoritative resource-per-zone source (the reconstruct dataset / retail field-position data) -- a pure random scatter with no source is NOT acceptable (it would invent placements). First step: re-read `generate_seed_prospecting.py` + the Y9 plan/memory to see exactly what position source was used and what's available to do better. NOT a wire bug; no CV entry. |
| PB-24 | `[x]` | **Website Vault: selecting an item with a long name reflows the icon-row layout on BOTH panels, making navigation difficult.** | **FIXED 2026-06-17 (commit 7e8d2301).** Root cause: `Vault.module.css` `.vxfer__cols` used `grid-template-columns: 1fr auto 1fr` -- the middle (transfer control + selected-item name) was an `auto` column with no cap, so a long name grew it and squeezed the two `1fr` vault columns; the vault grids are `repeat(auto-fill, 64px)` (base.css `.vgrid`), so a narrower column fits fewer 64px tracks and the icons reflow/jump under the cursor. Fix: cap the middle column at `minmax(180px, 220px)` (minmax max is a hard ceiling) and give `.vxfer__mid` `max-width: 220px` + `overflow-wrap: anywhere` so the name wraps inside the fixed width instead of widening the column. Pure freya/online web (MIT) CSS; `tsc -b` + vite build green. No wire change. Reported 2026-06-17. Pure **freya/online** web UI (MIT) layout bug -- the detail/selection pane for a long item name pushes the icon grid and changes column count, so the grid jumps under the cursor. Solo-doable frontend fix (CSS: give the icon grid a fixed track count / the detail pane a fixed/scrolling width so selection never reflows the grid). Find the vault/storage screen under `freya/online/web/src/screens/`. NOT a wire bug; no CV entry. Cosmetic but cheap. |
| PB-23 | `[x]` | **Website Vault: used-slot count is wrong and blocks storing to max** (Medium). Owner saw 56/96 used on a Trader but the vault reported full with ~40 free slots. | **FIXED 2026-06-17 (commit 7e8d2301).** Root cause: a divergence between the vault VIEW and the vault WRITE over what counts as an occupied slot. The C++ server empties a vault slot by UPDATEing its row to `item_id = 0` (`Player::SaveVaultChange` PlayerSaves.cpp:1303 reads the (now empty) `item->ItemTemplateID` = 0; SaveManager.cpp:732 UPDATEs in place, NEVER deletes), so a heavily-used vault accumulates stale `item_id = 0` rows. `accountVaultStorage` (store_mail.go:207) hides them (`resolveItems(0)` fails -> slot dropped), so the page showed 56; but `freeVaultSlot` (store_mail_write.go) counted EVERY row with no `item_id` filter, so it saw all 96 slots "used" and returned `errNoVaultSpace` -> `errDestVaultFull`. Fix (3 parts, all freya/online MIT, no wire change): (1) `freeVaultSlot` now treats a slot free unless it holds a real item (`item_id IS NOT NULL AND item_id <> 0`), matching the view; (2)+(3) both write paths (`insertVaultItem` in store_vault_write.go + the loot INSERT in store_mail_write.go) changed to `ON CONFLICT (avatar_id, inventory_slot) DO UPDATE` because a reclaimed slot may still carry a stale zero row that a plain INSERT would PK-collide with. `go build`/`vet`/`test` green, gofmt-clean. Reported 2026-06-17 (Veret, 🔸 Medium). Pure **freya/online** vault storage-accounting bug (MIT) -- the "slots used" / capacity calc disagrees with the actual occupancy, so it refuses stores while space remains. Likely counting multi-slot/stacked items wrong, or double-counting, or using the wrong capacity constant for the larger Trader vault. **Solo-doable (no client):** find the vault read/write + capacity check in `freya/online/server/` (storage/vault handlers) and the web slot renderer; reconcile the "used" computation against how items actually occupy slots in the DB. Add a test pinning the count for a known item set. NOT a wire bug; no CV entry. Highest-value of the website bugs. |
| PB-22 | `[?]` | **In-game map shows EVERYTHING as "?" markers even for navs you've never been near; should only reveal a nav/waypoint once it's within scanner range.** | Reported 2026-06-17 (sigsegv). Fidelity bug: the sector map reveals all navs immediately instead of gating on proximity. Connects to the **PB-13 expose/explore findings**: a nav becomes *exposed* (shows on map, no XP) at radar `RadarRange+scan` range via `ExposedNavList`/`SaveDiscoverNav`, and *explored* (XP) only at `Signature()` range. If everything shows "?" from the start, either (a) the server is sending every nav as exposed at sector-enter (wrong -- should send only those already in the avatar's `ExposedNavList` from prior play), or (b) the proxy/0x0097 path now floods the client with all nodes (watch for interaction with the **PB-4 GalaxyMap.dat serve** -- though that's the GALAXY map, not the in-sector nav map, so confirm which map is meant). "?" = exposed-but-not-yet-identified marker. **Triage:** code-read the sector-enter nav-send path (`server/src/ObjectManager.cpp` discover/expose, see PB-13) for whether it gates on `ExposedNavList`; capture the sector-enter nav stream to see how many navs are sent and with what discover flag. Gated by server-integrity rules if a server emit changes (capture-first + CLI pin + plans/29 CV). Needs real-client repro. |
| PB-21 | `[?]` | **Equipment stat persists after unequip: wearing a higher-capacity shield/reactor then swapping to a different item KEEPS the higher capacity even though the better item isn't equipped (until relog / entering a station / death).** | Reported 2026-06-17 (Veret). **SOLO CODE-READ DONE 2026-06-17 -- the plan's original hypothesis ("unequip doesn't recompute downward") is WRONG; the server path IS symmetric.** Traced the full equip/unequip stat chain: (1) `PlayerConnection.cpp HandleInventoryMove` calls `SendAuxShip()` after BOTH equip-from-cargo (`case 1`/ToInv2, :2676) AND unequip-to-cargo (`case 2`/ToInv1, :2971) -- no missing refresh. (2) `Equipable::SetStats(Remove=true)` (`Equipable.cpp:1443`) removes EVERY tracked contribution via the `DelStat(m_StatIDs[ID])` loop (:1461-65) and zeroes the slot (`SetMaxShield(0)`/`SetMaxEnergy(0)` for shield/reactor), then `SetStats(false)` on the new item re-adds; `EquipEffects(Remove)` being a no-op (:1626) is BY DESIGN -- effect-stat IDs are pushed into the same `m_StatIDs` array (:1655/:1681) so the generic DelStat loop removes them too. (3) `Stats::_SetStat` assigns each contribution a unique `m_ValueID` (:168) and `_DelStat` erases that exact id and recomputes (`CalculateValue`->`FindMaxBuff`/`CalculateStatType`/`CalculateStat`, :283-87) -- precise, not max-collision. (4) `_DelStat` calls `_UpdateAux(STAT_SHIELD/STAT_ENERGY)` (:290) which maps straight to `SetMaxShield`/`SetMaxEnergy` (`Stats.cpp:423/431`) for both base (`"ITEM_VALUE"`) and effect (`"ITEM_EFFECTS"`) deliveries. (5) `FinishInstall` (:543) re-sends `SendAuxShip` after the delayed install. So removal recomputes the max downward AND pushes it to the diff-based aux. **Could NOT find a server-side leak.** Remaining candidates (need RUNTIME evidence, not static read): (a) **diff-baseline bug** -- `SendAuxShip` only emits CHANGED fields (`_SendAuxShip` `HasDiff`/`BuildDiff`, `PlayerClass.cpp:1219`); if the ShipIndex diff baseline for the max-shield/max-energy field isn't tracked, a `SetMaxShield(lower)` may not diff out, while the FULL aux on station/relog/death does carry it -- this is the STRONGEST remaining server candidate and is checkable by capturing the unequip 0x001B ship-aux diff; (b) current-vs-max confusion (the `ShieldUpdate`/`EnergyUpdate` recharge path vs the max); (c) pure client display caching. **NEXT (not solo): capture a cleartext proxy<->server unequip + swap, byte-pin the 0x001B ship-aux diff in `CliClient.Core`, and check whether the lowered max is actually on the wire** -- if it IS, the bug is client display; if it's NOT, it's the diff baseline (a). Gated capture-first + CLI pin + plans/29 CV per server-integrity rules; do NOT blind-fix. Relates to **Phase AG (effect-stat-review)**. Needs real-client repro. |
| PB-20 | `[x]` | **Auction House: "Energy Cannon" (a level-1 laser weapon) is almost certainly a GM-only weapon and should not be listed for sale.** | **FIXED 2026-06-17 (commit 7e8d2301).** REJECTED the first-pass `no_manu` signal (that means "not craftable," not "GM-only" -- it would wrongly drop every legit non-craftable drop/reward from the AH). Found the real structural tell: every real piece of equipment has a NON-ZERO `item_base.sub_category` (its slot/class); a zero one is a dev/test item. Across all 1601 items in the bot's equipment categories exactly TWO have `sub_category = 0` -- "Energy Cannon" (6137) and "Disruptor Mortar" (6437) -- and neither is obtainable (no manufacture/drop/prospect). Fix: added `AND sub_category <> 0` to the two equipment buckets in `botPickCandidate` (`freya/online/server/bots.go`); ore bucket left untouched (ores may legitimately be sub_category 0 and aren't affected). Structural filter, NOT a name blacklist, so future dev junk stays out automatically. `go build`/`vet`/`test` green. Pure freya/online (MIT), no wire change. Reported 2026-06-17 (Veret). |
| PB-20b | (info) | _(Disruptor Mortar, id 6437, is the only other dev weapon caught by the same filter -- noted so it isn't re-reported separately.)_ | -- not a separate report; documented under PB-20. The AhBot is listing an item that shouldn't be obtainable. "Energy Cannon" exists in `db/postgres/seed_phase_y.sql` / `db/mysql/net7.sql`. Pure **freya/online** AhBot bug (MIT) -- the bot's listing query isn't filtering out GM/dev items. There's already a per-category AhBot pricing + an Engines AH filter (recent commits) so the filter scaffolding exists. **Solo-doable (no client):** (1) confirm the item's GM/no-sell flag in the content DB (item flags / `min_level` / restriction column); (2) exclude GM-flagged items from the AhBot listing query in `freya/online/server/bots.go` -- prefer filtering on the content flag, not a hardcoded name blacklist. NOT a wire bug; no CV entry. |
| PB-19 | `[~]` | **Progen Sentinel: "Build Ammunition" skill is missing (max rank 7); unclear if it's implemented at all.** | Reported 2026-06-17 (Veret). **Confirmed real + skill DEFINITION added 2026-06-17:** primary source = client `cskill_t.ini` `[All Skills]` enumerates `59=Build Ammunition` (so it IS a retail skill, ID 59), and the Build_Ammunition wiki page gives Category=Trade, max rank 7, classes Progen Sentinel + Terran Scout, unlocked at level 30 via the mission "Bite The Bullet" (Activated=false -> Passive). Added the `<Skill ID="59" ...>` block to `server/data/Skills.xml` (Sentinel + Scout, Max=7, Quest=1, LearnLvl=30). **HONEST SCOPE:** this is the skill *definition* only -- it makes the skill exist, show in the tree, and be trainable/parseable. It does NOT implement (a) the "Bite The Bullet" unlock mission, (b) the ammo analyze/refine/manufacture recipes the skill gates, or (c) the ammo economy -- those are separate, likely-unimplemented work tied to PB-26. Real-client check: CV-PB19 (does the skill appear at rank 0/7 on a Sentinel and train correctly). |
| PB-18 | `[?]` | **`client.exe` crashes on UNDOCK from a PLANET (undock/leave-planet path).** | Reported 2026-06-15 (the cap11 = "land-and-fly-on-planet-dock-undock-crash" session crashed on the planet undock; cap12 = "leave-planet-via-gate" was recaptured afterward). DISTINCT from the historical undock/dock<->space crash, which is FIXED and owner-confirmed (see "No undock crash" memory) -- this is specifically leaving a PLANET surface. NOT yet root-caused and NOT a proven server byte-order defect; do not frame as a crash-bisection keystone. The capture is the cleartext proxy<->server leg, so it does NOT show what the client mis-parsed (the client<->proxy leg is encrypted, and the proxy re-frames/fabricates). Triage: (1) a `WINEDEBUG` crash trace on the planet-undock keypress to localize the faulting client code path; (2) diff the planet-undock server->client opcode sequence in cap11 (up to the crash) against a clean space-undock sequence to spot a missing/extra/mis-ordered frame (planet undock likely involves a sector/handoff + the `0x0097`/nav + object-create band); (3) only then, if a server/proxy emit is implicated, fix capture-first with a plans/29 CV entry. May interact with PB-4 galaxy-map serve (now that the proxy streams 0x0097) and PB-17 (loading hang). Needs the real client + trace; cannot fix blind. |
| PB-17 | `[ ]` | **Loading screen never progresses after sitting in a zone or station too long; requires client restart.** | Re-confirmed 2026-06-17 (Veret, 🔺 Major). Reported 2026-06-14. After idling in a sector or station, a subsequent load (zone transition / dock / undock) hangs on the loading screen forever and only a `client.exe` restart recovers. Cause unknown. Likely the SAME idle-timeout family as **PB-9** (idle ~5-10 min -> unresponsive): if the server reaps the player or tears down the sector while idle (PB-9 findings: 2-min player reaper refreshed by the 30s `0x3005` keepalive; 5-min empty-sector teardown), a following load waits on a sector/handoff reply that never comes -> infinite loading screen. The loading-hang debt also overlaps the historical Phase-N loading-screen-hang investigation (empty effect payloads). Triage: repro with a timer, then `docker compose logs` (server/proxy) at the moment of the stuck load to see whether the keepalive thread died, the player was reaped, or a sector handoff reply was dropped. Tied to PB-9; do not fix blind -- needs a repro + logs. |
| PB-16 | `[x]` | **Freya website galaxy map: wrong zone colors.** Aragoth sector renders as a Progen Zone but should be a **blue** zone; Proxima Centauri renders wrong but should be a **green** (Terran) zone. | Reported 2026-06-14. Pure **freya/website** (MIT) map-rendering bug -- NOT a server/wire bug. **FIXED 2026-06-14 (commit 6e1261d6).** Root cause was the hand-authored palette in `freya/online/web/src/screens/Galaxy.tsx`: it had TWO Terran swatches -- the **blue** one named `terran` (H256) and the **green** one `infiniticorp` (H150, which IS the Terran megacorp) -- so "green (Terran)" was impossible to express. Per owner: the bug report is the RETAIL primary source and is authoritative (do NOT reconcile to the content DB, whose `systems.notes` is separately lossy). Fix: collapsed the redundant `infiniticorp` into `terran` and recolored `terran` to green (H150); made `neutral` a real blue (was near-gray H250 C0.018 -> H256 C0.105); reassigned ONLY the two reported sectors -- Aragoth `progen`->`neutral` (now blue), Proxima Centauri `neutral`->`terran` (now green). Other systems were relabeled to PRESERVE their existing color across the palette swap (Sol/Beta Hydri `terran`->`neutral` stay blue; Tau Ceti/Alpha Centauri/61 Cygni `infiniticorp`->`terran` stay green), so no non-reported sector changes hue except the 3 deep maelstrom regions + Aquitaine, which sharpen from near-gray to blue as a side effect of the neutral fix. Legend updated (drops InfinitiCorp). Color is driven entirely by the hardcoded `SYSTEMS[].f`; the `/api/galaxy` faction only bridges name->sector-id, so no backend change. `tsc -b` + vite build + 23 vitest green. Distinct from PB-4 (in-game galaxy map shows nothing) and PB-13 (in-game nav/explore data). |
| PB-15 | `[x]` | **AhBot AH purchases arrive at 0% structure (damaged), not full quality.** | Re-reported 2026-06-17 (Veret) -- "any equipment bought will be at structure 0%, true stats only show when equipping + re-entering station to repair." This is the SAME bug; FIXED 2026-06-14, fix may not yet be DEPLOYED to the server the owner is playing on (verify the fix is live). Reported 2026-06-11. **FIXED 2026-06-14** (root cause (a), commit on dev). Items bought from AhBot listings showed base stats but **0% structure** (read as broken; repair at a starbase revealed the intact values). Root cause: `botPostOne` (`freya/online/server/bots.go`) inserted into `auction_listings` WITHOUT a `structure` value, so the column took its `DEFAULT NULL`. The buyout path (`store_ah_write.go` Buyout) reads `structure` from the listing and forwards it through the mailbox attachment -> inventory CLAIM (`store_mail_write.go`), so a NULL landed in the won item's cargo row. The C++ server reads structure on the game-native fraction scale (1.0 == 100%; `PlayerInventory.cpp` treats `<0.5` as damaged, `0.0` as destroyed), so the item displayed as 0%/destroyed. Fix: the bot now seeds `structure = 1.0` (full) for equipment listings -- gated on the SAME `withQuality` flag, since ores/resources have no quality and no structure (they correctly stay NULL). Player-to-player listings were always fine (they carry the seller's real structure). Pure freya-online (MIT) DB-write fix, not a wire change -- no CV entry needed; owner can spot-check by buying a bot equipment listing and confirming 100% structure on claim. `go build`/`vet`/`test` green, gofmt-clean. |
| PB-14 | `[~]` | **Analyze Item computer crashes the game; cannot break down items.** | Reported 2026-06-10. The starbase manufacturing/analyze terminal: "Analyze" (and item breakdown / dismantle) crashes `client.exe`. Server-side this is the manufacturing terminal path -- `MODE_ANALIZE` / `MODE_DISMANTLE` in `server/src/PlayerManufacturing.cpp` (analyze flow ~309-925: `GetAnalyzeChance`, `SendClientSound("analyze_1.wav")`, the `"%s analyzed %s"` broadcast, the `Analyze successful/failed` + `"Your Race can not analyze this item"` failure-message emits), driven by the manufacture opcodes `0x0079..0x007F` (`ENB_OPCODE_007E_MANUFACTURE_ACTION` + `0x007C` refinery item id), dispatched at `PlayerConnection.cpp:~531`. "Crashes" => the server most likely emits a malformed analyze/dismantle reply (or a manufacturing aux packet) the client can't parse -- the SAME class as the htonl/byte-order and aux-struct traps in CLAUDE.md, OR an out-of-bounds item/recipe ref. NOT fixable blind. **Triage (capture-first, per server-integrity rules):** (1) cleartext proxy<->server capture of an Analyze + a Dismantle attempt; (2) parse request+reply in `CliClient.Core`, pin the bytes; (3) diff the analyze/dismantle reply struct vs a retail manufacturing-band capture for a byte-order/size mismatch; (4) only then touch the emitter, with a capture-frame citation + a plans/29 CV entry. **First SOLO step (no client):** code-read `PlayerManufacturing.cpp` MODE_ANALIZE/MODE_DISMANTLE for an obvious bad emit -- unbounded `m_RefinesInto[MAX_REFINE_TO]`/recipe index, an `AddData` width mismatch like PB-1, or an htonl on a host-order field. **SOLO CODE-READ DONE 2026-06-10 (findings below; no blind fix -- all three touch wire/UI behaviour and are gated capture-first):** The breakdown executor is `Player::ManufactureTimedReturn` (`PlayerManufacturing.cpp:851`), MODE_ANALIZE/MODE_DISMANTLE case `862-1022`; the reply is built by `AuxManufacturingIndex::BuildPacket` (`server/src/AuxClasses/AuxManufacturingIndex.cpp:25`, a flag-gated walk into a fixed `PacketBuffer`) and emitted via `Player::SendAuxManu` (`PlayerClass.cpp:1302`) as `ENB_OPCODE_001B_AUX_DATA`. (1) **STRONGEST symptom match for "unable to break down items":** `PlayerManufacturing.cpp:962` `if (compList.size() == 0) return;` early-returns from the timed callback and SKIPS all three replies at `1018-1020` (`SendAuxPlayer`/`SendAuxShip`/`SendAuxManu`). On a dismantle that rolls normal-success but resolves zero components (all `Components.Item[*]` slots -1, or every `g_ItemBaseMgr->GetItem` lookup fails), the server consumes the player's ACTION and answers with NOTHING -> client manufacturing UI hangs. Server-side response-drop. (2) **Analyze success loop `882-888`:** sets `Components.Item[cID].SetItemTemplateID(cItemID)` and calls `SendItemBase(cItemID)` for ALL 6 slots unconditionally, including empty slots where `m_CurManuItem->Component(cID)` returns 0/-1 -- emitting a component of template id 0 the client then renders. (3) The `dismantleComp::operator<` (`838-849`) IS a valid strict-weak-ordering, so the upstream `//oh god, this is going to crash ... and has` comment at `964` is NOT a sort-comparator UB; `std::random_shuffle` (`968`) is removed in C++17 but still compiles in this tree. CONCLUSION: defect (1) is the best server-side candidate but is a response-drop/hang, not provably THE client crash; turning any of these into a fix changes whether/what aux packet is sent == a wire-behaviour change, so it stays capture-first (parse the analyze+dismantle reply in `CliClient.Core`, byte-pin vs a manufacturing-band capture, then fix with a capture-frame citation + plans/29 CV entry). Upstream captures with manufacture/analyze content CONFIRMED present (`capture_1` 0x7E ManufactureAction; `capture_3` "buy 2 Analyzable" + 0x7E/0x7C flows; `capture_2` decoded 0x7F) in `archive/kyp-snapshot/capturedPackets/capture_{1,2,3}.rar`. **FIX APPLIED 2026-06-15 (commit 149e9044; client.exe verification pending).** New cleartext proxy<->server captures of live-retail analyze+dismantle (cap3 = analyze-damage / analyze-success / analyze-fail; cap4 = analyze-success + dismantle-success, many runs) PROVE the retail server ALWAYS answers a manufacturing action with a `0x001B` AUX_DATA `ManufacturingIndex` reply -- there is no path where it consumes the action and emits nothing. That pins defect (1) as the bug: `PlayerManufacturing.cpp:962` `if (compList.size() == 0) return;` was an early-return out of `Player::ManufactureTimedReturn` that SKIPPED the `SendAuxPlayer`/`SendAuxShip`/`SendAuxManu` reply block (`1018-1020`), so a dismantle/analyze that resolved zero components hung the client manufacturing UI (no reply ever arrives). Fix: replaced the `compList.size()==0` early-return + the bare shuffle/sort/loop with a guarded `if (!compList.empty()) { ...sort/random_shuffle/loop... }` so control ALWAYS falls through to the three replies; the reply is sent even when zero components resolve, matching retail. Also removed the stray `20.0f;;` double-semicolon and the `//oh god, this is going to crash` comment (the comparator is a valid strict-weak-ordering -- see finding (3)). CLI parse: `ManufacturingIndexRecord` / `0x001B` already decoded + pinned in `CliClient.Core`; capture frames cited in the commit. CV entry: `plans/29` CV for the manufacturing reply (real-client analyze+dismantle no longer hangs). Integration round-trip test (assert 0x001B reply arrives, not hang) tracked as follow-up. |
| PB-13 | `[?]` | **Exploration/nav content bugs across multiple sectors** (reported 2026-06-09). (a) Inverness "Dragon's Path 1-8" missing map icons toward the lair. (b) Arduinne ~90% of nodes already explored, no XP gained. (c) Glorys Orbit: Nav "Glorys Orbit 1 & 3", "Gemini 7" undiscoverable (show as discovered only after relog, no XP). (d) Slayton: Nav "Brand 4", "Red Dragon Camp 3" undiscoverable. (e) Shepard ~90% explored, no XP. (f) Asteroid Belt Alpha ~20% explored, no XP. | NOT a single bug -- this is **content/explore-state data**, almost certainly in the `net7` content DB (nav/explore tables) and the server's exploration-grant logic, not a wire bug. Common thread "already explored, no XP" => either the navs ship pre-flagged explored for every avatar, or the explore-grant path doesn't fire/award. "Undiscoverable until relog" => the discover packet isn't sent on first proximity but the explore row exists, so relog re-syncs. Needs: (1) inspect the nav + per-avatar explore tables for these sectors, (2) find the server explore-discover + XP-grant code path. CANNOT be fixed blind -- needs the real client to repro per-sector and DB inspection. SOLO-DOABLE first step: audit the content DB for these named navs. **AUDIT DONE 2026-06-09 (findings below):** (1) `sector_nav_points.base_xp` is a RED HERRING -- 9372/9428 rows are 0, but `base_xp` is read by NO server/proxy code; `Player::AwardNavExploreXP` (`server/src/PlayerExperience.cpp:104`) computes XP from a hardcoded base of 400 scaled by `sector_data->challenge_rating` + a `Signature()` multiplier, clamped 250-1250. So zero `base_xp` does NOT cause "no XP." (2) The XP gate is purely the per-avatar EXPLORED list: `ObjectManager.cpp:625/662` awards XP only when `!obj->GetEIndex(player->ExploredNavList())` (per-avatar `net7_user.avatar_exploration`). (3) Two DISTINCT states the report likely conflates: *exposed/discovered* (`ExposedNavList`/`SaveDiscoverNav`, set at radar `RadarRange+scan` or visual `Signature` range, shows on map, NO XP) vs *explored* (`ExploredNavList`/`SaveExploreNav`+`AwardNavExploreXP`, set only when `range <= Signature()` explore range, grants XP). "Discovered after relog but no XP" is consistent with a nav that got EXPOSED but never EXPLORED (player never flew close enough). So "no XP on 90% explored" means those object_ids are already in the avatar's EXPLORED list. REMAINING (needs real-client repro, not solo): why specific navs (Gemini 7, Glorys Orbit 1&3, Brand 4, Red Dragon Camp 3) don't expose on first proximity during play, and whether any avatar has explore rows it didn't earn (forensic on `avatar_exploration` per avatar). The PB-13 "missing map icons" (Dragon's Path) is likely the same expose-on-proximity path. |
| PB-12 | `[?]` | **Screenshot keybind crashes the game.** | Reported 2026-06-09. Client-side crash (`client.exe` under WINE) on the screenshot key. Pure client/WINE issue -- the screenshot path likely hits a Win32/DirectDraw surface-capture call WINE mishandles, or a client code path. NOT a server/proxy bug. Needs a WINE crash trace (`WINEDEBUG`) on the keypress to localize. Cannot fix without the real client + trace; out of scope for server-side work. |
| PB-11 | `[?]` | **Vendor NPC plays its full dialogue animation before showing wares (up to 25+ s).** | Re-confirmed 2026-06-14 ("NPC vendors have a delay for showing you what they offer based on their speech animations, up to 20+ seconds"). Reported 2026-06-09. The vendor inventory/UI appears only after the NPC dialogue animation completes. Likely the server delays sending the vendor item list until after a dialogue/conversation step, OR the client gates the buy UI on an animation-complete event. Related to PB-3 (vendor buy). Needs a capture of the vendor-open sequence to see whether the item-list packet is late or the client is animation-gating. Triage before any change. |
| PB-10 | `[?]` | **Tractor beam + loot gravitation not visible/audible; loot camera doesn't work.** | Reported 2026-06-09. Per the "No undock crash" memory, tractor/loot is a **CV-gate** (un-citable fields need capture + real-client), NOT a crash. The loot/tractor visual+audio effects and loot-cam are client-presented off opcodes the proxy may consume/fabricate (prospect/tractor/loot are in the proxy-consumed control band -- see CLAUDE.md). Needs a capture of a loot/tractor event to see what the server emits vs what the proxy forwards to the client. Do NOT code blind. |
| PB-9 | `[?]` | **Idle ~5-10 min -> game aspects unresponsive; requires end-process + relaunch.** | Reported 2026-06-09. INVESTIGATED (2026-06-09), inconclusive -- not fixed. Findings: proxy sends `0x3005` keepalive every 30s (`proxy/UDPClient_linux.cpp` `MVASKeepaliveThread`, `KEEPALIVE_PERIOD_MS=30000`), gated on `m_recv_thread_running` (line ~1070) and `m_ConnectionActive && m_PlayerID!=0`; started at sector LOGIN `0x0002` ONLY on the global plane (`ClientToServer_linux_stubs.cpp:~509`). Server player-reaper drops a player 2 min after last access (`PlayerManager.cpp:~662`), refreshed by the keepalive (`UDP_MVAS.cpp HandleKeepCommsAlive` -> `SetLastAccessTime`). Empty sectors are torn down after 5 min (`ServerManager.cpp SECTOR_IDLE_TEARDOWN_MS_DEFAULT=300000`) but ONLY when occupancy==0, so an occupied idle sector should survive. The 2-min reaper + 30s keepalive does NOT cleanly explain a 5-10 min symptom, so the cause is unproven. Hypotheses to test WITH a repro: (1) recv thread exits during idle -> keepalive thread dies with it -> reaper fires; (2) `GetOccupancy()` wrongly hits 0 for a lone idle player -> sector torn down. **Changing server/proxy timeout logic is gated by CLAUDE.md server-integrity rules and needs a repro/capture first -- not fixing blind.** |
| PB-8 | `[?]` | **Free warp only travels in the previously locked-on warp's direction.** | Re-confirmed 2026-06-17 (Veret): "when free warping the ship always faces the previous warp direction when locked on, unable to free warp in the direction you face." Reported 2026-06-09. Free-warp (un-targeted warp) appears to reuse the last locked warp vector instead of the current heading. Likely the warp-initiate path uses a stale stored target/heading when no lock is set. Tied to PB-2 (no client position feed -> server has no current heading for a free warp, so it falls back to the last known warp vector). Investigate after PB-2's position feed is live, since a working heading feed may be the precondition. **Also affects prospecting (reporter, 2026-06-17): "when I prospect it rotates the camera back to some other rotation."** Same root: the client's current orientation/rotation (quaternion) is never fed to the server (PB-2 feeds position only), so any server-driven re-sync (free warp, prospect lock) snaps the ship/camera back to the last server-known orientation instead of the player's current heading. So PB-8's fix is not just a heading vector for warp -- the PB-2 position feed must also carry the ship's rotation quaternion, and the server must honour it on re-sync. Wire/server-gated (extends the 0x1004 feed payload + server consume), NOT a solo content fix. Needs real-client repro. |
| PB-7 | `[~]` | **Cannot use default thrusters near planets / gravity wells** (refinement of PB-2). | Reported 2026-06-09: "thrusters use MVAS which we fixed but doesn't work on planets." **CONFIRMED same-root-as-PB-2 by live capture 2026-06-15 (cap11 = land-and-fly-on-planet).** Planet-surface flight uses the IDENTICAL wire protocol as space flight -- there is NO planet-specific movement protocol: the cap11 planet-flight session streams `0x0014 MOVE` (x12) + `0x0012`/`0x0013` orient on the sector flow AND `0x1004` MVAS position to udp/3806 (x213), exactly as space flight does. So "can't fly with MVAS on a planet" is NOT a missing planet handler -- it is the SAME missing in-client position feed as PB-2 (the server only got thrust-impulse MOVE and dead-reckoned). The PB-2 fix (FreyaPosFeed.dll 0x1004 feed) covers planet flight with no extra work. Tracked under PB-2; same real-client confirm (CV-MVAS-POS in plans/29) validates planet flight too -- add planet flight to that CV's checklist. |
| PB-6 | `[~]` | **Cannot buy STACKS of items / Buy button does nothing -- only single drag works** (refinement of PB-3). | Re-confirmed 2026-06-14 ("NPC vendor buy stack button does not work even though it shows stack price and confirmation"). Reported 2026-06-09, priority for ammo + trading. **CAPTURED 2026-06-15 (cap1).** A stack buy is the SAME `0x0027 INVENTORY_MOVE` as a single buy, with the stack quantity carried in `InvMove.Num`: the cap1 stack-buy datagram decodes to `FromInv=4 (vendor), ToInv=1 (cargo), ToSlot=-1, Num=0x64 (100)`. So stacks are NOT a separate opcode and the client DOES send the requested quantity in `Num` -- there is nothing un-captured here. **Root = CV-21** (plans/29): the vendor slot was presented as a stack of 1, pinning the client's quantity slider at 1, so a "stack buy" could never be requested. CV-21's fix presents each slot at `MaxStack()`; cap1's `Num=100` stack-buy frame is the live-retail proof (CV-21 previously had only a Net-7 capture with `Num=1`). The `case 4` handler already honours `InvMo.Num` (`Cost = Price*Num`, `CargoAddItem`). **Next = CV-21 real-client check** (slider 1..stack, stack buy lands the full count in cargo, credits drop by unit*qty). No longer capture-blocked; tracked under PB-3 + CV-21. |
| PB-5 | `[x]` | **Launcher: after launching once, the Play button can't relaunch without fully closing the launcher.** | Reported 2026-06-09. FIXED 2026-06-09 (this branch). Root cause: `OnPlayClick` set `c_Button_Play.IsEnabled=false` permanently after launch and nothing ever re-enabled it (`MainWindow.axaml.cs`); `_clientRunning=true` also pins the status timer. The launcher CANNOT reliably observe the client's exit -- in PB-2 injector mode `FreyaInject.exe` resumes `client.exe` and returns immediately (`FreyaInject.cpp:117-120`), and under WINE the `wine` wrapper exits with it, so the process we Start is never the client. Fix: make Play idempotent instead of locking it -- Play is left enabled after launch, and each click first tears down the previous session's `AuthRelay` (freeing its loopback port) before launching again. No fragile PID tracking. Launcher is C# (Avalonia, `net10.0`), builds clean. |
| PB-4 | `[~]` | **Galaxy map doesn't work (shows nothing in-game).** | Re-confirmed 2026-06-14 ("galaxy map shows nothing in game"). Reported 2026-06-08. **Root cause IDENTIFIED 2026-06-14 (behavioural analysis of the retail client 0x0097 parser/renderer + the proxy galaxy-map control band) -- see plans/26 Z-5 for the full per-record wire layout.** The client populates its renderable map-node collections (systems/sectors/gates/links) ONLY from 0x0097 record sub-types 5/6/7/8/9 (and 0xb). Type 4 -- the ONLY sub-type our server emits (`SendGalaxyMap`) -- sets just the "you are here" label and the array capacity, adding NO renderable node, so the client draws an empty map. Separately, the retail proxy can serve a prebuilt `..\database\GalaxyMap.dat` to the client on the 0x2010/0x2011 control band (file-stream-then-finalize); our `HandleGalaxyMapRequest` emits only an empty 0x2011 cache signal. **Two candidate fixes, both wire changes needing the live harness + a real-client (CV) check**: (a) have the server stream the Type-5..9 record sub-types, or (b) have the proxy serve a populated GalaxyMap.dat. Next concrete step: byte-pin the Type-5/9 records against the EXISTING capture fixtures (`galaxymap_system_aragoth`, `galaxymap_sector_earth`; plus Type-4 `live_galaxymap_0097.hex`), extend the CLI decode past the name, then implement + CV. Note the behavioural read of the client got the Type-9 tail layout wrong (predicted "3 ids only"; the capture shows 3 ids + name + coords) -- trust the captures, not the inferred offsets. May be related to PB-13 map-icon gaps. **FIX APPLIED 2026-06-15 (option (b), proxy serve; commits 5b869893 + 79624c28; client.exe verification = CV-MAP pending).** Chose the proxy path: the retail proxy serves the prebuilt `GalaxyMap.dat` node stream on the `0x2010` DATA_FILE request for inner opcode `0x0097`, and `UDPClient::SendClientDataFile` (`proxy/UDPProxyToClient_linux.cpp:744`) routed that case to a no-op stub. Implemented `UDPClient::SendCachedGalaxyMap()` (same file, :643): opens `GalaxyMap.dat` (path = `FREYA_GALAXY_MAP_PATH` env, else `SERVER_DATABASE_PATH "GalaxyMap.dat"` = `./database/GalaxyMap.dat`), walks its `[len:u16][opcode:u16=0x0097][body]` frames (len includes the 4-byte header) and re-emits each record via `m_SectorConnection->SendResponse(0x0097, body, body_len)`, then `SetReceivedGalaxyMap()`. Byte-equivalent to the retail whole-file blob (per-frame RC4 advances the same keystream under `m_Mutex`). Every failure path logs a real WARNING (never silent, never crashes). docker-compose mounts `./server/data:/app/database:ro` so the committed `server/data/GalaxyMap.dat` (305 records) resolves. Proxy builds clean (`docker compose build proxy`, zero new warnings under -Wall -Wextra). Source: cap6 (login-opengalaxymap) shows the client's 0x2010/0x0097 request; CLI `GalaxyMapRecord` decodes all record Types (5/6/7/8/9) with unit tests. Primary path is the proxy serve, NOT server Type-5..9 streaming (Z-5 stays a gap for the server emit). |
| PB-3 | `[~]` | **Vendor "Buy" button does nothing; must drag item to inventory instead.** | Reported 2026-06-08. **0x001F INFERENCE DISPROVEN 2026-06-15 by live-retail capture (cap1 = purchaseammo-stack-single-sell, cleartext proxy<->server).** The Buy button emits **0x0027 INVENTORY_MOVE**, the SAME opcode as the drag path -- NOT 0x001F TRADE. cap1 has 25x `0x0027` client->server and ZERO `0x001F`. Each buy datagram decodes (after the proxy's 8-byte GameID+seq client-request prefix) to the bare `InvMove` struct in NETWORK byte order, exactly as `Player::HandleInventoryMove` ntohl's it (`PlayerConnection.cpp:2550-2555`): single buy = `FromInv=4 (vendor), ToInv=1 (cargo), ToSlot=-1, Num=1`; the field semantics match our `case 4` handler 1:1. Retail answered every 0x0027 with `0x001B` inventory aux (53x in cap1) = success. The CLI `InventoryMoveRecord` already decodes this struct correctly (all 6 BE fields). **CONCLUSION:** our `case 4` (FromInv==4 vendor->cargo) IS the correct handler and the wire format is fully understood -- there is NO missing 0x001F handler. This RECONCILES with **CV-21** (plans/29), whose fix already targets the real root: the vendor SLOT was presented with `StackCount=1`, so the client's buy-quantity slider was pinned at 1 (`Player::NPCTradeItems` now sets each slot's `StackCount=MaxStack()`). cap1 is the live-retail vendor-buy capture CV-21 explicitly LACKED -- it confirms retail presents stacks (the stack buy below carries `Num=100`), proving CV-21's logical fix against a primary source. **Next:** the CV-21 real-client check (slider now runs 1..stack, Buy completes); if it still fails after the CV-21 fix is deployed, THEN local-repro `case 4` for a deeper credit/`CheckStack`/`CargoAddItem` defect. See PB-6 for the stack variant. |
| PB-2 | `[~]` | **Can't fly with normal thrusters -- client shows you moving but server position diverges; spacebar/autopilot/warp snap you back to the real (server) position; stuck on planets where you can't warp.** | Reported 2026-06-01. ROOT CAUSE (corrected): our proxy has NEVER fed client position to the server. `proxy/UDPProxyMVAS.cpp` (which streamed `0x1004` to MVAS port 3806) existed but was already inert in the proxy build -- its position source `engine_read_process` was a `return false` stub in `proxy/Net7.cpp:288`; the only real cross-process scrape lived in `launcher/SocketTest.cpp` (the old Win32 launcher, a separate binary, not how players run today). That dormant file was then deleted in `4dc71638` (Phase AA consolidation). So this is NOT a simple "port" -- there is no working feed to restore; a NEW position source is required. The server only gets thrust-impulse `0x0014 MOVE` (no coords) and dead-reckons via `CalcNewPosition`, so it diverges; autopilot/warp are server-driven (explicit `SetPosition` + `SendLocationAndSpeed(true)`) so they re-sync. PRIMARY SOURCE: live Net7Proxy capture `proxy/local-debug/Combat-...-20260604-072157.pcapng` -- `0x1004` to udp/3806 x196 during thrust (the real proxy streamed it densely). FIX (in-client hook, loopback-datagram transport): an in-client Detours hook (`freya/client-injection/ClientPositionFeed.{h,cpp}`) reads the engine ship state in-process and sends it to the proxy as a fixed 40-byte UDP datagram on loopback port `FREYA_CLIENT_POS_PORT`=3807 (`freya/client-injection/ClientPositionShared.h`); the proxy consumer (`UDPClient::ReadClientShipPosition` -> `SendPositionIfChanged`, `proxy/UDPClient_linux.cpp`) binds 3807, drains the latest sample (latest-wins, 1.5s staleness gate), and streams `0x1004` to 3806, change-gated. **Transport was changed from named shared memory to a loopback datagram (2026-06-08)** because the SHM design structurally could NOT reach `just play-local`: there the proxy is a Linux docker process and the WINE client's Win32 named mapping is invisible to it. A loopback datagram is the ONE transport that works in all three run modes -- play-local (WINE client -> docker proxy, port published `127.0.0.1:3807:3807/udp`, loopback-only), play-online/NET7MP (WINE client -> WINE proxy), and native Win32. The proxy binds 127.0.0.1 on the Win32 build and INADDR_ANY on the docker build (host publish loopback-restricted). NO server change and NO `0x1004` wire change -- the server still consumes 0x1004 over its authenticated DTLS channel exactly as before; the position the proxy attributes is its own session's `m_PlayerID`, so a loopback feed only ever moves the feeder's own avatar (no new server trust boundary). `0x1004` wire shape still pinned by `CaptureReplayTests.MvasPosition_1004_*` (fixture `live_mvas_position_1004.hex`). DLL INJECTION (now wired, 2026-06-08): the feed ships as a minimal standalone 32-bit DLL `bin/FreyaPosFeed.dll` (`freya/client-injection/PosFeedDllMain.cpp` guarded `DllMain` -> `FreyaClientPosFeed_Start`, only inside `client.exe`) built via `just build-posfeed-dll` (needs the **i686** MinGW toolchain -- `sudo apt install gcc-mingw-w64-i686-posix g++-mingw-w64-i686-posix` -- because `client.exe` is PE32/i386 and the injected DLL must match its bitness; the 64-bit MinGW used for the proxy cannot emit it). It is deliberately NOT the legacy `ClientDetours.dll` (that links MSVC `detours.lib` mingw can't link, and hooks hardcoded client offsets specific to one build). Injection under WINE is a launch-time remote-thread inject, NOT `AppInit_DLLs`: WINE (tested wine-11.8) does not implement the `AppInit_DLLs` loader hook at all, so registering the DLL there loads it into nothing (proven empirically -- it injected into neither `client.exe` nor `notepad`). Instead a tiny 32-bit MinGW injector `bin/FreyaInject.exe` (`freya/client-injection/FreyaInject.cpp`, built alongside the DLL by `just build-posfeed-dll`) does the classic `CreateProcess(CREATE_SUSPENDED)` + `VirtualAllocEx`/`WriteProcessMemory`(dll path) + `CreateRemoteThread(LoadLibraryA)` + `ResumeThread`; on any failure it `TerminateProcess`es the child so a half-injected client never reaches the user. The launcher (`tools/LaunchFreya` `Launcher.ConfigurePositionFeedInjection`, gated on the `EnablePositionFeed` setting; `play-local` sets it true and builds the DLL+injector as a required step) stages the 32-bit DLL into BOTH `system32` and `syswow64` (WOW64 file redirection sends a 32-bit process opening `system32` to `syswow64` on a win64 prefix), then `LaunchClient` spawns `wine FreyaInject.exe <client.exe DOS path> <FreyaPosFeed.dll DOS path> -SERVER_ADDR ... -PROTOCOL TCP` instead of launching `client.exe` directly. `DllMain` emits an `OutputDebugStringA` marker on attach so injection is observable even while the feed is inert (definitive load check is `WINEDEBUG=+loaddll`). Native-Windows injection (Detours withdll) is NOT wired -- WINE path only. REMAINING BLOCKER: real-client confirmation of the engine read -- tracked as CV-MVAS-POS in `plans/29`. The build-specific read (`FreyaReadEngineShipState_Local()` in `freya/client-injection/ClientEngineOffsets.h`, included directly by `ClientPositionFeed.cpp`) is filled and committed; the CV entry proves it reads the right transform against the real client. Both proxy build targets (Linux-native docker + Win32 mingw cross) compile green with the new consumer; the producer compiles clean under mingw. Same subsystem noted in `plans/27 §"Why it broke"`. |
| PB-1 | `[x]` | **Credits underflowed to ~270 billion, change on every login.** | Fixed 2026-06-01, commit `421cd9bd` (local). `Player::SaveCreditLevel` wrote a u64 credits value through the 4-byte `AddData<unsigned long>` specialization (on Linux `u64 == unsigned long`), so the SaveManager 8-byte read pulled an uninitialized high dword. Fixed with explicit `AddData64` (8-byte LE emitter) in `PacketMethods.h`; regression test `freya/tests/server/protocol/credit_save_width_test.cpp` (3/3 pass). Server-internal IPC, no CV/CLI gating needed. |

## Notes

- **Triage of the 2026-06-19 batch (5 items, Terran Tradesman class progression + AH).**
  All NEW (PB-27..PB-31); none overlap a prior PB. Theme: most are content/data
  bugs (quest item grants, missing trainer quests, missing/mis-placed spawns) plus
  one freya/online AH bug -- NONE are server/proxy wire bugs, so none are gated by
  the capture-first server-integrity rules.
  - **Solo-doable now (no client):** PB-29 (Reconfiguration of Shields quest --
    has a wiki primary source), PB-31 (AH multi-character bid/buy -- freya/online),
    and the AUDIT half of PB-27/PB-28/PB-30 (confirm the defect in content data).
  - **Needs a primary source before the FIX (not blocked on the real client, blocked
    on authoritative content):** PB-28 (the CORRECT Hull Patch step item is unknown),
    PB-27 (whether retail spawned Scuttle Nymphs in High Earth at all -- don't invent
    a spawn). PB-30 is likely fixed-by-PB-29 (prereq chain) -- verify, don't assume.
  - Per the standing instruction these are TRACKED only -- no fixes started until
    the owner gives the go-ahead.
- **Triage of the 2026-06-17 Discord batch (13 items from Veret + sigsegv).**
  Mapped to PBs; 5 were already tracked, 8 are new (PB-19..PB-26).
  - Already tracked (re-confirmation stamped): loading-screen hang -> **PB-17**;
    vendor buy-stack button -> **PB-6**; vendor speech-animation delay -> **PB-11**;
    AH items at 0% structure -> **PB-15** (already FIXED 2026-06-14 -- verify it's
    deployed to the live server); free-warp direction -> **PB-8**.
  - New: Build Ammunition skill -> **PB-19**; Energy Cannon GM weapon in AH ->
    **PB-20**; shield/reactor capacity persists after swap -> **PB-21**; map shows
    all navs as "?" -> **PB-22**; website vault slot count wrong -> **PB-23**;
    website vault long-name reflow -> **PB-24**; mining nodes stacked on waypoints
    -> **PB-25**; Mordanite/Gravity-Link rip recipe -> **PB-26**.
  - **Honest solo-vs-blocked split** (unlike the 2026-06-09 batch, several of
    these ARE solo-fixable without the real client):
    - **Solo-doable now (content/website, no client, no wire change):** PB-20
      (AhBot GM filter -- freya/online), PB-23 + PB-24 (website vault -- freya/online),
      PB-25 (prospecting seed -- content data), PB-26 (Mordanite rip-recipe audit
      -- content data), PB-19 (Build Ammunition -- needs a primary-source skill
      list to confirm it's real before adding).
    - **Need real-client repro / capture-first (server/proxy wire, gated by the
      CLAUDE.md server-integrity rules):** PB-17 (idle loading hang, ties to PB-9),
      PB-21 (equip stat recompute), PB-22 (nav expose-on-proximity, ties to PB-13),
      PB-8 (free-warp direction, ties to PB-2).
  - Per the owner's instruction, these are TRACKED only -- no fixes started until
    the owner gives the go-ahead.
- **Triage of the 2026-06-09 batch.** Of the newly-reported bugs, exactly one
  was solo-fixable from code alone: PB-5 (launcher relaunch), now fixed. The
  rest fall into two buckets, both of which honestly CANNOT be fixed blind:
  - **Need a real-client repro / packet capture** before any code change:
    PB-6 (vendor buy/stacks), PB-7/PB-2 (thrusters near planets), PB-8 (free
    warp direction), PB-9 (idle hang -- investigated, cause unproven), PB-10
    (tractor/loot effects -- CV-gate), PB-11 (vendor dialogue gating), PB-12
    (screenshot crash -- client/WINE), PB-4 (galaxy map).
  - **Content/DB data dig** (not a wire bug): PB-13 (per-sector nav/explore/XP).
    Solo-doable next step is to audit the `net7` content DB for the named navs.
  Anything touching the server/proxy wire path is additionally gated by the
  CLAUDE.md server-integrity rules (primary-source citation + CLI parse/tests +
  plans/29 CV entry), so guess-fixes are not acceptable.
- PB-1 fix is local-only (commit `421cd9bd`) -- not yet pushed/deployed per
  the standing "author, don't deploy" instruction.
- PB-2 is a proxy wire-behaviour change: primary-source citation is the live
  `0x1004` capture (`proxy/local-debug/Combat-...-20260604-072157.pcapng`),
  CLI parse of `0x1004` exists (`MvasClient` + `CaptureReplayTests`), and
  `plans/29` CV-MVAS-POS tracks real-client confirmation. The earlier "port the
  deleted `UDPProxyMVAS.cpp`" framing was wrong -- that file's position source
  was a `return false` stub in the proxy build, so nothing functional was lost
  to "port back". The feed is a NEW in-client-hook source, scaffolded inert.
