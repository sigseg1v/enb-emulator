# Phase K — in-game opcode handlers + UDP proxy plane

Goal: take the Phase J "TLS terminates, handshake works, opcodes are dispatched" baseline and turn it into actually playable login → enter sector → see other players. Each item is sized to be portable independently (file-level WIN32 walls drop one at a time).

## Carry-over from Phase J

- TCP 3500 (SECTOR_SERVER_PORT) has no listener on Linux. `HandleMasterJoin` sends a hardcoded ServerRedirect there.
- `ProcessGlobalServerOpcode` (`proxy/ClientToGlobalServer.cpp`, 286 LOC, ~15 handlers) is a Linux logging stub.
- `ProcessSectorServerOpcode` (`proxy/ClientToSectorServer.cpp`, 757 LOC, ~50+ handlers) is a Linux logging stub.
- UDP proxy plane (`proxy/UDPProxyMVAS.cpp`, `UDPProxyToClient.cpp`, `UDPProxyToGlobal.cpp`, `UDPClient.cpp`) compiles to nothing on Linux.
- Login ticket handoff (login → game server) not wired. Win32 path is `RegisterSectorServer` over SSL on SSL_LOCALCERT_LOGIN_PORT (3807); endpoint not bound on Linux.
- `net7ssl` has no per-connection threading — single-request / single-response per TLS session. Fine for dev; not fine for load.
- `/who.cgi` is a 404 stub (depends on PlayerManager).

## Items

- [x] Bind TCP 3500 (SECTOR_SERVER_PORT) accept loop in the Linux proxy and publish it on the host docker-compose port map.
      Status: done — bind was already there (proxy/ServerManager.cpp:76 unconditional `TcpListener sector_comms(..., SECTOR_SERVER_PORT, ..., CLIENT_TO_SECTOR_SERVER)`); accepted sockets already route through the same Connection ctor / RC4 handshake / framed dispatch as 3801. Only missing piece was the host port publish.
      Touches: `docker-compose.yml` (added `"3500:3500/tcp"` under proxy.ports).
      Notes: Verified end-to-end: `exec 3<>/dev/tcp/127.0.0.1/3500` succeeds from the host, proxy logs `accept on port 3500 from 172.18.0.1` + `DoKeyExchange: ...` (handshake starts then fails on empty input — expected for a no-op test connection). Integration suite still 5/5.

- [x] Port `ProcessMasterServerOpcode` chain end-to-end (avatar select → ServerRedirect). Currently `HandleMasterJoin` is the only handler exercised; full table at `proxy/ClientToMasterServer.cpp:Process…`.
      Status: done — ProcessMasterServerOpcode in `proxy/ClientToMasterServer.cpp:30-43` only dispatches one opcode (0x0035 MASTER_JOIN); everything else falls into the UNRECOGNIZED OPCODE default and logs. Linux HandleMasterJoin (Phase J commit 5bd0afd / 6ffe642) drives the full SendMasterLogin → ServerRedirect flow via the UDP plane subset (commit a357d11), with a Phase J fallback to hardcoded local ServerRedirect on UDP timeout. The "chain end-to-end" framing was based on a wrong assumption that the master server had multiple handlers — the design is much simpler: master server = one opcode in, one opcode out. The deeper avatar-select flow lives in ProcessGlobalServerOpcode, not here.
      Notes: live `MasterJoin.LiveMasterJoinReturnsServerRedirect` test covers this end-to-end (6/7 in `just integration-test`). The UDP plane fallback path is exercised because server-side HandleMasterHandoff is still a stub (carries forward as a server-side Phase K item, separate from the proxy port).

- [~] Port `ProcessGlobalServerOpcode` handlers. Group by dependency on UDP plane vs. pure-TCP — port pure-TCP ones first.
      Status: in progress — five handlers ported and verified end-to-end this phase. (a) VersionRequest 0x0000 → VersionResponse 0x0001 (pure-TCP). (b) GlobalConnect 0x006D → GlobalAvatarList 0x0070 (via UDP_GLOBAL_SERVER_PORT 3810; both happy path and STRESS_TEST_CLOSED rejection covered). (c) GlobalTicketRequest 0x006E → GlobalTicket 0x006F (via SendAvatarLogin; failure path tested — galaxy-full timeout returns response_code=1002, exercises UDP_Global.cpp's wire-size fix). (d) GlobalDeleteCharacter 0x0071 → GlobalAvatarList refresh (via SendDeleteCharacter UDP round-trip; landed alongside PacketMethods.h ExtractLong cast bugfix). (e) GlobalCreateCharacter 0x0072 → GlobalAvatarList refresh (via SendCreateCharacter UDP round-trip; landed alongside ColorInfo/ShipData/GlobalCreateCharacter long→int32_t migration AND the GlobalAvatarListCodec AvatarData offset fix AND the sql_query AppendValue UB fix). Only **ProcessGlobalTicket** carries forward — it's the inbound-from-server side that drives 0x006F handoff post-AvatarLogin; the integration test today exercises only the failure branch where the timeout never produces a ticket. The Linux dispatcher lives in `proxy/ClientToServer_linux_stubs.cpp`; unimplemented opcodes still log + return for visibility into what real clients send.
      Touches done: `proxy/ClientToServer_linux_stubs.cpp` (Linux ProcessGlobalServerOpcode dispatch — VersionRequest, GlobalConnect, GlobalTicketRequest, GlobalDeleteCharacter, GlobalCreateCharacter, GlobalError forwarding, GlobalAvatarList forwarding), `tests/client/version_request_test.cpp` (live test), `tests/integration/CliClient.IntegrationTests/Opcodes/{GlobalConnect,GlobalTicketRequest,GlobalDeleteCharacter,GlobalCreateCharacter}Tests.cs` (round-trip coverage), `server/src/PacketMethods.h` (ExtractLong cast `long*` → `int32_t*`), `server/src/mysql/mysqlplus.cpp` (sql_query AppendValue helper — see decisions log 2026-05-24), `common/include/net7/PacketStructures.h` (ColorInfo/ShipData/GlobalCreateCharacter long→int32_t), `tools/cli-client/src/CliClient.Core/Opcodes/Inbound/GlobalAvatarListCodec.cs` (AvatarData offsets race=46/prof=50/gender=54/mood=58 — ATTRIB_PACKED has no implicit padding).
      Touches remaining: ProcessGlobalTicket (the post-AvatarLogin handoff handler — exercised today only via the timeout/failure branch); `proxy/ClientToGlobalServer.cpp` (still WIN32-walled — full removal needs every handler ported).
      Notes: cli-integration 38/38 green from a fresh `docker compose down -v`. Coverage floor: TestedOpcodes.cs MinTestedCount 7→11 (added 0x006E/0x006F/0x0071/0x0072).

- [~] Port `ProcessSectorServerOpcode` handlers, same staging.
      Status: Wave 3 (2026-05-24, commit bd6f218) lit up the remaining 5 sector opcodes — Linux dispatch is now parity-complete with Win32 ClientToSectorServer.cpp:15-110. Done by porting `UDPClient::ForwardClientOpcode` out of WIN32-walled `proxy/UDPProxyToClient.cpp:19` into `proxy/UDPClient_linux.cpp` (5 lines: same m_PlayerID/m_ConnectionActive gate + SendResponse(m_ClientPort,...) body — m_ClientPort already populated by existing HandleMasterJoin → SendMasterLogin → 0x2009 confirm path). 10/10 sector cases now match Win32, including: original 5 stubs (LOGIN/MOVE/STARBASE_ROOM_CHANGE/LOGOFF_REQUEST/SERVER_HANDOFF) which now ALSO forward via the new bottom-of-switch `g_ServerMgr->m_UDPConnection->ForwardClientOpcode(...)` (Win32 forwards them too); plus the 5 new handlers — (f) START_ACK 0x0006 forwards + sends 0x3008 STARBASE_LOGIN_COMPLETE or 0x3004 PLAYER_SHIP_SENT based on SectorID, marks LoginComplete on both UDPClient and UDPConnection. (g) TURN 0x0012 rate-limited 1 per 250ms via m_Turn_Sent. (h) TILT 0x0013 same cadence with m_Tilt_Sent. (i) ACTION 0x002C explicit forward + ProcessAction_Linux helper (all 6 Win32 sub-cases are //commented-out logs, no-op). (j) WARP 0x009B HandleWarp_Linux + m_SectorTCPRequest=false + bottom forward; pre-warp SendPositionIfChanged is WIN32-walled in UDPProxyMVAS.cpp — skipped on Linux as documented carry-over (rubber-band UX nit, not correctness). KillTCPConnection from Win32's PLAYER_SHIP_SENT branch lives in WIN32-walled UDPProxyToClient.cpp — skipped; equivalent close happens naturally as server tears state. Default case also forwards (Win32 line 108): server is authority on opcode-meaningful-in-state, proxy just relays.
      Touches done: `proxy/UDPClient_linux.cpp` (UDPClient::ForwardClientOpcode, Wave 3), `proxy/ClientToServer_linux_stubs.cpp` (full 10-case Wave 3 dispatch + ProcessAction_Linux/HandleStarbaseRoomChange_Linux/HandleWarp_Linux helpers), `tests/client/sector_login_test.cpp` (live smoke test on port 3500 — still green).
      Touches remaining: `proxy/ClientToSectorServer.cpp` (WIN32-walled — kept for the WIN32 build; Linux dispatch is parity-complete in the linux-stubs file).
      Notes: cli-integration 38/38 green from clean `docker compose down -v` rebuild after Wave 3. Server-side will now receive sector opcodes; without the bidirectional UDPProxyToClient.cpp port we can't observe server-pushed TCP/UDP responses back to the client yet.

- [~] Port the UDP proxy plane (`UDPClient`, `UDPProxyMVAS`, `UDPProxyToClient`, `UDPProxyToGlobal`). POSIX UDP sockets + blocking recv thread.
      Status: minimum-viable subset done — HandleMasterJoin now drives a real SendMasterLogin -> server:3808 UDP exchange.
      Touches: `proxy/UDPClient_linux.cpp` (new, ~390 LOC; POSIX socket + bind to INADDR_ANY:0 ephemeral + connect to game-server:3808, pthread-launched blocking recv thread, full subset for SendMasterLogin + MasterLoginConfirm + WaitForResponse + FixedClientComm; getenv("NET7_GAME_SERVER_HOST") default "server" via getaddrinfo, no hardcoded IP), `proxy/ClientToMasterServer.cpp` (Linux HandleMasterJoin now mirrors the Win32 path: SendMasterLogin -> set port/ip/sector -> SendServerRedirect; falls back to the Phase J option-b local ServerRedirect on -1 timeout), `proxy/Net7.cpp` (Linux main constructs `UDPClient(MVAS_LOGIN_PORT, FIXED, ip_internal)` and registers it as BOTH m_UDPConnection and m_UDPClient — Win32 used two distinct FIXED+MULTI instances; on Linux we only port FIXED so one object serves both roles).
      Notes: docker logs show "UDPClient: resolved game server 'server' -> 172.18.0.6 / UDPClient: bound UDP <ephem> (src) -> 172.18.0.6:3808". Live integration test `MasterJoin.LiveMasterJoinReturnsServerRedirect` goes green; `just integration-test` 6/6. SendMasterLogin currently times out after ~5s (server-side HandleMasterHandoff is "Unimplemented opcode" stub — see server/src/ClientToGlobalServer.cpp:488) and falls through to the local ServerRedirect; the proxy wiring is correct end-to-end. Still WIN32-walled on Linux (not on the MasterJoin->ServerRedirect path; portable independently as their opcode handlers land): position-update thread (`UDPProxyMVAS::MVASThread`), avatar/ticket/global-error UDP path (`UDPProxyToGlobal.cpp` minus the SendMasterLogin slice that lives in UDPClient_linux.cpp), keep-alive / packet-sequence resend, OpenMultiPort / CLIENT_TYPE_MULTI_PORT (client-only).

- [x] Wave 6 (2026-05-24): server→client UDP fan-out (`UDPProxyToClient.cpp` Linux port).
      Status: done — `proxy/UDPProxyToClient_linux.cpp` (new, ~370 LOC) implements the master-plane recv side that was missing from Wave 3: `ProcessClientOpcode` (direct passthrough to `m_SectorConnection->SendResponse`), `IncommingOpcodePreProcessing` (LOGOFF / SERVER_HANDOFF / START state transitions), `SendClientDataFile` (0x2010 — forward verbatim except 0x0097 galaxy map which is logged), `SendPacketSequence` + `SendLoginPacketSequence` (full reliable-delivery + split-packet reassembly, mirrors Win32 UDPProxyToClient.cpp:138-351 line-for-line with usleep/check_memory swaps), `SendClientPacketSequence` (walks inner [size,opcode,data] tuples; dispatches one TCP frame per opcode via SendResponse since the WIN32-only Queue*/SendQueuedPacket batchers don't exist on Linux), `HandleCustomOpcode` (minimal — 0x2020 stage confirm + 0x100A terminate; launcher-side opcodes 0x2011-0x2019 return false to let the outer raw-forward path fire), `HandleStageConfirm` (sends 0x2021 ACK back to server), `SendCommsAlive`, `RecordLastHandoff`, `KillTCPConnection` no-op. RecvThread switch in `proxy/UDPClient_linux.cpp` extended with the four new dispatches (0x2010 / 0x2016 / 0x201A / default → ProcessClientOpcode). `proxy/Connection.cpp` Linux SendResponse now takes `m_Mutex` around m_CryptOut + m_SendBuffer + SendAll because the UDP RecvThread is now a concurrent producer with the TCP RecvThread. File-scope `bool g_ShuttingDown = false;` and `uint32_t time_debug = 0;` defined in the new file (Win32 origins are WIN32-walled). What this does NOT include: SendDataFileToClient cache path (depends on WIN32-walled ClientToSectorServer.cpp::SendDataFile) — galaxy-map cache request is logged-no-op; launcher Queue* helpers — opcodes 0x2011/0x2012/0x2013/0x2014/0x2018/0x2019 are forwarded raw rather than batched.
      Touches: `proxy/UDPProxyToClient_linux.cpp` (new file), `proxy/UDPClient_linux.cpp` (RecvThread switch + file-comment update), `proxy/Connection.cpp` (Mutex around Linux SendResponse + comment update).
      Notes: clean build, cli-integration 38/38 green from existing stack. Wave 6 unblocks Task #69 (sector LOGIN 0x0002 → START round-trip) — server can now reply to a sector-port client and the proxy will deliver the reply over TCP.

- [!] Wire ticket handoff: bind SSL_LOCALCERT_LOGIN_PORT (3807) on the game server, validate ticket against the same store that net7ssl issued from. Decide: shared MySQL `tickets` table vs. AF_UNIX SOCK_DGRAM push from login to server.
      Status: blocked / re-scoped after Win32 design discovery — there's no cross-process ticket state to share. The Win32 `AccountManager::GetUsernameFromTicket` (login-server/Net7SSL/AccountManager.cpp:839) and the server-side equivalent (`server/src/AccountManager.cpp`) both validate a ticket by `strchr(ticket, '-')` and taking the prefix as the username. The format is `<username>-<rand()>`, with no signature, MAC, or shared secret. "Validation" is effectively a username extraction; anyone who knows a valid username can construct a ticket the server will accept. SSL_LOCALCERT_LOGIN_PORT 3807 is the *other* direction (game server registers itself with login, not ticket transfer) — see Win32 `SSL_Connection.cpp:628 RegisterSectorServer(m_IpAddress, port_number, num_sectors, username)` which adds the sector server to the login service's known-sectors list for ServerRedirect routing decisions.
      Reason for `[!]`: closing as "blocked" because the original framing was wrong; building real ticket sharing now would diverge from Win32 parity. Real cryptographic ticket validation belongs in a future security-hardening Phase, not Phase K. The minimum-viable port already does the right thing — login issues `<user>-<rand>`, proxy/server parses with `strtok(ticket, "-")` and trusts it.
      Touches done (parity): `login-server/Net7SSL/LinuxAuth.cpp:82 BuildTicketLocked` (issues `<user>-<rand>`), `proxy/ClientToGlobalServer.cpp:134` (Win32 path: `strtok(ticket, "-")`).
      Touches remaining (parity): port the `strtok` line into the Linux HandleGlobalConnect when that handler is implemented (carries with `ProcessGlobalServerOpcode` item).
      Open follow-up (out of scope for Phase K): replace string-format ticket with HMAC-signed token, per RFC 6750-style bearer; shared MySQL or Redis ticket store; cap-share between processes via 60-second TTL.

- [x] End-to-end opcode round-trip test against the live proxy. `tests/client/master_join_test.cpp`: post-handshake, send MasterJoin (0x0035), expect ServerRedirect (0x0036) carrying SECTOR_SERVER_PORT (3500).
      Status: done — env-gated like the other live tests (`NET7_TEST_PROXY_HOST` / `NET7_TEST_PROXY_PORT`). Builds via `gtest_discover_tests`; run with `NET7_TEST_PROXY_HOST=127.0.0.1 NET7_TEST_PROXY_PORT=3801 ctest -R MasterJoin`.
      Touches: `tests/client/master_join_test.cpp`, `tests/CMakeLists.txt`.
      Notes: Verified pass against the running proxy container — `[ OK ] MasterJoin.LiveMasterJoinReturnsServerRedirect` + proxy logs `<client> MasterJoin avatar_id=0 ToSectorID=0 FromSectorID=0`. Probes for `port=3500` at offsets {8, 20} to handle both Win32 and Linux ServerRedirect layouts (see PacketStructures.h follow-up below).

- [x] Replay-test promotion: `tests/client/replay_test.cpp` LivePostHandshakeReplay now keeps both directions of the captured port-3387 stream (real 68-byte Win32 MasterJoin → 14-byte ServerRedirect), enables `verify_response_opcode`, and asserts `packets_received > 0` and `opcode_mismatches == 0`. This complements `master_join_test` (which uses synthesized frames) by replaying actual recorded Win32 client bytes through the live RC4 stream.
      Status: done — LivePostHandshakeReplay went from "send didn't error" to validating that captured Win32 MasterJoin produces a 0x0036 ServerRedirect under live RC4. 8/8 integration tests green; test time +5s as it now blocks on the response.
      Touches: `tests/client/replay_test.cpp`.

- [x] Wire `master_join_test` into the `just integration-test` target.
      Status: done — added `master_join_test` to the cmake `--target` list and `MasterJoin` to the ctest `-R` filter. `just integration-test` now reports 6/6 green (5 prior tests + new MasterJoin round-trip).
      Touches: `justfile`

- [x] PacketStructures.h `long` → `int32_t` migration. The structs sent over the wire use `long`, which is 4 bytes on Win32 and 8 bytes on Linux x86_64. This makes MasterJoin 64 vs 108 bytes and ServerRedirect 10 vs 18 bytes. Half the bug also lived in `proxy/Connection.cpp` Linux SendResponse, which stamped `size = payload + sizeof(long)` (= 8) leaving a 4-byte gap between header and payload.
      Status: in progress — wire structs on the Linux dispatch + UDP master plane migrated. (a) Linux SendResponse `sizeof(long)` → `sizeof(EnbTcpHeader)`. (b) `ServerRedirect` 18B→10B (`port` now at offset 8 on every platform). (c) `VersionRequest` 16B→8B — was working by coincidence (test sends 8B payload; Linux struct's 8B `long` fields aliased the wire bytes thanks to LE truncation in `(uint32_t) request->Major` plus zero-padding for `Minor`). (d) `MasterJoin` 108B→64B — this was the real win. Before: HandleMasterJoin read `avatar_id_lsb`/`ToSectorID` from offsets 32/40 (8-byte field layout), saw zeros, hit the `SendMasterLogin timed-out` fallback path. After: log line now reads `avatar_id=1 ToSectorID=1` from the synthesized test, and `avatar_id=4150549696 ToSectorID=45151` from the captured Win32-client replay — actual captured values, finally parsed correctly. (e) `EnbUdpHeader` 20B→12B + ATTRIB_PACKED — `player_id` + `packet_sequence` were both `long`, so every UDP frame between proxy and server carried an 8-byte gap that a real Win32 client never produces. Server and proxy recompile from the same header so the size flip is symmetric on the Linux-only path. Remaining: other wire structs not yet exercised by the Linux dispatch (GlobalAvatarList, ServerHandoff, GlobalCreateCharacter, Login, et al.) and per-handler memcpy targets.
      Touches done: `proxy/Connection.cpp` (Linux SendResponse), `common/include/net7/PacketStructures.h` — **complete migration**, every `long`/`unsigned long` wire-struct field declaration converted to `int32_t`/`uint32_t`; per-struct comments document why; structs covered in order of when they landed: (a) early 2026-05-24 wave: ServerRedirect, VersionRequest, MasterJoin, EnbTcpHeader, EnbUdpHeader; (b) GlobalAvatarList family — AvatarData/AvatarInfo/Galaxy/GlobalAvatarList::num_galaxies — restored 2042B canonical wire size from 2518B Linux x86_64; (c) ColorInfo / ShipData / GlobalCreateCharacter — restored 17B/207B/539B canonical wire sizes from 21B/226B/571B; (d) commit a82459d sweep (this session): WarpPacket, InvMove, InvSort, ItemState, ChangeBaseAsset, Create, ServerParameters, Login, Navigation, CreateAttachment, DecalItem, Decal, NameDecal, ColorizationItem, Colorization, AvatarDescription, Subparts, ConstantPositionalUpdate, FormationPositionalUpdate, RequestTarget, SetInterface, SetTarget, ActionPacket, ActionPacket2, ClientSetTime, VerbRequest, CameraControl, LogoffRequest, TriggerEmote, NotifyEmote, OptionPacket, SelectTalkTree, ChatStream, ClientChat, ClientChatRequest, ClientChatList, ClientChatError, ClientChatEvent, ClientSkillsRequest, StarbaseAvatarChange{,_S2C}, StarbaseRoomChange, StarbaseRequest, Relationship, ObjectEffect, ObjectToObjectEffect, InitRenderState, ActivateRenderState, SimplePositionalUpdate, PlanetPositionalUpdate, ComponentPositionalUpdate, AdvancedPositionalUpdate, EquipUse, AbilityUse, StarbaseSet, ShipInfo, CTARequest, MovePacket, SkillAction, SkillUse, MissionDismissal, MVASHandoff, ManufactureData, ManufactureTechLevelFilter, FindMember (+ nested fm_item), RecustomizeAvatarStart, RecustomizeShipStart, RecustomizeShipDone, RecustomizeAvatarDone. Knock-on fix: `Player::SetupWarpNavs(short, long *)` decl/defn updated to take `int32_t *target_id` to match WarpPacket.TargetID's migrated type. Also: `server/src/UDP_Master.cpp` (related wire bugs: `sector_id` was over-read 8B for a 4B field; termination packet sent `sizeof(long)=8` where wire expects 4); `server/src/UDP_Global.cpp` (HandleGlobalTicketRequest cast fixes; AVATARLOGIN_CONFIRM reply size 8B→4B); `tests/client/master_join_test.cpp` (probe simplified to {8}).
      Notes: post-Phase-R there is only one PacketStructures.h (under common/) — sibling copies under server/src/ and login-server/Net7SSL/ no longer exist. Post-sweep: server + proxy + login-server build clean; `just cli-integration` 38/38 green from a fresh `docker compose down -v` rebuild. The sweep does NOT cover raw byte-buffer `*((long *) &data[N])` reads in `server/src/SaveManager.cpp` and elsewhere — those are the same class of Linux-port bug (8B reads from 4B wire slots) but live outside PacketStructures.h; tracked as a separate follow-up since they're more invasive (each call site needs the slot offset and surrounding reads verified against the wire frame).

- [x] Per-connection threading in net7ssl (Linux) — detached worker thread per accepted SSL session so a slow handshake doesn't head-of-line block other connects.
      Status: done — `SSL_Listener::HandleAcceptedConnection` runs SSL_accept + read + dispatch + write + close on a detached `std::thread`. Capped at 64 concurrent workers (std::atomic counter); over-cap connections are refused with a log line so a flood can't fork-bomb. Lighter than the Win32 `SSL_Connection` model (no connection pool, no persistent thread per client) but matches the request shape — each TLS session is one round trip.
      Touches: `login-server/Net7SSL/SSL_Listener.h` (+13 LOC: atomic counter, HandleAcceptedConnection decl, <atomic> include), `login-server/Net7SSL/SSL_Listener.cpp` (-50/+65 LOC: inline accept body moves into HandleAcceptedConnection, listener launches detached thread).
      Notes: Verified end-to-end — fired 5 concurrent `curl https://.../AuthLogin` and the proxy log shows 5 interleaved "Accepted SSL connection" / "SSL handshake OK" / "HTTP: GET /AuthLogin" lines instead of strict serial ordering. `just integration-test` 6/6 still green.

- [x] Proxy↔server **global UDP control plane** (UDP_GLOBAL_SERVER_PORT 3810) — wire endpoint for the avatar-list handoff that the kyp-era TCP cluster (SSL_LOCALCERT_LOGIN_PORT 3807) used to carry before Phase Q deleted it. Round-trip path: client sends 0x006D GlobalConnect on TCP 3805 → proxy `UDPClient(global).SendTicket` writes `[u32 0x2002 TICKET]` to server:3810 → server `UDP_Connection::HandleGlobalOpcode` (server/src/UDP_Global.cpp:43) routes through `ProcessTicketInfo` → reads account from net7_user (status, ticket username via strtok) → constructs Player → `SendAvatarList` writes `[u32 0x2003 AVATARLIST]` back to proxy:UDP → proxy forwards as 0x0070 GLOBAL_AVATAR_LIST on TCP 3805.
      Status: done — cli-integration covers both branches of the plane: `GlobalConnectTests.ValidTicket_RoundTripsThroughUdpGlobalPlane_ReturnsAvatarList` (happy path → 0x0070 GlobalAvatarList) AND `GlobalConnectTests.StressTestClosedAccount_GlobalConnect_ReturnsGlobalErrorCode12` (error path → 0x0075 GlobalError code 12 STRESS_TEST_CLOSED). The error path uses an out-of-pool seed account `cli_test_status0` (id 9000010, status=0) so the happy-path Pool stays clean. 35/35 cli-integration green.
      Touches: `tests/integration/CliClient.IntegrationTests/Opcodes/GlobalConnectTests.cs` (sends 0x006D with `[u32 ticket_len_be][char ticket[len]][NUL]` payload; happy-path test waits for 0x0070 and fails loud with the G_ERROR code if 0x0075 arrives instead; error-path test does the inverse — waits for 0x0075, fails loud if 0x0070 arrives since that would mean someone weakened the server's status check), `tests/integration/CliClient.IntegrationTests/Coverage/TestedOpcodes.cs` (+3 entries for 0x006D + 0x0070 + 0x0075, `MinTestedCount` 4→7), `tests/integration/CliClient.IntegrationTests/TestAccounts.cs` (`StressTestClosed` accessor out-of-Pool), `tests/integration/CliClient.IntegrationTests/Smoke/HarnessSmokeTest.cs` (also asserts seed.sql carries the out-of-pool fixture), `tests/integration/CliClient.IntegrationTests/Fixtures/seed.sql` (6th account `cli_test_status0`).
      Notes — diagnostics that lit up while landing this:
       - **Seed-account `status` was the blocker.** Server `ProcessTicketInfo` (UDP_Global.cpp:138-143) hard-rejects `status==0` with `G_ERROR_STRESS_TEST_CLOSED` (12). Pool was seeded with status=0 (boilerplate default from the pre-Phase-K era when login was the only path exercised). Real accounts use status=100 (cf. `net7_user.sql` admin row). Fixed in `tests/integration/CliClient.IntegrationTests/Fixtures/seed.sql` — status now 100 for all five `cli_test0N` accounts plus an enum-table comment.
       - **Proxy's `g_GlobalErrorMsg[]` was truncated.** Table only had codes 0-11; AccountManager.h defines through code 14. When the server returned code 12 the proxy's `Connection::GlobalError` (proxy/ClientToServer_linux_stubs.cpp) logged "unknown error code 12 — dropping" and the client never saw the rejection, masking the seed bug behind a generic timeout. Fixed: full 0-14 table with comments tying each entry to its `G_ERROR_*` constant.
       - **GlobalAvatarList codec landed (third commit, this session).** PacketStructures.h's AvatarData/AvatarInfo/Galaxy/GlobalAvatarList migrated from `long` to `int32_t` to restore Win32 wire-format parity (mirrors the e74f07c MasterJoin migration). Server rebuilt: GlobalAvatarList now serialises to the canonical 2042 bytes (was 2518 on Linux x86_64 with 8-byte longs — silently divergent from Win32). New `GlobalAvatarListCodec` (tools/cli-client/src/CliClient.Core/Opcodes/Inbound/) decodes the full struct: 5 AvatarSlot pairs (AvatarInfo + AvatarData), num_galaxies, and up to 2 Galaxy entries with name/ip/port/player-counts. Happy-path test now asserts `WireSize == 2042`, all 5 slots zero-filled (seeded `avatars` table is empty), galaxy[0].name non-empty, GalaxyID=1, MaxPlayers>0 — these are stronger evidence than "non-empty payload" that the migration took.
       - **The escape-hatch test (`[!]` blocking) is now gone.** The cli-integration Phase T plan can re-open Items 10-12 (enumerate sectors/missions/items workflows) once a GlobalAvatarList codec lands and an avatar can actually be selected → enter sector.
       - **Error-path coverage added (second commit).** `StressTestClosedAccount_GlobalConnect_ReturnsGlobalErrorCode12` validates the inverse path: client → proxy → server (rejects status=0 in `ProcessTicketInfo`) → proxy (forwards via `Connection::GlobalError` + `g_GlobalErrorMsg[12]`) → client. Test asserts both the error code (12) and the human-readable message text from the proxy's table comes through — proves the proxy-table truncation fix landed correctly and would regress loudly if anyone trimmed `g_GlobalErrorMsg[]` back to 12 entries. It also acts as a server-integrity tripwire: if the status-check weakens, the test fails with a CLAUDE.md citation in the failure message.
      Subsystem unblocked: cli-integration now has both the happy-path and the explicit-rejection round-trip locked in. Adding the next UDP-plane scenario (CreateCharacter / DeleteCharacter / SendAvatarLogin) becomes incremental, not architectural.

- [!] `/who.cgi` HTML output — there is nothing meaningful to port here. Win32 reference (`login-server/Net7SSL/SSL_Connection.cpp:256-260`) is a `//TODO` empty branch that leaves `response = nullptr` and falls through. Server-side `m_PlayerMgr.WhoHtml(response_length)` (`server/src/SSL_Connection.cpp:314`) is **referenced but never defined** — there is no `WhoHtml` symbol anywhere in `server/src/`. The Win32 build links because that branch sits inside `#ifdef WIN32` walls that are never compiled (or it's a runtime fault — neither has been verified). Either way, there's no upstream HTML generator to mirror.
      Status: blocked — moved from `[ ]` to `[!]`. The Linux behavior (`MakeNotFound` 404 in `LinuxAuth.cpp:524-526`) is actually *more honest* than the Win32 fall-through, which silently writes nothing and lets the connection die. Building a real implementation belongs in a future phase that also implements `PlayerManager::WhoHtml` from scratch on both platforms — out of scope for "match Phase K parity."
      Touches if revisited: `login-server/Net7SSL/LinuxAuth.cpp` (HTML response writer), `server/src/PlayerManager.cpp` (new WhoHtml method that lists active players), and an IPC channel (shared file / AF_UNIX socket / DB query) since net7ssl can't reach server PlayerManager directly today.

- [x] Wave 7 (2026-05-25): SectorManager constructor uninit `m_JobListCount` segfault — hidden by docker `restart: unless-stopped`.
      Status: done — `server/src/SectorManager.cpp` constructor now initializes `m_JobListCount = 0`. Prior bug: header declares `long m_JobListCount;` with no in-class initializer (SectorManager.h:236); constructor set m_JobTerminalLevel / m_JobListID but not this one. `SetupSectorServer` (SectorManager.cpp:170) only sets it for stations with sector_id>9999 AND JTLevel>0, leaving every non-station sector (and JTLevel==0 stations) iterating garbage in `RefreshJobs` (SectorManager.cpp:1444 — `for (i = 0; i < m_JobListCount; i++) { JobNode *jn = m_JobList[i]; ... }`). RunSectorEventThread calls RefreshJobs first thing (line 787), so every sector pthread crashed on the spot. Crash was invisible because docker-compose `restart: unless-stopped` re-launched the process within ~1s and the container's RestartCount climbed silently while logs looked normal. Diagnosed via dmesg `net7[...]: segfault at 0 ip ... error 4` (user-mode read of non-present page) → coredumpctl stack → addr2line.
      Touches: `server/src/SectorManager.cpp` (+3 lines for m_JobListCount=0 with comment; BeginSectorThread tidied — added pthread_create error log, added bound-port success log, dropped two dead comment lines).
      Notes: After fix, container stays up (RestartCount=0). All 174 sectors log `BeginSectorThread sector_id=N bound UDP port P`. `/proc/net/udp` inside the container shows 197 UDP listeners (174 sector ports + master 3808 + global 3810 + MVASauth 3806 + ephemeral senders). Unblocks Task #69 (sector LOGIN 0x0002 → START round-trip) — was the prerequisite the proxy MVAS plane wiring needed.

- [x] Wave 7 (cont.) — sector LOGIN 0x0002 → START round-trip wiring (2026-05-25, commits d8bfa44 + 7e15637 + 82d955f + ea6d695 + 3ecc04a).
      Status: done — `SectorLoginTests.FullSectorLogin_ReceivesStart` passes in 7s; full 39-test integration suite passes in 2m33s. Approach diverged from the original "third UDPClient on MVAS plane" sketch: instead of standing up a third connect()'d socket, the **global plane was switched to unconnected** (sendto + recvfrom + IP whitelist), because the server-side reality is that **post-login in-game UDP comes from MVASauth (server:3806) to the proxy's global-plane source port** — not a separate destination. The server captures `player->m_Player_Port` from the AVATARLOGIN packet's source port (server/src/UDP_Global.cpp:227) and from then on all `player->m_UDPConnection` writes go to (proxy_ip, that_port) via MVASauth's socket. A connected SOCK_DGRAM on the global plane filtered by single peer port (3810) and silently dropped MVASauth's 3806 frames. Unconnected mode keeps the default-peer for outgoing sendto() (still routes to 3810 for global-control opcodes 0x2003/0x2005/0x200C/0x200E) while accepting recv() from any source port + dropping non-server source IPs.
      Three classes of bug fixed: (a) **proxy SendPacketSequence calling convention** — Linux UDPClient was passing `msg` (payload-only) where Win32 passes the whole UDP packet; SendClientPacketSequence re-casts buffer to EnbUdpHeader and skipped 12 payload bytes, producing the "bad opcode 0x0000 len 0x0" symptom on every 0x2016 frame. Fix: pass `(char *) m_RecvBuffer` for both 0x2016 and 0x201A. (b) **proxy global plane unconnected** — `proxy/Net7.cpp` constructs `udp_to_global` with `unconnected=true`; `proxy/UDPClient.{h,cpp}` ctor takes optional `unconnected` flag stored in `m_Unconnected`; `proxy/UDPClient_linux.cpp` OpenFixedPort gates connect() on `!m_Unconnected`, UDP_RecvFromServer branches on m_Unconnected to recvfrom + IP whitelist, UDP_Send port==0 branches on m_Unconnected to sendto m_SockAddr. `proxy/ClientToServer_linux_stubs.cpp` activates the global plane on master-handoff confirm and flips LoginComplete on START_ACK so its SendPacketSequence early-return doesn't gate in-game frames. `proxy/UDPProxyToClient_linux.cpp` HandleStageConfirm forwards exactly 4 bytes (int32_t stage_wire) instead of 8 (sizeof(long)). (c) **sizeof(long) wire-size sweep across server** — `server/src/PlayerConnection.cpp` SendOpcode + SendLoginStageConfirm + HandleLoginAckReturn + SendStart + admin Terminate + SendTalkTreeAction + LOOT_HULK_PERMISSION all use int32_t; `server/src/PlayerClass.cpp` SendShipDataToSelf + SendShipData self-branch use int32_t game_id_wire; `server/src/PlayerManager.cpp` force-terminate uses int32_t player_id, and GetPlayer mask widened from `& 0x00FFFFFF` (silently capping at 24 bits) to `~(PLAYER_TAG | MANU_TAG)`; `server/src/UDP_Master.cpp` ProcessHandoff passes int32_t game_id to AddData. `server/src/SectorData.h` IS_PLAYER macro: dropped the bogus `< 0xFFFFF` upper bound that was rejecting every char-based GameID (CharID = account_id*5+slot+1 routinely exceeds 1M; e.g. account 9000001 slot 0 → CharID 45000006). The PLAYER_TAG bit alone is the canonical check, matching the macro's name.
      Bonus fixes that landed alongside: **Phase N MySQL stored-proc inlining** — `accLogin`/`avaLogin`/`avaLogout`/`incWarn`/`logoutOnShutdown` were silent no-ops since libpqxx can't load MySQL DELIMITER blocks; bodies inlined in SaveManager/AccountManager/ServerManager per `db/postgres/seed.sql` source; schema qualifier `net7_user.` stripped from all UPDATEs. **`fflush(stdout)`** after every LogMessage/LogMySQLMsg so `docker compose logs server` sees output in real time (was block-buffered without a TTY → debugging took ~10x longer than necessary). **TlsLoginTests.ValidAccount_ReturnsValidTicket** flaky-assertion fixed (asserted `Length >= 20` against `LinuxAuth.cpp` ticket format `%s-%d` which is 12-21 chars for cli_test01; replaced with `StartsWith(username + "-")` + digit suffix check; comment claimed "40-character hex ticket" the code never emitted).
      Touches: `proxy/Net7.cpp`, `proxy/UDPClient.{h,cpp}`, `proxy/UDPClient_linux.cpp`, `proxy/UDPProxyToClient_linux.cpp`, `proxy/ClientToServer_linux_stubs.cpp`, `server/src/PlayerConnection.cpp`, `server/src/PlayerClass.cpp`, `server/src/PlayerManager.cpp`, `server/src/UDP_Master.cpp`, `server/src/SectorData.h`, `server/src/SaveManager.cpp`, `server/src/AccountManager.cpp`, `server/src/SectorContentSQL.cpp`, `server/src/ServerManager.cpp`, `tests/integration/CliClient.IntegrationTests/ServerFixture.cs` (WaitForServerSectorAsync polls docker logs for `BeginSectorThread sector_id=10151` marker), `tests/integration/CliClient.IntegrationTests/Opcodes/SectorLoginTests.cs` (new, ~444 LOC), `tests/integration/CliClient.IntegrationTests/Handshake/TlsLoginTests.cs` (ticket assertion fix), `tools/cli-client/src/CliClient.Core/Opcodes/OpcodeId.cs` (+0x0005/+0x2020/+0x2021).
      Notes: end-to-end happy path is now green. Next-Phase-K work is broader opcode coverage (sector enumerate / inventory / movement / chat at sector scope), not handshake plumbing. See decisions log entry 2026-05-25 "Phase K sector LOGIN round-trip GREEN".

- [x] Wave 8 (2026-05-25): first post-START sector opcode — 0x0033 CLIENT_CHAT Group/no-group round-trip.
      Status: done — `SectorChatTests.GroupChat_WhenUngrouped_ReceivesNotInGroupErrorString` passes in 7s; full 40-test integration suite passes in 3m6s. Exercises the entire server→client server-side opcode pipeline that every in-sector reply rides: `Player::HandleClientChat` (server/src/PlayerConnection.cpp:4544) → `Type==1` branch → `GroupID()==-1` check → `SendVaMessage("Error: You are not in a group!")` → `Player::SendMessageString` (PlayerConnection.cpp:10918) → `SendOpcode(0x001D)` → `m_UDPQueue` → `SendPacketCache` → 0x2016 PACKET_SEQUENCE on UDP → proxy `UDPClient::SendClientPacketSequence` (proxy/UDPProxyToClient_linux.cpp:531) → 0x001D MESSAGE_STRING on sector TCP 3500. The Group/no-group branch chosen because it is the simplest server-state-independent path that produces a deterministic single-frame reply (slash-commands require GM permissions, broadcast requires another receiver, target/local/guild need extra state; "not in a group" only depends on the default `GroupID()==-1`).
      Refactor that landed alongside: handshake plumbing extracted from `SectorLoginTests.cs` (was ~445 LOC) into reusable `SectorHandshake.cs` static helper (~330 LOC) — `EstablishAsync(server, ticket, account, slot, sectorId, firstName, shipName)` returns a `Session` (IAsyncDisposable with Global+Sector connections + GameId + StartId + Slot); also exposes `DeleteCreatedCharacterAsync` for cleanup. SectorLoginTests is now ~95 LOC orchestration over the helper. Every future post-START opcode test calls `SectorHandshake.EstablishAsync` instead of duplicating the auth → GlobalConnect → CreateCharacter → GlobalTicketRequest → MasterJoin → sector LOGIN → drain-to-START chain.
      Touches: `tests/integration/CliClient.IntegrationTests/Opcodes/SectorChatTests.cs` (new — uses Pool[4] cli_test05 to avoid colliding with SectorLoginTests' Pool[3] and CreateCharacterTests' Pool[2] on IsUsernameUnique), `tests/integration/CliClient.IntegrationTests/Opcodes/SectorHandshake.cs` (new — extracted handshake helper), `tests/integration/CliClient.IntegrationTests/Opcodes/SectorLoginTests.cs` (refactored to use SectorHandshake), `tools/cli-client/src/CliClient.Core/Opcodes/OpcodeId.cs` (+0x0006 StartAck, +0x001D MessageString), `tests/integration/CliClient.IntegrationTests/Coverage/TestedOpcodes.cs` (MinTestedCount 15→17; entries for 0x001D MESSAGE_STRING and 0x0033 CLIENT_CHAT).
      Notes: no server changes needed — the chat path was wire-correct end-to-end thanks to the Phase K Wave 7 SendOpcode int32_t header fix landing the right wire shape for every server→client opcode. Phase K coverage ratchet: 15/207 → 17/207 (8.2%). Next-Phase-K targets: 0x0044 REQUEST_TIME → 0x0034 CLIENT_SET_TIME (needs HandleRequestTime `*((long *) data)` → `int32_t` fix — same class as the Wave 7 sweep), and 0x0006 START_ACK → 0x0092 CAMERA_CONTROL (closes the login state machine — SetActive(true) gates Active() checks elsewhere).

- [x] Wave 9 (2026-05-25): second post-START sector opcode — 0x0044 REQUEST_TIME → 0x0034 CLIENT_SET_TIME round-trip, with the predicted `sizeof(long)` over-read fix to `Player::HandleRequestTime` **and** a latent `AuxBase::m_Max_Buffer` uninit bug that the test surfaced.
      Status: done — `SectorRequestTimeTests.RequestTime_RoundTripsClientSentTickAndReturnsServerTimes` passes in 7s; full 41-test integration suite green in 3m17s. Exercises the simplest possible in-sector request/reply pair (HandleRequestTime is unconditional — no player-state guards) and asserts that the server echoes the client's exact tick back as `ClientSent`, which is a positive correlation signal: a wire-size or byte-order bug at the server would put garbage in that slot. Client sentinel chosen as `(int)(DateTime.UtcNow.Ticks & 0x7FFFFFFF)` so a leaked-recv-buffer regression can't accidentally echo a matching value run-to-run.
      Server fix 1 — `server/src/PlayerConnection.cpp:1624` `*((long *) data)` → `*((int32_t *) data)` — same class of bug as the Wave 7 sweep. Win32 wire payload is 4 bytes (a single `long` = 4 on Win32), but the unmigrated Linux server read `*((long *) data)` which is 8 bytes — pulling 4B of payload plus 4B of whatever followed it in the recv buffer, then echoing that garbage back to the client as its own `ClientSent` tick. Test would have failed on the `Assert.Equal(clientTick, echoedClientSent)` line. The `ClientSetTime` struct itself (`common/include/net7/PacketStructures.h:563`) was already migrated to `int32_t` in Wave 7, so the wire size on the reply side (12B) was already correct — only the request-side read was wrong.
      Server fix 2 — `server/src/AuxClasses/AuxBase.cpp` `AuxBase::AuxBase()` now sets `m_Max_Buffer = (unsigned long)-1`. Test surfaced this: SectorRequestTime in isolation passed, but failed in the 3-test sector sequence (SectorLogin → SectorChat → SectorRequestTime) with OperationCanceledException — server log showed thousands of `Error: Bufferoverflow in Aux!` printf bursts and zero `Received RequestTime` entries. Root cause: only 4 of 57 `AuxBase` subclasses (`AuxHulkIndex=1000`, `AuxShipIndex=10000`, `AuxMobIndex=2000`, `AuxHarvestable=2000`) explicitly set `m_Max_Buffer`. The other 53 leave it uninitialised. On Win32 the heap garbage happened to be large enough that `AddData()`'s `sizeof(T)+index < m_Max_Buffer` guard passed through; on Linux the garbage is often small, so every `AddData()` call hits the printf branch. With 3+ players in the same sector, the per-player serialisation flood saturates stdout and stalls the sector tick — REQUEST_TIME never reaches `HandleRequestTime`. The fix initialises `m_Max_Buffer` to `ULONG_MAX` in the base constructor, restoring Win32 de-facto behaviour without weakening any actual bound (the load-bearing safety mechanism is caller-side buffer sizing, not this check; the four subclasses that *know* a tighter cap keep their explicit assignments and override the default).
      Touches: `server/src/PlayerConnection.cpp` (HandleRequestTime sizeof(long) fix), `server/src/AuxClasses/AuxBase.cpp` (m_Max_Buffer default), `tests/integration/CliClient.IntegrationTests/Opcodes/SectorRequestTimeTests.cs` (new — uses Pool[5] cli_test06 to avoid colliding with SectorLoginTests' Pool[3] and SectorChatTests' Pool[4]; 90s budget; 200-frame drain cap; xmldoc records all three failure classes including the AuxBase suite-state regression), `tests/integration/CliClient.IntegrationTests/TestAccounts.cs` (Pool gains cli_test06 entry), `tests/integration/CliClient.IntegrationTests/Fixtures/seed.sql` (cli_test06 INSERT row; comments updated 9000005→9000006 and "all five"→"all six"), `tools/cli-client/src/CliClient.Core/Opcodes/OpcodeId.cs` (+0x0034 ClientSetTime, +0x0044 RequestTime), `tests/integration/CliClient.IntegrationTests/Coverage/TestedOpcodes.cs` (MinTestedCount 17→19; entries for 0x0034 CLIENT_SET_TIME and 0x0044 REQUEST_TIME).
      Notes: Phase K coverage ratchet: 17/207 → 19/207 (9.2%). The HarnessSmokeTest `SeedSql_IsCopiedToOutput_AndMentionsEveryPooledAccount` iterates Pool dynamically so the new 6th account is picked up without test code changes. Per CLAUDE.md server-integrity rules: the `m_Max_Buffer` fix is preservation-aligned (restores 15+ years of upstream behaviour by initialising what was uninitialised — Win32 happened to have large garbage there) and does not loosen any real bound. Next-Phase-K targets: 0x0006 START_ACK closure (SetActive(true) gates Active() checks for movement/inventory), 0x0012 TURN and 0x0013 TILT (simplest movement opcodes — fan-out across sector players is the new path), 0x002C ACTION (combat trigger).

- [x] Wave 17 (2026-05-25): 0x0057 SKILL_UP survival round-trip — untrained skill for the avatar's profession trips the "already maxed" early return (Level=0 == MaxSkillLevel=0).
      Status: done — `SectorSkillUpTests.SkillUp_OnUntrainedSkill_DoesNotBreakConnection_RequestTimeStillRoundTrips` passes; full integration suite green from the running stack. Zero-server-change wave — Phase R already had SkillAction canonical as `{int32_t GameID; int SkillPoints; short SkillID;}` = 10B (common/include/net7/PacketStructures.h:987; `int` is 4B on both Win32 and Linux x86_64 so the struct width is invariant across platforms). SKILL_UP (0x0057) is not explicitly listed in proxy/ClientToServer_linux_stubs.cpp so it hits the `default:` arm at line 514 and falls through to bottom-of-switch `g_ServerMgr->m_UDPConnection->ForwardClientOpcode(...)`. Server-side `Player::HandleSkillAction` (server/src/PlayerSkills.cpp:97) has three early-return guards before any state mutation: (1) `SkillLevel == SkillMaxLevel` (already maxed); (2) insufficient skill points; (3) `SkillLevelRequirement > 0` (prereq skill not high enough). For a SkillID valid in the AuxSkills wrapper but untrained for the avatar's class, the first guard trips on `0 == 0` and the handler returns silently — no DB write, no AuxPlayer/AuxShip refresh, no reply. Pipe survival is the only assertable post-condition (matches Waves 10/11/13/14/15/16 pattern).
      SkillID choice — **and the trap we walked into**: initial draft sent SkillID=169 reasoning from `_Skills::Skill[170]` (server/src/AuxClasses/AuxSkills.h:23-26, the raw data array). **This crashed the sector thread** because the dispatcher path indexes through `m_PlayerIndex.RPGInfo.Skills.Skill[Action->SkillID]` where `RPGInfo.Skills` is `class AuxSkills` (AuxRPGInfo.h:134), whose `Skill` member is **`AuxSkill Skill[64]`** (AuxSkills.h:86) — the wrapper array is only 64 entries, not 170. `AuxSkills::Init` confirms it only loops `i<64` (AuxSkills.h:64-67). Any SkillID ≥ 64 dereferences past the wrapper array end into Player-object memory, reads a garbage `_Skill * Data` pointer, and `GetAvailability()` faults the sector thread (verified via `docker inspect enb-emulator-server-1 --format '{{.RestartCount}}'` = 2 with StartedAt jumping to the same second the bad SKILL_UP frame was sent). Corrected SkillID to **29 SKILL_JENQUAI_CULTURE** — per the seeded Postgres `skills` table, `warrior_max_level = -1` for that row, so the per-profession init loop in PlayerSaves::LoadPlayer (server/src/PlayerSaves.cpp:609-647) skips the entry (`Skills[29].ClassType[0].MaxLevel` stays ≤ 0) and `RPGInfo.Skills.Skill[29]` keeps the AuxSkill::Init defaults (Level=0, MaxSkillLevel=0, Availability[3]=0). First early-return fires, pipe survives.
      Regression coverage — if anyone reverts SkillAction's `int SkillPoints` to `long SkillPoints`, the struct grows from 10B to 14B on Linux x86_64: SkillID would read from byte 12 (instead of 8), past the end of the 10B wire payload, into undefined memory. A garbage SkillID < 64 would index a random AuxSkill wrapper slot — could trip into the mutation path on the wrong skill, corrupting player state. A garbage SkillID ≥ 64 would crash the sector thread on the OOB Data-pointer deref (the same crash that bit the initial test). Also catches: proxy default-case ForwardClientOpcode regression (drops the opcode); m_Mutex deadlock if a regression moves the lock above the early-return guards; dispatcher mis-route at server/src/PlayerConnection.cpp:499 (hand-maintained ~200-entry switch); AuxSkills::Init loop-bound shrinking below 30 (the Jenquai-Culture pick at 29 is inside the current `<64` bound, but a regression shrinking it to 32 would crash on Jenquai-Culture too).
      Server-side bounds gap left for future hardening — `PlayerSkills.cpp:101` has no `if (Action->SkillID < 0 || Action->SkillID >= 64) return;` guard before the OOB-prone indexing. The real Win32 client never sends SkillID ≥ 64 (UI only surfaces trainable skills, all in [0..57] per the SkillNames[64] table at PlayerSkills.cpp:37-95), so retail wire never tripped it — it's an existing latent gap, not a regression. Per CLAUDE.md "tightening the server toward greater fidelity is always welcome", a defensive bounds guard would be a legitimate hardening but is intentionally NOT bundled into this wave (scope = exercise the round-trip; the right fix is a `static constexpr int kMaxSkills = 64;` in AuxSkills.h used both for the array decl and the bounds check, and probably part of a broader sweep of similar dispatcher reads). Documented in plans/99-decisions-log.md 2026-05-25 for future contributors.
      Touches: `tests/integration/CliClient.IntegrationTests/Opcodes/SectorSkillUpTests.cs` (new — uses Pool[12] cli_test14 to avoid colliding with Pool[3..11]; 90s budget; 400-frame drain cap; xmldoc documents survival-probe rationale, the AuxSkills::Skill[64] vs _Skills::Skill[170] trap and prior bad reasoning, the SkillID=29 Jenquai-on-Terran-Warrior rationale via skills.warrior_max_level=-1, sizeof(long) SkillAction regression class, proxy default-case fragility, m_Mutex deadlock + dispatcher mis-route + AuxSkills::Init bound hazards, and the CLAUDE.md server-integrity note that the SKILL_UP click is a published retail action), `tests/integration/CliClient.IntegrationTests/TestAccounts.cs` (Pool gains cli_test14 entry — 9_000_014), `tests/integration/CliClient.IntegrationTests/Fixtures/seed.sql` (cli_test14 INSERT row, status=100), `tools/cli-client/src/CliClient.Core/Opcodes/OpcodeId.cs` (+0x0057 SkillUp in sorted position between 0x004E StarbaseRequest and 0x006D GlobalConnect), `tests/integration/CliClient.IntegrationTests/Coverage/TestedOpcodes.cs` (MinTestedCount 26→27; sorted-position entry for 0x0057 SKILL_UP between 0x004E STARBASE_REQUEST and 0x006D GLOBAL_CONNECT).
      Notes: Phase K coverage ratchet 26/207 → 27/207 (13.0%). Per CLAUDE.md server-integrity rules: zero permissiveness added; the SKILL_UP click on an untrained skill is exactly what the retail Win32 client emits and the retail server's no-op silent-return is faithfully preserved. Next-Phase-K targets: 0x005A VERB_REQUEST (NPC talk-tree exercise — could pair with action=4 STARBASE_REQUEST once we have a seed NPC); 0x0011 NAV (in-flight nav target selection — natural follow-on to MOVE); 0x002F BUY_ITEM (vendor purchase — pure server state mutator on inventory).

- [x] Wave 18 (2026-05-25): 0x005A VERB_REQUEST survival round-trip — SubjectID-mismatch branch trips the silent-no-op early return in `Player::HandleVerbRequest`.
      Status: done — `SectorVerbRequestTests.VerbRequest_OnNonMatchingSubject_DoesNotBreakConnection_RequestTimeStillRoundTrips` passes in isolation (1/1, 7s) AND when the full integration suite is run against a freshly-recycled stack (49/49, 1m32s). Zero-server-change wave — Phase R already had VerbRequest canonical as `{int32_t SubjectID; int32_t ObjectID; int32_t Action;}` = 12B (common/include/net7/PacketStructures.h:570). VERB_REQUEST is not explicitly listed in proxy/ClientToServer_linux_stubs.cpp so it hits the `default:` arm at line 514 and falls through to bottom-of-switch `ForwardClientOpcode(...)`. Server-side `Player::HandleVerbRequest` (server/src/PlayerConnection.cpp:3547) is short — reads SubjectID/ObjectID via ntohl(), checks `subject_id == GameID() && pkt->Action == 1`, and on mismatch returns silently. We send SubjectID=0; cli_test15's GameID (assigned by SaveManager as `account_id*5 + slot + 1`) is non-zero, so the equality test fails and the handler exits without entering UpdateVerbs. Pipe survival is the only assertable post-condition (matches Waves 10/11/13/14/15/16/17 pattern).
      Why a survival probe — even on a SubjectID match, `UpdateVerbs(true)` (server/src/PlayerClass.cpp:3500) early-returns when `GetObjectFromID(GetTargetGameID())` returns null (no targeted object), which is exactly the post-handshake state for a freshly-created character. No reply on either branch. Per CLAUDE.md server-integrity rules we don't fabricate one.
      Full-suite re-run gotcha — running SectorVerbRequestTests in isolation first, *then* re-running the full suite (without recycling the docker stack) produced `Failed: 1, Passed: 48` with `G_ERROR_ACCOUNT_IN_USE (code=13)` from cli_test15 at SectorHandshake.cs:89 — the prior isolated invocation left cli_test15 marked active on the global server. Fix: `docker compose down -v && docker compose up -d` between runs cleans the global session state. The full suite green from a fresh stack is the authoritative verification. (Future hardening: the test's `finally` block could explicitly log the account off before deleting the character, instead of relying on session expiry. Out of scope for this wave.)
      Regression coverage — if anyone reverts VerbRequest's three int32_t fields to long, the struct grows from 12B to 16B/20B/24B on Linux x86_64: Action would read from byte 16 (instead of 8), past the end of the 12B wire payload into undefined memory, mis-comparing against the `Action == 1` guard at minimum. SubjectID=0 is a fixed point under byte-swap so ntohl regressions on SubjectID itself wouldn't surface here — but a regression that dropped ntohl on a non-zero SubjectID would suddenly hit (or miss) the GameID-equality branch on the wrong endianness. Also catches: proxy default-case ForwardClientOpcode regression (drops the opcode); dispatcher mis-route at server/src/PlayerConnection.cpp:507 (hand-maintained ~200-entry switch); null-deref regression in the equality short-circuit (current code reads both subject_id and object_id from the wire before the equality check).
      Touches: `tests/integration/CliClient.IntegrationTests/Opcodes/SectorVerbRequestTests.cs` (new — uses Pool[13] cli_test15 to avoid colliding with Pool[3..12]; 90s budget; 400-frame drain cap; xmldoc documents survival-probe rationale, why SubjectID=0 trips the silent-no-op branch, sizeof(long) VerbRequest regression class, proxy default-case fragility, ntohl byte-order regression class, dispatcher mis-route hazard, and the CLAUDE.md server-integrity note that VerbRequest with mismatched SubjectID is exactly what the retail Win32 client would emit if its UI dispatched a verb-refresh outside a targeted context), `tests/integration/CliClient.IntegrationTests/TestAccounts.cs` (Pool gains cli_test15 entry — 9_000_015), `tests/integration/CliClient.IntegrationTests/Fixtures/seed.sql` (cli_test15 INSERT row, status=100), `tools/cli-client/src/CliClient.Core/Opcodes/OpcodeId.cs` (+0x005A VerbRequest in sorted position between 0x0057 SkillUp and 0x006D GlobalConnect), `tests/integration/CliClient.IntegrationTests/Coverage/TestedOpcodes.cs` (MinTestedCount 27→28; sorted-position entry for 0x005A VERB_REQUEST between 0x0057 SKILL_UP and 0x006D GLOBAL_CONNECT).
      Notes: Phase K coverage ratchet 27/207 → 28/207 (13.5%). Per CLAUDE.md server-integrity rules: zero permissiveness added; the VerbRequest wire shape is byte-identical to what the retail Win32 client emits on a right-click verb-refresh; the silent-no-op-on-mismatch branch is faithful retail behaviour. Next-Phase-K targets: 0x0011 NAV (in-flight nav target selection — natural follow-on to MOVE); 0x002F BUY_ITEM (vendor purchase — pure server state mutator on inventory); 0x0017 STOP_MOVE or similar movement-state-flip opcodes (Wave 14 MOVE engine-on already established the path).

- [x] Wave 19 (2026-05-25): 0x0017 REQUEST_TARGET direct-reply round-trip — null-target request reaches `Player::HandleRequestTarget` which unconditionally emits 0x0019 SET_TARGET with the hard-coded `-1` sentinel TargetID. **First direct-reply correlation in Phase K post-Wave-9** (Waves 10/11/13/14/15/16/17/18 were all survival probes).
      Status: done — `SectorRequestTargetTests.RequestTarget_OnNullTarget_ReceivesSetTargetWithSentinelTargetIdMinusOne` passes in isolation (1/1, 7s) AND when the full integration suite is run against a freshly-recycled stack (50/50, 2m25s). Zero-server-change wave — Phase R already had RequestTarget canonical as `{int32_t GameID; int32_t TargetID;}` = 8B (common/include/net7/PacketStructures.h:528) and SetTarget canonical with the same shape (PacketStructures.h:540). REQUEST_TARGET is not explicitly listed in proxy/ClientToServer_linux_stubs.cpp so it hits the `default:` arm at line 514 and falls through to bottom-of-switch `ForwardClientOpcode(...)`. Server-side `Player::HandleRequestTarget` (server/src/PlayerConnection.cpp:3390) **unconditionally** calls `SendSetTarget(request->TargetID, -1)` regardless of whether the requested target exists — `GetObjectFromID(0)` returns null for both newtarget and oldtarget, `m_ProspectWindow` is false on a fresh starbase character so the OpenInterface branch is skipped, SendSetTarget fires, then `BlankVerbs()` (PlayerClass.cpp:3467 = safe memset) runs and the threat-rank logic at the bottom is gated on `newtarget != null` so it's skipped. This is the **first post-Wave-9 direct-reply opcode** — Waves 10/11/13/14/15/16/17/18 were all survival probes because their handlers either no-op'd, fanned out only to OTHER players, or guarded on player state we couldn't satisfy.
      Why a direct-reply assertion beats a survival probe — survival probes ("pipe still alive") prove the handler didn't crash but say nothing about correctness. Direct-reply with a known sentinel (`-1`) **plus** a known mirror (`GameID == request->TargetID == 0`) **plus** a known payload size (8B) gives three positive-correlation signals in one test. A regression that swapped the SetTarget GameID/TargetID field order would silently break (positive: replyTargetID=-1 would appear in the GameID slot). A regression that widened either field to long would balloon the reply to 12B or 16B (positive: payload length assertion fails immediately). A regression in the server→client UDP fan-out path that affected only this opcode on a sub-branch of `SendClientPacketSequence` framing logic (proxy/UDPProxyToClient_linux.cpp:531) would surface as a timeout here even though Wave 8's MESSAGE_STRING still passed.
      Full-suite re-run gotcha recurrence — same ACCOUNT_IN_USE-on-re-run pattern as Wave 18: running SectorRequestTargetTests in isolation, then re-running the full suite without recycling the docker stack produced `G_ERROR_ACCOUNT_IN_USE (code=13)` for cli_test16. Fix: `docker compose down -v && docker compose up -d` between runs. This is now the **second wave** to hit this pattern, suggesting a wave-N+1 hardening opportunity: the test's `finally` block could explicitly send `0x0003 LOGOFF` (or whatever the global plane logoff sequence is) before deleting the character, instead of relying on session expiry. Filed as future hardening; out of scope for Wave 19.
      Regression coverage — if anyone reverts RequestTarget's `int32_t GameID/TargetID` to long, the struct grows from 8B to 12B/16B on Linux x86_64: TargetID would read from byte 8 (past the end of the 8B wire payload) into undefined memory, then **walk the threat-rank path on a stale newtarget pointer** which could crash. If SetTarget reply is widened to long, the assertion that payload length is exactly 8 catches it immediately. If the SetTarget struct fields are reordered (GameID/TargetID swap), our assert `replyTargetID == -1` fails (the literal -1 would appear in the GameID slot instead). Also catches: proxy default-case ForwardClientOpcode regression (drops the opcode → timeout); server→client UDP fan-out regression in SendClientPacketSequence framing on a sub-branch unique to this opcode (timeout); dispatcher mis-route at server/src/PlayerConnection.cpp:443 (hand-maintained ~200-entry switch); BlankVerbs/SetTargetGameID introducing a crash hazard.
      Touches: `tests/integration/CliClient.IntegrationTests/Opcodes/SectorRequestTargetTests.cs` (new — uses Pool[14] cli_test16 to avoid colliding with Pool[3..13]; 90s budget; 400-frame drain cap; xmldoc documents direct-reply rationale, full HandleRequestTarget code path walk-through, GameID/TargetID mirror semantics, 6 regression classes with primary-source citations, and the CLAUDE.md server-integrity note that TargetID=0 is exactly what the retail Win32 client emits when the user clicks empty space to clear the active target, and the unconditional SetTarget(-1) reply is the faithful retail behaviour — the -1 sentinel means "no rank/threat info yet"), `tests/integration/CliClient.IntegrationTests/TestAccounts.cs` (Pool gains cli_test16 entry — 9_000_016), `tests/integration/CliClient.IntegrationTests/Fixtures/seed.sql` (cli_test16 INSERT row, status=100), `tools/cli-client/src/CliClient.Core/Opcodes/OpcodeId.cs` (+0x0017 RequestTarget and +0x0019 SetTarget in sorted position between 0x0014 Move and 0x001D MessageString), `tests/integration/CliClient.IntegrationTests/Coverage/TestedOpcodes.cs` (MinTestedCount 28→30; sorted-position entries for 0x0017 REQUEST_TARGET and 0x0019 SET_TARGET between 0x0014 MOVE and 0x001D MESSAGE_STRING).
      Notes: Phase K coverage ratchet 28/207 → 30/207 (14.5%) — first **+2** ratchet in Phase K (both the request and the reply get coverage entries). Per CLAUDE.md server-integrity rules: zero permissiveness added; the REQUEST_TARGET-with-TargetID=0 wire shape is byte-identical to what the retail Win32 client emits when the user clicks empty space; the unconditional SetTarget reply with TargetID=-1 is the faithful retail behaviour. Next-Phase-K targets: 0x0018 REQUEST_TARGETS_TARGET (natural follow-on — the -1 sentinel in our SetTarget reply tells the client to re-request via this opcode, so a 0x0017→0x0018 chain test would exercise both halves of the target-info handshake); 0x002D ACTION2 (simpler-payload sibling of Wave 13's ACTION); 0x0027 INVENTORY_MOVE (first inventory-state-mutator wave); 0x0034 inverse sweep for further opcodes that have no direct reply.

- [x] Wave 20 (2026-05-25): 0x0018 REQUEST_TARGETS_TARGET direct-reply round-trip — unknown-player request reaches `Player::HandleRequestTargetsTarget`'s `else`-branch (PlayerManager::GetPlayer returns null on the 0x12345678 sentinel) and emits the literal `SendSetTarget(0, -1)`. **Stronger assertion than Wave 19** — Wave 19's REQUEST_TARGET reply mirrored `request->TargetID` into the reply's GameID slot, so a 0x0017-vs-0x0018 dispatcher mis-route could not be distinguished. Wave 20 sends a non-zero recognisable TargetID and asserts the reply's GameID slot is **literal 0** (not 0x12345678) — a mis-route to HandleRequestTarget would echo back 0x12345678 and fail the assertion.
      Status: done — `SectorRequestTargetsTargetTests.RequestTargetsTarget_OnUnknownPlayer_ReceivesSetTargetWithLiteralZeroGameId` passes in isolation (1/1, 7s) AND when the full integration suite is run against a freshly-recycled stack (51/51, 1m47s). Zero-server-change wave — Phase R already had RequestTarget canonical as `{int32_t GameID; int32_t TargetID;}` = 8B (common/include/net7/PacketStructures.h:528) and SetTarget reply canonical with the same shape (PacketStructures.h:540). REQUEST_TARGETS_TARGET is not explicitly listed in proxy/ClientToServer_linux_stubs.cpp so it hits the `default:` arm at line 514 and falls through to the bottom-of-switch `ForwardClientOpcode(...)`. Server-side `Player::HandleRequestTargetsTarget` (server/src/PlayerConnection.cpp:3494) calls `PlayerManager::GetPlayer(request->TargetID)` — the hash-map keyed on CharacterID (server/src/PlayerManager.cpp:209-219) returns null for 0x12345678 because no seeded player has that ID (SaveManager assigns `account_id*5+slot+1`, so for cli_test17's 9_000_017 / slot 0 the CharacterID is 45_000_086 ≪ 305_419_896). The else-branch then unconditionally fires `SendSetTarget(0, -1)` → `ShipIndex()->SetTargetGameID(-1)` → `SendAuxShip()` (no-op when `HasDiff()` is false, which is the case for a freshly-handed-off character).
      Why a stronger assertion than Wave 19 — Wave 19's reply was `SendSetTarget(request->TargetID, -1)` with `request->TargetID=0`, so reply GameID=0. A 0x0017→0x0018 dispatcher mis-route would emit `SendSetTarget(0, -1)` from the same `else`-branch (still GameID=0). Wave 19 therefore could not catch a mis-route in either direction. Wave 20 sends TargetID=0x12345678 → in the correct 0x0018 path the else-branch produces literal `SendSetTarget(0, -1)` (reply GameID=0); in a 0x0018→0x0017 mis-route the handler would produce `SendSetTarget(0x12345678, -1)` (reply GameID=0x12345678). The assertion `replyGameID == 0` is the differentiator. This is the **first cross-handler dispatcher-misroute differentiator** in Phase K.
      Regression coverage — (a) PacketStructures.h RequestTarget long-revert (shared struct for both 0x0017 and 0x0018, so 8B canonical revert would surface on either wave); (b) PacketStructures.h SetTarget reply long-revert (payload-length-8 assert); (c) 0x0018↔0x0017 dispatcher mis-route at server/src/PlayerConnection.cpp:447 (the differentiator the Wave-19 test could not catch); (d) PlayerManager::GetPlayer hash-map regression — if a refactor accidentally returns a stale pointer instead of null on unknown IDs the if-branch fires, overwrites data[4..7] with the stale player's targeted GameID, then chains into HandleRequestTarget with a now-non-zero TargetID → reply GameID becomes the stale target's ID and the assertion fails; (e) proxy default-case ForwardClientOpcode dropping the opcode (0x0018 is not explicitly listed in proxy/ClientToServer_linux_stubs.cpp); (f) SendAuxShip HasDiff() default-flip — would produce an extra unexpected 0x001B AUX_DATA in the drain loop (diagnostic, not assertion); (g) ShipIndex()->SetTargetGameID(-1) null-deref hazard.
      Touches: `tests/integration/CliClient.IntegrationTests/Opcodes/SectorRequestTargetsTargetTests.cs` (new — uses Pool[15] cli_test17 to avoid colliding with Pool[3..14]; 90s budget; 400-frame drain cap; xmldoc documents the "why 0x0018 follows 0x0017" handshake rationale, full handler else-branch walk-through, the GameID=0 vs GameID=request->TargetID differentiator vs Wave 19, 7 regression classes with primary-source citations, and the CLAUDE.md server-integrity note that the literal `SendSetTarget(0, -1)` on unknown-player lookup is faithful retail behaviour), `tests/integration/CliClient.IntegrationTests/TestAccounts.cs` (Pool gains cli_test17 entry — 9_000_017), `tests/integration/CliClient.IntegrationTests/Fixtures/seed.sql` (cli_test17 INSERT row, status=100), `tools/cli-client/src/CliClient.Core/Opcodes/OpcodeId.cs` (+0x0018 RequestTargetsTarget in sorted position between 0x0017 RequestTarget and 0x0019 SetTarget), `tests/integration/CliClient.IntegrationTests/Coverage/TestedOpcodes.cs` (MinTestedCount 30→31; sorted-position entry for 0x0018 REQUEST_TARGETS_TARGET between 0x0017 REQUEST_TARGET and 0x0019 SET_TARGET; also updated the 0x0019 SET_TARGET citation to credit both Wave 19 and Wave 20).
      Notes: Phase K coverage ratchet 30/207 → 31/207 (15.0%). Per CLAUDE.md server-integrity rules: zero permissiveness added; the REQUEST_TARGETS_TARGET wire shape and the literal `SendSetTarget(0, -1)` reply on unknown-player lookup are exactly retail behaviour (the same handler is invoked by the real Win32 client after every SET_TARGET reply with TargetID=-1, and the else-branch fires whenever the targeted player has logged off or doesn't exist on the server). Full-suite re-run gotcha recurrence — same ACCOUNT_IN_USE-on-re-run pattern as Waves 18 and 19; needed `docker compose down -v` between the isolated test run and the full-suite run. This is now the **third wave** to hit this pattern — the wave-N+1 hardening (explicit logoff in `finally`) becomes increasingly attractive but stays out of scope here. Next-Phase-K targets: 0x002D ACTION2 (simpler-payload sibling of Wave 13's ACTION); 0x0027 INVENTORY_MOVE (first inventory-state-mutator wave — needs seeded inventory rows in seed.sql); 0x0029 ITEM_STATE (item-state-flip cousin of INVENTORY_MOVE); 0x002E OPTION (client-side option toggle — likely a pure-no-op survival probe candidate); 0x001A DEBUG (debug-mode opcode — guarded on admin status, may be a fast survival probe).

- [x] Wave 16 (2026-05-25): 0x004E STARBASE_REQUEST survival round-trip — Job-Terminal-open sub-action (the leanest read-only branch in the umbrella starbase-interaction handler).
      Status: done — `SectorStarbaseRequestTests.JobTerminal_DoesNotBreakConnection_RequestTimeStillRoundTrips` passes; full integration suite green (47/47). Zero-server-change wave — Phase R already had StarbaseRequest canonical as `{int32_t PlayerID; int32_t StarbaseID; char Action;}` = 9B (common/include/net7/PacketStructures.h:812). STARBASE_REQUEST is not explicitly listed in proxy/ClientToServer_linux_stubs.cpp (greps clean), so the opcode hits the proxy's `default:` arm at line 514 and falls through to bottom-of-switch `g_ServerMgr->m_UDPConnection->ForwardClientOpcode(...)`. Server-side `Player::HandleStarbaseRequest` (server/src/PlayerConnection.cpp:9842) dispatches on Action through 8 sub-cases — Action=6 (Job Terminal) is the leanest read-only branch, calling `SectorManager::GetJobList(m_ScratchBuffer)` and conditionally emitting 0x0093 JOB_LIST when jobs exist. Sector 10151's seed has no job rows so the handler returns silently — no direct reply, pipe survival is the assertable post-condition (matches Waves 10/11/13/14/15 pattern).
      Why Action=6 over Action=1/4/7/8/9/10/11 — Action=1 (Exit Station) calls LaunchIntoSpace which mutates m_Gating and drives ship physics state, risky on a fresh starbase character; Action=4 (Talk-to-NPC) takes m_StarbaseTargetID = pkt->StarbaseID then runs CheckMissions + CheckForNewMissions DB queries before reaching the NPC=NULL fall-through that sends ENB_OPCODE_0054_TALK_TREE — heavy and not guaranteed deterministic; Action=7-11 all need authoritative server state (registered job ids, customisation contexts) that a freshly-handed-off character doesn't have. Action=6's only mutation is `m_TradeWindow = false` — pure state flag flip with no follow-on effects.
      Regression coverage — if anyone reverts PacketStructures.h StarbaseRequest from int32_t to long, the struct grows from 9B to 17B on Linux x86_64; Action would read from byte offset 16 (instead of 8), past the end of the 9B wire payload into undefined memory. A garbage Action read of 1 would invoke LaunchIntoSpace on a docked player; Action=10/11 with garbage StarbaseID would trip RecustomizeAvatarStart/RecustomizeShipStart payload constructors against uninitialised m_Database.ship_data. Also catches: proxy default-case ForwardClientOpcode regression (drops the opcode); SectorManager::GetJobList null-deref or buffer overrun on m_ScratchBuffer; dispatcher mis-route to a different handler.
      Touches: `tests/integration/CliClient.IntegrationTests/Opcodes/SectorStarbaseRequestTests.cs` (new — uses Pool[11] cli_test13 to avoid colliding with Pool[3..10]; 90s budget; 400-frame drain cap; xmldoc documents Action=6 rationale, sizeof(long) StarbaseRequest regression class, proxy default-case fragility, GetJobList null-deref hazard, and the CLAUDE.md server-integrity note that the Job-Terminal click is a published retail action), `tests/integration/CliClient.IntegrationTests/TestAccounts.cs` (Pool gains cli_test13 entry — 9_000_013), `tests/integration/CliClient.IntegrationTests/Fixtures/seed.sql` (cli_test13 INSERT row, status=100), `tools/cli-client/src/CliClient.Core/Opcodes/OpcodeId.cs` (+0x004E StarbaseRequest in sorted position between 0x0044 RequestTime and 0x006D GlobalConnect), `tests/integration/CliClient.IntegrationTests/Coverage/TestedOpcodes.cs` (MinTestedCount 25→26; sorted-position entry for 0x004E STARBASE_REQUEST after 0x0044 REQUEST_TIME).
      Notes: Phase K coverage ratchet 25/207 → 26/207 (12.6%). Per CLAUDE.md server-integrity rules: zero permissiveness added; Action=6 is exactly what the retail Win32 client emits when the user clicks the Job Terminal NPC inside a starbase. Next-Phase-K targets: 0x0057 SKILL_UP (skill progression on Player::m_Skills); 0x005A VERB_REQUEST (NPC talk-tree exercise — could pair with action=4 STARBASE_REQUEST once we have a seed NPC); 0x0011 NAV (in-flight nav target selection — natural follow-on to MOVE).

- [x] Wave 15 (2026-05-25): 0x009F STARBASE_ROOM_CHANGE survival round-trip — pivoted from the originally-planned 0x009B WARP target after `PrepareForWarp` analysis revealed deep `m_Equip[1/2]` reactor/engine reads + nav-distance lookups that would surface real bugs but also risk crashing the server on a freshly-created starbase character. STARBASE_ROOM_CHANGE is the natural in-station parallel to Wave 14's MOVE — Luna Station (10151) is a starbase per `StaticData.h:63-74` and walking between rooms is the most-common in-station wire frame.
      Status: done — `SectorStarbaseRoomChangeTests.RoomChange_DoesNotBreakConnection_RequestTimeStillRoundTrips` passes; full integration suite green from a fresh `docker compose down -v` rebuild. Zero server-side changes — Phase R already had StarbaseRoomChange canonical as 3× int32_t = 12B (common/include/net7/PacketStructures.h:805), Wave 12's clean-wire substrate made the path correct, and the proxy already forwards via HandleStarbaseRoomChange_Linux at proxy/ClientToServer_linux_stubs.cpp:491-496 (whose body is empty matching Win32's commented-out no-op, then falls through to bottom-of-switch ForwardClientOpcode). Server-side Player::HandleStarbaseRoomChange (server/src/PlayerClass.cpp:631) reads the 12B struct, mutates m_Room/m_Oldroom under m_Mutex, then iterates the sector player list and sends 0x00A0 STARBASE_ROOM_CHANGE to every OTHER player — single-player integration test has no other observers so the fan-out loop is empty and no direct reply comes back. Pipe survival is the only assertable post-condition (matches Waves 10/11/13/14 pattern).
      Touches: `tests/integration/CliClient.IntegrationTests/Opcodes/SectorStarbaseRoomChangeTests.cs` (new — uses Pool[10] cli_test12 to avoid colliding with Pool[3..9]; 90s budget; 400-frame drain cap; xmldoc documents survival-probe rationale, sizeof(long) StarbaseRoomChange regression class, fall-through-to-forward fragility, m_Mutex deadlock + GetSectorPlayerList iteration hazards, and the CLAUDE.md server-integrity note that OldRoom=0/NewRoom=1 matches retail "first room transition after entry"), `tests/integration/CliClient.IntegrationTests/TestAccounts.cs` (Pool gains cli_test12 entry — 9_000_012), `tests/integration/CliClient.IntegrationTests/Fixtures/seed.sql` (cli_test12 INSERT row, status=100), `tools/cli-client/src/CliClient.Core/Opcodes/OpcodeId.cs` (+0x009F StarbaseRoomChange), `tests/integration/CliClient.IntegrationTests/Coverage/TestedOpcodes.cs` (MinTestedCount 24→25; sorted-position entry for 0x009F STARBASE_ROOM_CHANGE between 0x0075 GLOBAL_ERROR and 0x2020 LOGIN_STAGE_S_C).
      Notes: Phase K coverage ratchet 24/207 → 25/207 (12.1%). Per CLAUDE.md server-integrity rules: zero permissiveness added; OldRoom=0/NewRoom=1 is exactly what the retail Win32 client emits when the user clicks the first doorway after entering a starbase (matches the special-cased OldRoom=-1/NewRoom=0 starbase-entry branch at server/src/PlayerClass.cpp:638). WARP (0x009B) deferred — its HandleWarp path needs a separate session for the reactor/engine equipment-load + nav-distance lookup analysis. Next-Phase-K targets: 0x004E STARBASE_REQUEST (read starbase data — should have clean read-only path); 0x0057 SKILL_UP (skill progression on Player::m_Skills); 0x005A VERB_REQUEST (NPC talk-tree exercise).

- [x] Wave 14 (2026-05-25): 0x0014 MOVE survival round-trip — engine-on toggle, the most-common in-flight wire frame.
      Status: done — `SectorMoveTests.Move_EngineOn_DoesNotBreakConnection_RequestTimeStillRoundTrips` passes; full integration suite green from the running stack. Like Wave 13 this is zero-server-change: Phase R PacketStructures.h already had MovePacket correctly typed as `{int32_t GameID; char type;}` (5B canonical, common/include/net7/PacketStructures.h:981), Wave 12's template AddData specialisation made every reply path 4B-canonical, and the proxy passes MOVE through cleanly — `proxy/ClientToServer_linux_stubs.cpp:487-489` is a literal `break;` so the frame falls through to the bottom-of-switch `g_ServerMgr->m_UDPConnection->ForwardClientOpcode(...)`, matching Win32. Server-side `Player::HandleMove` (server/src/PlayerConnection.cpp:1843) reads MovePacket, calls `AbortProspecting`/optional `LeaveFormation`/`FormationEngineOperation(this, type!=4)`/`Move(type)` — all pure state mutators behind an `if (!WarpDrive())` guard (true for fresh starbase characters). type=1 selects the engine-on branch (anything != 4 = engine on per line 1861 switch).
      Touches: `tests/integration/CliClient.IntegrationTests/Opcodes/SectorMoveTests.cs` (new — uses Pool[9] cli_test11 to avoid colliding with Pool[3..8]; 90s budget; 400-frame drain cap; xmldoc documents survival-probe rationale, MovePacket sizeof(long) regression class, FormationEngineOperation/AbortProspecting/WarpDrive guard hazards, and the CLAUDE.md server-integrity note that type=1 matches retail W-key press), `tests/integration/CliClient.IntegrationTests/TestAccounts.cs` (Pool gains cli_test11 entry — 9_000_011 skips 9_000_010 which is the out-of-pool StressTestClosed fixture), `tests/integration/CliClient.IntegrationTests/Fixtures/seed.sql` (cli_test11 INSERT row, status=100), `tools/cli-client/src/CliClient.Core/Opcodes/OpcodeId.cs` (+0x0014 Move), `tests/integration/CliClient.IntegrationTests/Coverage/TestedOpcodes.cs` (MinTestedCount 23→24; sorted-position entry for 0x0014 MOVE between 0x0013 TILT and 0x001D MESSAGE_STRING).
      Notes: Phase K coverage ratchet 23/207 → 24/207 (11.6%). Per CLAUDE.md server-integrity rules: zero server-side changes — Wave 12's clean-wire substrate plus Phase R MovePacket layout carried the load. The test sends only inputs the retail Win32 client emits; type=1 is what the client sends when the user hits W. No fabricated reply (HandleMove emits none on this branch and retail server doesn't either). Next-Phase-K targets: 0x009B WARP (server-side HandleWarp at PlayerConnection.cpp:1876 sets up nav array + calls PrepareForWarp; needs Navigation array seeded with valid sector_id pair); 0x0057 SKILL_UP (skill progression — pure server state mutator on Player::m_Skills); 0x005A VERB_REQUEST (NPC interaction — exercises talk-tree path); 0x0034 inverse → 0x0044 sweep for further opcodes that have no direct reply.

- [x] Wave 13 (2026-05-25): 0x002C ACTION survival round-trip — first opcode wave riding on the clean-wire substrate that Wave 12 established.
      Status: done — `SectorActionTests.Action_NoOpSubAction_DoesNotBreakConnection_RequestTimeStillRoundTrips` passes in 7s; full integration suite 44/44 green from the running stack (~3.5 min). No server-side changes required this wave — the Phase R PacketStructures.h migration already had ActionPacket correctly typed as 4× int32_t, the Wave 12 template AddData<long> specialisation made every server-side reply path 4B-canonical, and the Wave 3 proxy ForwardClientOpcode dispatch (proxy/ClientToServer_linux_stubs.cpp:471-477) already mirrors Win32's explicit-forward-then-ProcessAction shape. The test exercises the full 0x002C wire path end-to-end and the connection survives — which is the strongest assertion available because the chosen sub-action (23, "keep trading???") is a literal commented-out no-op in HandleAction (server/src/PlayerConnection.cpp:4104-4108) and the retail server emits no reply on this branch (per CLAUDE.md server-integrity, fabricating a reply is forbidden).
      Design — same survival-probe shape as Waves 10 (START_ACK) and 11 (TURN/TILT). ActionPacket dispatch in Player::HandleAction is a 30-ish entry switch on the Action int32_t at offset 4; sub-actions 1/7/8/9/25/26/28/29/30 all need authoritative in-space state (target obj, equipped weapons, registered starbase, etc.) that a freshly-handed-off starbase character doesn't have. Sub-action 23 has zero side effects, doesn't touch the Target field, doesn't touch the SectorManager, doesn't even SendVaMessage (the only line is commented out). It's the most preservation-faithful "exercise the dispatcher without mutating anything" probe available.
      Regression coverage — if anyone reverts PacketStructures.h ActionPacket back to `long` fields, the struct grows from 16B to 32B on Linux x86_64 and Action reads from offset 8 (where the wire has Target's high half) — would miss case 23, fall to default, hit the UNRECOGNIZED ACTION SendVaMessage path. Connection still survives in that regression (the SendVaMessage just routes a string reply back), but the explicit case-23 choice keeps the test's intent visible. Also catches: proxy ProcessSectorServerOpcode dropping the opcode (the per-test xmldoc has the full Linux dispatch citation); HandleAction's pre-switch `GetObjectFromID(Target=0)` null-deref (case 23 doesn't use obj, so a crash there would surface as REQUEST_TIME never coming back).
      Touches: `tests/integration/CliClient.IntegrationTests/Opcodes/SectorActionTests.cs` (new — uses Pool[8] cli_test09 to keep its Create/Delete cycle clear of Pool[3..7] owned by SectorLogin/SectorChat/SectorRequestTime/SectorStartAck/SectorTurnTilt; 90s budget; 400-frame drain cap; xmldoc covers why a survival probe rather than direct assertion, the regression class for ActionPacket long→int32_t revert, proxy ProcessSectorServerOpcode dispatch citation, HandleAction Target=0 null-deref hazard, and the CLAUDE.md server-integrity note that sub-action 23 is a published retail value and the no-reply behaviour matches retail), `tests/integration/CliClient.IntegrationTests/TestAccounts.cs` (Pool gains cli_test09 entry), `tests/integration/CliClient.IntegrationTests/Fixtures/seed.sql` (cli_test09 INSERT row), `tools/cli-client/src/CliClient.Core/Opcodes/OpcodeId.cs` (+0x002C Action), `tests/integration/CliClient.IntegrationTests/Coverage/TestedOpcodes.cs` (MinTestedCount 22→23; entry for 0x002C ACTION inserted in sorted position between 0x001D MESSAGE_STRING and 0x0033 CLIENT_CHAT).
      Notes: Phase K coverage ratchet: 22/207 → 23/207 (11.1%). Per CLAUDE.md server-integrity rules: zero server changes — Wave 12's clean-wire substrate carried the load. The test only sends inputs the retail Win32 client emits; sub-action 23 is one of the retail client's published values; the no-direct-reply pattern matches retail. Next-Phase-K targets: 0x009B WARP (HandleWarp_Linux helper already lit on the proxy side; server side dispatches through PlayerCombat); 0x0014 MOVE (proxy passes through directly per ClientToServer_linux_stubs.cpp:487-489; server handler is PlayerMoveable::HandleMove); 0x0057 SKILL_UP (skill progression — pure server state mutator); 0x005A VERB_REQUEST (NPC interaction — exercises the SaveManager talk-tree path).

- [x] Wave 12 (2026-05-25): finish the wire-relevant `long` sweep — kill the bug class at its template root rather than playing whack-a-mole at call sites.
      Status: done — full integration suite 43/43 green from a fresh `docker compose down -v` rebuild in 2.3 minutes (Wave 11 took 3.5 min; the speedup is unrelated, probably build cache). After Wave 11 committed I re-grepped and found ~34 wire-relevant `*((long *) ...)` sites remaining across `server/src/`, `proxy/`, and `login-server/`. Fixing each by hand was viable but failed the brutally-honest test: the **template** `AddData<T>` in `server/src/PacketMethods.h:23` was the actual root cause for hundreds of latent sites that *don't* show up in a `*((long *) ...)` grep — every `AddData(pptr, obj->GameID(), index)` call (and there are 586+ across the server) where the second arg is `long`-typed silently emits `sizeof(long)` bytes, i.e. 8 on Linux vs the wire's 4. Same for `AuxBase::AddData`. Fixing those at the template level closes the bug class once.
      Template fix — `server/src/PacketMethods.h:23` and `server/src/AuxClasses/AuxBase.h:32`: added explicit `AddData<long>` / `AddData<unsigned long>` specialisations that cast through `int32_t` / `uint32_t` and advance the index by 4. On Win32 `sizeof(long)==4` so the new spec is a no-op semantic change. On Linux the spec restores the canonical 4B wire shape on every AddData<long>(...) caller in one stroke. Verified no false-positive collision with the unrelated `mysqlplus::sql_query::AddData(field, value)` member overload in AccountManager.cpp (different signature, different class).
      Remaining call-site fixes — finished the manual sweep for completeness on the sites where the template fix wasn't enough (the direct `*((long *) ...)` casts, not template AddData):
       - `server/src/PlayerConnection.cpp` — 14 more sites beyond Wave 11: line 838 `*((long *) &buffer[4]) = avatar_id` in SendDataFileToClient (file-format wire write, 4B avatar slot); lines 1055-1056 point_data writes in SendPointEffect (4B wire slot stride); lines 1945+1948 Contrails() aux_data GameID + bool slot; line 1958 SendResourceName aux_data resourceID; line 2167 SendAuxNameSignature packet GameID; line 2192 SendAuxNameResource packet GameID; lines 2289+2290+2298 SetResourceDrainLevel aux_data GameID + slot_index + stack count; line 2326 SetHuskDrainLevel `AddData(pptr, long(0), index)` (caller side — switched to `int32_t(0)` because the next line `*((long *) &pptr[9]) = slot_index` overwrites the slot AddData just placed at offset 9, and AddData(long) would have placed the offset at 13 on Linux); line 2328 SetHuskDrainLevel `*((long *) &pptr[9]) = slot_index` (4B wire slot overwrite); line 2370 SendProspectAUX case 0 aux_data offset-15 wire slot; line 2422 SendProspectAUX case 3 aux_data GameID; lines 3613+3614 SendAttackerUpdates attacker_data update + mob_id (4B+1B+4B layout — 8B write at offset 5 would have run off the end of the 9B buffer); lines 4359+4361 SendResourceContentsAUX aux_data GameID + level. The remaining 4 hits in PlayerConnection.cpp (4429/4430/4436/4437) are inside the `/* ... */`-commented-out `SendResourceContentsAUXForHulk` and are dead code — left as-is.
       - `server/src/SaveManager.cpp` — 2 more sites in `HandlePetition` (1271, 1273): inbound petition packet GameID + ProblemType — classic Wave 11 over-read pattern.
       - `server/src/PlayerSaves.cpp:126` — `*((long *) &pos_data[28])` for sector-num in the 32-byte position save buffer (an 8B write would have overflowed the buffer).
       - `server/src/PlayerClass.cpp` — 2 sites: line 1028 subparts packet 4B slot overwrite; line 3332 weapon-fire packet inbound GameID read (must be 4B to match the equality compare against `this->GameID()` which is itself a 4B wire GameID).
       - `server/src/ServerManager.cpp:875` and `server/src/UDPConnection.cpp:106` — `*((unsigned long *) host->h_addr_list[0])` was reading 8 bytes from a 4-byte `in_addr` slot. Even though `ip_address` was then truncated by the assignment, the 4 garbage bytes pulled from past `h_addr_list[0]` could have come from anywhere — fixed to `uint32_t*`.
       - `proxy/ServerManager.cpp:432` — same h_addr_list 4B IPv4 fix as the server side.
       - `proxy/UDPProxyMVAS.cpp:71+182` — inbound UDP plane reads: TerminateClient player_id and ToggleSendFrequency g_thread_speed. The TerminateClient one was the most dangerous of the bunch because the equality compare `if (player_id == m_PlayerID)` would have silently never matched on Linux until both sides agreed on width — Wave 11 fixed m_PlayerID's write-side; Wave 12 fixes the read-side.
       - `proxy/ClientToSectorServer.cpp` — 7 sites in QueueAuxNameSignature/QueueAuxNameResource/QueueNavigation/QueueResourceName/QueueContrails/QueueProspectAUX: same aux_data + packet GameID 4B slot fixes as their server-side mirrors.
       - `proxy/UDPClient.cpp:326` — `(float)*((long *) &msg[0])` for SignalWrongVersion. This site is inside a Win32-only block (the function calls `::MessageBox`) so it's not compiled on Linux, but fixed for parity.
       - `login-server/Net7SSL/UDPClient.cpp:159+164` — `g_MaxPlayerCount` and `g_PlayerCount` from inbound SSL register/playercount payloads.
      What this sweep does NOT touch (carried over from Wave 11): internal counters, time deltas, loop indices, queue API parameters (`CheckQueue(... long *player_id)`), mysql vendored headers (`server/src/mysql/my_global.h`, `server/src/mysql/config-win.h` — `sint4korr` / `int4store` macros are MySQL/MariaDB upstream code; per CLAUDE.md `server/third_party/**` is not ours to rewrite).
      Touches: `server/src/PacketMethods.h` (AddData<long>/<unsigned long> specialisations), `server/src/AuxClasses/AuxBase.h` (AuxBase::AddData(long) / AuxBase::AddData(unsigned long) overloads), `server/src/PlayerConnection.cpp` (14 sites), `server/src/SaveManager.cpp` (2 sites), `server/src/PlayerSaves.cpp` (1 site), `server/src/PlayerClass.cpp` (2 sites), `server/src/ServerManager.cpp` (1 site), `server/src/UDPConnection.cpp` (1 site), `proxy/ServerManager.cpp` (1 site), `proxy/UDPProxyMVAS.cpp` (2 sites), `proxy/ClientToSectorServer.cpp` (7 sites), `proxy/UDPClient.cpp` (1 site), `login-server/Net7SSL/UDPClient.cpp` (2 sites).
      Notes: Phase K coverage ratchet unchanged at 22/207 (this wave is bug-class elimination, not opcode coverage). Per CLAUDE.md server-integrity rules: every change here is preservation-aligned — restoring Win32 4B wire widths that Linux was over-reading/over-writing. No server permissiveness was added; no input validation was loosened. The template specialisation in particular makes Linux's `AddData<long>(...)` behave **exactly** like Win32's, which is the strongest form of preservation parity possible. The Wave 11 framing "fix all longs" is now substantively complete for the server-native code; further `long`s in the codebase are either internal scratch (correct to keep 64-bit on Linux) or in code paths not compiled on Linux. Next-Phase-K targets unchanged: 0x002C ACTION (combat trigger — Player::HandleAction dispatch table); 0x009B WARP (HandleWarp); 0x0014 MOVE.

- [x] Wave 11 (2026-05-25): 0x0012 TURN + 0x0013 TILT survival round-trip — sends TURN then TILT, then validates the pipe with a follow-up 0x0044 REQUEST_TIME / 0x0034 CLIENT_SET_TIME round-trip. Also: **wire-relevant `long` sweep across server-native code** (the "fix all longs" pass the user pushed for after seeing Wave 9 / Wave 10 hit the same `sizeof(long)` class repeatedly).
      Status: done — `SectorTurnTiltTests.TurnAndTilt_DoNotBreakConnection_RequestTimeStillRoundTrips` passes; full integration suite green from a fresh `docker compose down -v` rebuild. Closes the gap on the two simplest movement opcodes (TURN/TILT — pure state mutators on `Moveable::m_Turn_Intensity` / `m_Tilt_Intensity`). Design pivot identical to Wave 10: a direct-reply assertion is **fundamentally unreachable** for movement opcodes because their fan-out happens later via `SendPositionalUpdate` on a sector tick, and the update is sent to OTHER observers in the visibility list — never back to the originating client. With a single-player integration test there is no other observer to receive the fan-out, so we use the same survival-probe shape as START_ACK: send TURN, send TILT, send REQUEST_TIME, assert CLIENT_SET_TIME echoes the sentinel tick. Pipe survival proves the server didn't crash on the local `PacketTurn` over-read, the proxy didn't drop the UDP plane on the per-opcode throttle, and `AbortProspecting()`/`WarpDrive()` guards didn't crash mid-check.
      Server fix 1 — `server/src/PlayerConnection.cpp:1793-1827` `HandleTurn` and `HandleTilt` local `PacketTurn` struct: `{ long GameID; float Intensity }` → `{ int32_t GameID; float Intensity }`. Win32 wire payload is 8B (4B GameID + 4B float); on Win32 `sizeof(long)==4` so the struct was 8B matching wire exactly. On Linux x86_64 `sizeof(long)==8` so the struct became 12B and `Intensity` read from offset 8 — past the end of the 8B wire payload, into undefined memory `data[8..]`. The garbage float would then go to `Moveable::Turn(intensity)` / `Moveable::Tilt(intensity)` and propagate through the physics tick on the next move — potentially as NaN.
      **Concurrent wire-relevant `long` sweep** (the user message: "can you just fix all longs? Like its obvious they are all 100% incorrect just switch them everywhere in the codebase to int32_t right?"). I pushed back on the literal interpretation — a global `s/long/int32_t/` is wrong because internal counters (loop indices, time deltas, queue API parameters like `CheckQueue(... long size, long *player_id)`) should stay 64-bit on Linux; silently narrowing them risks overflow bugs. Committed to the substantively-correct version: sweep every `long` that touches the wire — local packet struct definitions, `*((long *) data)` / `*((long *) &buffer[N])` reads/writes of inbound/outbound wire bytes, and ATTRIB_PACKED struct fields — leaving internal scratch alone. Sites swept this wave:
       - `server/src/SaveManager.cpp` ~14 sites: HandelInfraction (260-261), HandleNewRecipe (341), HandleManufactureAttempt (361), HandleAdvanceLevel (377), HandleChangeInventory (456-457 — item_id int32, cost uint32), HandleChangeEquipment (556), HandleChangeAmmo (615), HandleStorePosition (744), HandleAdvanceMission (771), HandleAdvanceMissionFlags (818), HandleMissionRemove (916), HandleMissionComplete (931), HandleDiscoverNav (1057), HandleExploreNav (1076), HandleSetSkillPoints (1109), HandleSetRegisteredStarbase (1122), HandleNewWarnLevel (1531), HandleChangeFieldRespawn (1741). All were `long X = *((long *) &data[N])` over-reads from 4B wire slots.
       - `server/src/PlayerConnection.cpp` ~9 sites beyond the Wave 11 PacketTurn fix: SendOpcode wire writes (792, 796) — `*((long *) &buffer[4]) = GameID()` and `*((long *) &buffer[12]) = avatar_id` were writing 8B over 4B slots, corrupting the next 4B; wire read 873; line 1310 `long *pLong = (long *) &packet[2]` for AdvancedPositionalUpdate (slot stride must be 4B — confirmed by `length = 2 + 4 * index;` immediately below); 1679 size header; 3649 TradeAction GameID into a 5B buffer where long-write overflowed; 10191 chat-stream GameID (header sums to 2+1+4=7 per the inline comment).
       - `server/src/PlayerConnection.cpp` GalaxyMap struct (10669-10674): `long Type; long Size; long PlayerID; char Variable[64]; long unknown;` → all `int32_t`. Struct went from 96B on Linux to 80B matching the Win32 wire shape (4+4+4+64+4=80). The trailing `unknown` write at 10706 also flipped to int32_t for symmetry.
       - `proxy/ClientToSectorServer.cpp` 4 sites: lines 50, 155, 157, 199 — all `*((long *) &m_RecvBuffer[0])` / `*((long *) &buffer[4])` over-reads on inbound wire. Line 157's SetStartID accepts a `long` parameter; the 4B wire read is widened on assignment which is fine.
       - `proxy/Connection.cpp` 5 sites in the RC4 key-exchange handshake: line 163 inbound length prefix `ntohl(*((unsigned long *) buffer))` → `(uint32_t *)`; line 257 outbound length prefix; 253/256/272 `sizeof(long)` → `sizeof(uint32_t)` arithmetic. The canonical wire spec is 4B big-endian length + WWRSA_BLOCK_SIZE-byte encrypted block (matches CLI client RsaHandshake.cs:101). Linux↔Linux worked by accident before — `ntohl` truncates and the LE zero-init of the high bytes made the 8B read+write effectively a 4B read+write.
       - `login-server/Net7SSL/Connection.cpp` 5 sites — mirror of `proxy/Connection.cpp` RC4 fixes (lines 164, 232, 235, 236, 247). Phase Q deleted the kyp-era TCP cluster but Net7SSL retains its own RC4 handshake for legacy callers; this side needed harmonisation to the same 4B canonical wire shape.
       - `login-server/Net7SSL/ClientToGlobalServer.cpp` + `proxy/ClientToGlobalServer.cpp` (each 2 sites): HandleDeleteCharacter + HandleGlobalTicketRequest `long X = ntohl(*(long *) m_RecvBuffer)` → `(uint32_t *)`. Same class of bug as the Wave 5 `PacketMethods.h::ExtractLong` fix that landed alongside the 0x0071 round-trip — just other call sites with the same pattern.
      What this sweep does NOT touch (and why): internal counters and time deltas in the same files (`for (long i = 0; i < count; i++)`, `long delta_ms = ...`) — those are scratch values that never see the wire, and `long` on Linux is 64-bit which is wider not narrower than Win32, so they're never the cause of wire-format drift. The `CheckQueue` queue API: `bool CheckQueue(unsigned char *pMessage, int *length, long size, long *player_id)` — the local `long player_id;` vars in `PulsePlayerInput` / `SendPacketCache` must stay `long` to match the queue's pointer type. Narrowing them would force the queue API change or introduce a cast-through-`int32_t*` aliasing trap.
      Touches: `server/src/SaveManager.cpp`, `server/src/PlayerConnection.cpp`, `proxy/ClientToSectorServer.cpp`, `proxy/Connection.cpp`, `proxy/ClientToGlobalServer.cpp`, `login-server/Net7SSL/Connection.cpp`, `login-server/Net7SSL/ClientToGlobalServer.cpp`, `tests/integration/CliClient.IntegrationTests/Opcodes/SectorTurnTiltTests.cs` (new — uses Pool[7] cli_test08 dedicated to avoid colliding with Pool[3..6] which are owned by SectorLogin/SectorChat/SectorRequestTime/SectorStartAck; 90s budget; 400-frame drain cap; xmldoc covers why it's a survival probe rather than direct assertion, the Wave 11 regression class, AbortProspecting/WarpDrive guard hazards, and the CLAUDE.md server-integrity note that intensities 0.5 / -0.25 are within retail client stick range), `tests/integration/CliClient.IntegrationTests/TestAccounts.cs` (Pool gains cli_test08 entry), `tests/integration/CliClient.IntegrationTests/Fixtures/seed.sql` (cli_test08 INSERT row; comments updated 9000007→9000008 and "all seven"→"all eight"), `tools/cli-client/src/CliClient.Core/Opcodes/OpcodeId.cs` (+0x0012 Turn, +0x0013 Tilt), `tests/integration/CliClient.IntegrationTests/Coverage/TestedOpcodes.cs` (MinTestedCount 20→22; entries for 0x0012 TURN and 0x0013 TILT).
      Notes: Phase K coverage ratchet: 20/207 → 22/207 (10.6%). Per CLAUDE.md server-integrity rules: the local `PacketTurn` struct fix is preservation-aligned (restores Win32 8B wire shape — the Linux 12B was the divergence from retail) and does not loosen any input check. The wider `long` sweep is also preservation-aligned: every site is restoring a Win32 4B wire width that Linux was over-reading or over-writing by 4B. No server permissiveness was added; the intensity values 0.5f / -0.25f are well within the retail client's stick range. Next-Phase-K targets: 0x002C ACTION (combat trigger — Player::HandleAction dispatch table); 0x009B WARP (HandleWarp — already has a Linux helper in proxy/ClientToServer_linux_stubs.cpp:HandleWarp_Linux, server-side reply path not yet exercised); 0x0014 MOVE (translation input; closes the basic-movement triad with TURN+TILT).

- [x] Wave 10 (2026-05-25): 0x0006 START_ACK survival round-trip — sends START_ACK, then validates the pipe with a follow-up 0x0044 REQUEST_TIME / 0x0034 CLIENT_SET_TIME round-trip.
      Status: done — `SectorStartAckTests.StartAck_DoesNotBreakConnection_RequestTimeStillRoundTrips` passes in 7s. Closes the gap: pre-Wave-10 the START_ACK code path was untested — both the server's `Player::HandleStartAck` (`server/src/PlayerConnection.cpp:1603`) and the proxy's `ENB_OPCODE_0006_START_ACK` branch (`proxy/ClientToServer_linux_stubs.cpp:413-449`, which synthesises a 0x3008 STARBASE_LOGIN_COMPLETE for sector>9999 or 0x3004 PLAYER_SHIP_SENT for sector<9999) had no integration coverage. Now they do.
      Design pivot — original Wave 10 conception was START_ACK → 0x0092 CAMERA_CONTROL, which would have been a direct reply assertion. That is **fundamentally unreachable** for a freshly-created character: `HandleStartAck` only calls `SendLoginCamera()` when `sectorNum < MAX_SECTOR_ID` (`server/src/Net7.h:363` — `MAX_SECTOR_ID=9999`), but every entry in `StartSector[]` (`server/src/StaticData.h:63-74`) is a starbase >9999. Per CLAUDE.md server-integrity rules ("NEVER make the server accept inputs or behaviour the real server did not"), we cannot drop the MAX_SECTOR_ID guard or fabricate a CAMERA_CONTROL emit for the starbase branch — that's divergence from retail. So the test pivots to an indirect survival probe: send START_ACK + assume the proxy fires its 0x3008 follow-up, then send REQUEST_TIME and assert the echoed sentinel comes back. Pipe survival proves both opcodes processed without crashing the server or the proxy and without tearing the UDP plane during the proxy's `SetLoginComplete(true)` triple-flip.
      Touches: `tests/integration/CliClient.IntegrationTests/Opcodes/SectorStartAckTests.cs` (new — uses Pool[6] cli_test07 dedicated to avoid colliding with SectorLogin/SectorChat/SectorRequestTime on IsUsernameUnique; 90s budget; 400-frame drain cap; xmldoc records why CAMERA_CONTROL was the wrong target and what the survival probe actually catches), `tests/integration/CliClient.IntegrationTests/TestAccounts.cs` (Pool gains cli_test07 entry), `tests/integration/CliClient.IntegrationTests/Fixtures/seed.sql` (cli_test07 INSERT row; comments updated 9000006→9000007 and "all six"→"all seven"), `tests/integration/CliClient.IntegrationTests/Coverage/TestedOpcodes.cs` (MinTestedCount 19→20; entry for 0x0006 START_ACK).
      Notes: no server or proxy changes — both paths were already wire-correct from the Wave 7 sizeof(long) sweep and the existing proxy 0x3008/0x3004 synthesis (which has been there since the Phase K Linux sector-stubs port — Wave 3). Phase K coverage ratchet: 19/207 → 20/207 (9.7%). CAMERA_CONTROL (0x0092) is **not** in TestedOpcodes — re-enable it as a real reply assertion once we have a test scenario that legitimately puts a player in an in-space sector (gate traversal from starbase, or an in-space spawn path). Removed the `OpcodeId.Known.CameraControl` enum entry as well — no test or producer currently references it, and the OpcodeId.Known list is documented as the integration-tested subset. Next-Phase-K targets: 0x0012 TURN and 0x0013 TILT (simplest movement opcodes — `sizeof(long)` review on `HandleTurn`/`HandleTilt` payloads expected, plus the new movement-fan-out path across sector players), 0x002C ACTION (combat trigger).
