# Master plan — Earth & Beyond emulator consolidation & modernization

This is the source of truth for what's done and what's next across invocations. Every Claude invocation in this repo MUST read this file first, then the plan file for the in-progress phase.

## Status table

| # | Phase | File | Status | Started | Last updated |
|---|---|---|---|---|---|
| A | Repo merge + scaffolding | [01-phase-a-merge.md](01-phase-a-merge.md) | complete | 2026-05-22 | 2026-05-22 |
| B | Best-effort Linux server build | [02-phase-b-linux-server.md](02-phase-b-linux-server.md) | complete | 2026-05-22 | 2026-05-22 |
| C | Postgres migration | [03-phase-c-postgres.md](03-phase-c-postgres.md) | complete (scaffold; mysqlplus.cpp rewrite is continuation) | 2026-05-22 | 2026-05-22 |
| D | C# tools → .NET 10 | [04-phase-d-csharp-tools.md](04-phase-d-csharp-tools.md) | complete (16 csproj build clean; itemeditor/enbpatcher deferred — need new csproj) | 2026-05-22 | 2026-05-22 |
| E | OpenSSL 1.0 → 3.x | [05-phase-e-openssl.md](05-phase-e-openssl.md) | complete (server clean under API_COMPAT 3.0; proxy/login-server prepped for later) | 2026-05-22 | 2026-05-22 |
| F | Warning cleanup | [06-phase-f-warnings.md](06-phase-f-warnings.md) | complete (baseline + 2 categories fixed; long-tail deferred per WARNINGS_BASELINE.md) | 2026-05-22 | 2026-05-22 |
| G | Tests | [07-phase-g-tests.md](07-phase-g-tests.md) | complete (3 binaries, CI ctest job, fixed Phase D sln→slnx CI regression; subsystem tests need source isolation first) | 2026-05-22 | 2026-05-22 |
| H | Deepen docs | [08-phase-h-docs.md](08-phase-h-docs.md) | complete (4 new docs + §8 flow walkthroughs + capture analysis in docs/03 §8 with 120k-packet histogram) | 2026-05-22 | 2026-05-23 |
| I | Dev env polish | [09-phase-i-dev-env.md](09-phase-i-dev-env.md) | complete (justfile/Dockerfiles/CI matrix/release.yml/pre-commit; wine-tools profile deferred) | 2026-05-22 | 2026-05-22 |
| J | Runnable end-to-end | [10-phase-j-runnable.md](10-phase-j-runnable.md) | complete (server UDP binds; Net7Proxy bind + RSA+RC4 handshake + framed opcode dispatch on Linux; Net7SSL login binds, terminates TLSv1.3, parses /AuthLogin against MySQL, issues tickets; AF_UNIX SOCK_DGRAM IPC bus replaces Win32 mailslots; live integration tests 5/5; in-game opcode handlers + UDP proxy plane + ticket handoff carry to Phase K) | 2026-05-23 | 2026-05-23 |
| K | In-game opcode handlers + UDP proxy plane | [11-phase-k-ingame.md](11-phase-k-ingame.md) | in progress -- login->undock->dock->logout decodes byte-exact vs the live net7 capture (Wave 333); server fix landed for the u64 Aux wire fields that were truncated to 4 bytes on Linux and crashed the retail client at login (Wave 334, commit 2cd8ca29); "Luna has no waypoints" diagnosed as exploration-gating, not missing data (Wave 335); 0x2019 RESOURCE_OBJECT_CREATE wire deficit fixed -- dropped 2nd HSV channel, 52->56 bytes, byte-exact vs the live capture (Wave 336); systematic S2C divergence audit of both live 2026-06-02 captures -- 22 misdiagnosed "ShipIndex" frames are AuxMobIndex, now decode byte-exact, only divergence is the embedded AuxPercent shield form, deferred (Wave 337); the 3 residual 0x001B AuxHulkIndex frames now decode byte-exact too (nested AuxInventory20/40 + AuxItem; bodyLen=payload-8 inventory-bearing-create quirk verified vs pcap) -- the 0x001B audit is fully closed, only the deferred AuxPercent shield-form server alignment remains (Wave 338); AuxPercent shield divergence investigated to a firm "do NOT touch the shared serializer" with sharpened unblock criteria (Wave 339); CI spam-failure fixed -- libsodium-dev added to the cmake jobs, 8 mojibake'd license headers restored, and the architecturally-impossible live-replay/MasterJoin ctests corrected to verify handshake+send-path only (the session-backed redirect round-trip is a Phase-T xUnit concern), all jobs green (Wave 340); the dock->space undock transition is fixed and works (owner-confirmed 2026-06-04; tasks #64/#65 completed -- CLI follows the undock handoff into space and the MVAS position feed is accepted). FULL wave-by-wave log (Waves 1-340) lives in the phase file -- do NOT inline per-wave detail in this status cell (it had grown to a 156KB single table row, blowing the every-startup read budget). | 2026-05-23 | 2026-06-02 |
| L | C# tools -> Avalonia (Linux-native UI) | [12-phase-l-avalonia.md](12-phase-l-avalonia.md) | complete -- all 15 C# tools have Linux-native Avalonia paths (Tiers 0-12e); sector-editor-avalonia boots end-to-end on a Piccolo2D->Avalonia shim that ships its own Pen/Brush types to avoid libgdiplus; launchnet7 LocalAuthRelay fixes the WINE schannel login flake via a loopback-plaintext -> upstream-TLS bridge on port 4180. See the phase file for the full per-tier/per-editor port log. | 2026-05-23 | 2026-05-28 |
| M | Eliminate Win32 from server-native code | [13-phase-m-win32-elimination.md](13-phase-m-win32-elimination.md) | complete — all 3 compat/ dirs deleted; mailslot→AF_UNIX rewrite for server+login MailslotManager; CreateMutex→flock via net7ipc::SingleInstance; _beginthreadex→pthread_create for every Linux-active call site; OpenSSL opensslconf.h Win32-gating patch removes the last <windows.h> drag; CMakeLists references stripped; CreateEvent/WaitForSingleObject audit: zero live hits — comment-only; vocabulary sweep landed: Sleep→usleep, GetTickCount→Net7TickMs via new common/include/net7/Ticks.h, WSAGetLastError→errno across ~20 .cpp files (commits dc5cfe4 + 2e9937b + 623300c). Umbrella headers Wave 1–5 stripped (dc5cfe4 + 2e9937b + 2538444 + 69e1445 + this commit): all three Net7.h variants now expose only SOCKET/INVALID_SOCKET/SOCKET_ERROR + canonical socket macros; ~70 Win32 typedefs/macros deleted; 23+ Linux-active call sites replaced with POSIX equivalents per umbrella. Wave 5 (this commit): proxy/Net7.{cpp,h} retired the obsolete Win32 client-launcher main (CreateProcess+Detours+WriteProcessMemory) — kept only the server-side Linux main + no-op stubs for WIN32-walled TU references. Pure-typedef strip resolved as "won't fix; cosmetic-only" per CLAUDE.md "don't refactor for hypothetical future requirements" — SOCKET kept (60+ canonical Linux-active sites, rewrite is zero-behavioural-change); DWORD/LPTSTR/HANDLE all gone from umbrellas, remaining audit hits exclusively inside `#ifdef WIN32` walls (dead on Linux). Audit grep: 154 raw / ~74 after filtering false positives (SOL_SOCKET, xstricmp), all SOCKET typedef + WIN32-walled. 20/20 ctest + 33/33 cli-integration green against rebuilt containers. | 2026-05-23 | 2026-05-26 |
| N | `mysqlplus.cpp` → libpqxx rewrite (Phase C continuation) | [14-phase-n-libpqxx-rewrite.md](14-phase-n-libpqxx-rewrite.md) | complete — Wave 1 (commit fdfcbe5) reimplemented the 7-class wrapper over libpqxx 7.x preserving every public signature (opaque handles, `?`→`$N`, backtick→double-quote, `mysql_escape_string` shim). Wave 2 (2026-05-24, commits 7c54053→c8c09fe) added the parameterised API on top of `pqxx::params`, swept every server-native sprintf-SQL call site to the parameterised path, and walled the lone leftover (`login-server/Net7Mysql/Tab2.cpp`, MFC-only) under `#ifdef WIN32`. `tools/sql_injection_audit.sh` ratchet drove the surface 148 → 0 and was tightened mid-wave (commit 75a4ba4) to also catch multi-line sprintf idioms — a synthetic injection now trips it. LinuxAuth.cpp `ValidateAccountLinux` rewritten on `mysql_stmt_*` prepared statements; `SafeUsername`/`SafePassword` deleted. Wrapper test covers hostile-literal round-trip + mixed-types/NULL. CI integration of the audit gate and the postgres_smoke_test extension still open (small follow-ups, tracked in the plan file). Wave 3 (per-DAO SQL-dialect rewrites where MySQL syntax leaks through) deferred. | 2026-05-24 | 2026-05-24 |
| O | OpenSSL 3.x for proxy + login-server (Phase E continuation) | [15-phase-o-openssl3-rest.md](15-phase-o-openssl3-rest.md) | complete — plan-file scope was stale at open. Migration had already landed piecemeal in Phase E + Phase J: both proxy and login-server already define `OPENSSL_API_COMPAT=0x30000000L` (CMakeLists + Dockerfile); login-server `SSL_Listener.cpp` gates `SSLv23_server_method` behind `OPENSSL_VERSION_NUMBER >= 0x10100000L` so modern OpenSSL takes the `TLS_server_method` branch; proxy `SSL_Connection.cpp` and `ServerManager.cpp` legacy SSLv2/v23 sites are Win32-walled and dead on Linux. **Phase O+ (in-line continuation, commit f65211c)**: deleted the 73-file vendored `server/src/openssl/` 2010 OpenSSL 1.0 header tree (~30k LOC) that the include-path order was preferring over the system OpenSSL 3.x headers; normalised the five remaining quote-form `"openssl/..."` includes to angle form. Verified 2026-05-24: all four targets build clean against system OpenSSL 3.0.13, link `libssl.so.3+libcrypto.so.3`, full gtest suite 23/23 pass with 9 env-gated skips. Server binary 14 MB (was 13.7 MB — slight increase from genuinely using the 3.x struct layouts now). | 2026-05-24 | 2026-05-24 |
| P | Stub-debt audit (kyp-era dead-on-Linux classification) | [16-phase-p-stub-debt-audit.md](16-phase-p-stub-debt-audit.md) | complete — Phase Q deleted rows 1, 3, 4, 6 (kyp-era TCP cluster). Row 5 (`PlayerClass.h` silent stubs: `ChangeProspectSkill`/`AddScanSkill`/`ChangeTractorBeamSpeed`/`SetTCPTerminate`) instrumented to `LogMessage` on entry. Row 8 (`LinuxAuth.cpp` /who.cgi) comment rewritten to make deliberate-no-op intent explicit. Second-pass sweep over 407 marker hits found no further silent-stub hazards. CI drift-tracking script deferred (low ROI now that the dangerous category is empty). | 2026-05-23 | 2026-05-23 |
| Q | Delete dead kyp-era TCP cluster from server/src | [17-phase-q-kyp-cluster-deletion.md](17-phase-q-kyp-cluster-deletion.md) | complete (15 files removed: Connection/ConnectionManager/TcpListener/SSL_Listener/SSL_Connection/ClientTo{Master,Global,Sector}Server/EffectManager/JobManager_DEP_; ServerManager.h shed m_ConnectionMgr field + GetSSLConnection decl + Phase-P loud-abort stubs; PlayerConnection.cpp kept — misleadingly named, it's the live UDP send layer for Player::Send*) | 2026-05-23 | 2026-05-23 |
| R | Extract `common/` for shared protocol headers | [18-phase-r-common-headers.md](18-phase-r-common-headers.md) | complete (Wave 1 + Wave 2: 7 headers consolidated into common/include/net7/ — Opcodes.h, Mutex.h, WestwoodRC4.h, WestwoodRSA.h, PacketStructures.h, Packing.h, Ports.h. All 3 binaries link [163/16/8]; 22/22 tests green incl. 8/8 live. Caught 2 real wire-format drifts: MasterJoin long->int32_t backport to server+login; SECTOR_SERVER_PORT 3500-vs-3501 semantic split into PROXY_LOCAL_TCP_PORT/SECTOR_SERVER_PORT. CLAUDE.md + docs/02-architecture.md §10 updated. Wave 3 (Globals/CircularBuffer/cmdcodes/Net7Types) deferred to backlog — not wire-format.) | 2026-05-23 | 2026-05-23 |
| S | Headless CLI client (C# / .NET 10) | [19-phase-s-cli-client.md](19-phase-s-cli-client.md) | complete — Items 1-9 + 13-17 done (14/17 actionable items); Items 10-12 (enumerate sectors/missions/items workflows) `[!]`-blocked behind Phase K (server-side avatar handoff + sector connect not landed — without them the avatar list is empty and there is nothing to enumerate). 175 unit tests green, 207-opcode coverage via NamedOpaqueCodec + RegisterAllNamedOpaque(), retail-capture round-trip tests (MasterJoin binary ticket, ServerRedirect, ClientChat against 3 capture-3 frames), HealthGuard kill-switch, NDJSON packet+chat log sinks, ConsoleSink, connect-and-login + send-chat subcommands + REPL shell, AuthLogin TLS client, EncryptedTcpConnection with Westwood RC4+RSA handshake. CliClient.Core public surface verified driveable in-process by an external test project (Phase T). docs/15-cli-client.md is the user-facing reference; plans/20-phase-t-cli-integration-tests.md is the populated Phase T hand-off. Re-open Items 10-12 once Phase K wires GlobalConnect/GlobalTicket/GlobalAvatarList. **Post-completion polish (2026-06-01):** REPL UX (Tab arg-value fill, flow-ordered command suggestions via `Priority`, full-line colour gated on TTY) + containerised multi-client (`freya/cli-client/Dockerfile`, `docker-compose.cli.yml`, `just play-cli` -- one proxy per CLI unit, since the Net7Proxy is a single-client bridge; commit 7fd4cc1e). 261 unit tests green. See the phase file's two trailing sections for detail. | 2026-05-24 | 2026-05-24 |
| T | CLI-client-driven integration test suite (xUnit / .NET 10) | [20-phase-t-cli-integration-tests.md](20-phase-t-cli-integration-tests.md) | complete — Items 1-4, 6-10 done (9/10 actionable items); Item 5 partial: ConnectAndLoginTests landed, enumerate-* `[!]`-blocked behind Phase K (no avatar handoff → nothing to enumerate). 33/33 integration tests green: 3 capture-replay (codec round-trip identity against retail capture_1 frames 220/222), 9 Robustness against in-process ScriptedServer (per Phase T rule: never break the real server to test client robustness — ScriptedServer drives the server side of the RSA+RC4 handshake using WestwoodRSA.DecryptBlock), 6 Coverage ratchet (hand-maintained tested-opcode list with equality-not-inequality check vs. MinTestedCount=4 — baseline VERSION_REQUEST/VERSION_RESPONSE/MASTER_JOIN/SERVER_REDIRECT; emits `[coverage] N/207 (X.X%)` to CI logs; ramps with Phase K). CI: cli-integration-test GitHub Actions job (ubuntu-24.04, 20-min timeout, docker compose up, TRX upload via actions/upload-artifact@v4, compose-logs dump on failure); justfile recipes `cli-integration` (full, reuses or spawns stack) and `cli-integration-fast` (no-docker subset ~540ms). User-facing reference: docs/16-integration-tests.md (hard rules, layout, per-category coverage, fixture mechanics, ScriptedServer architecture, capture-replay format + 5-step "how to add a fixture", how to debug locally + in CI). Preservation finding: sector_id byte-order divergence between retail capture (LE-like) and our proxy (BE via ntohl) — flagged in fixture+codec+test comments per server-integrity rules; NOT "fixed". Re-open Item 5 enumerate-* once Phase K wires GlobalConnect/GlobalTicket/GlobalAvatarList. | 2026-05-24 | 2026-05-24 |
| U | Linux client-installer fixes (optional, late-stage) | [21-phase-u-linux-installer-fixes.md](21-phase-u-linux-installer-fixes.md) | in progress — 2026-05-26: **Wave 1 (LaunchNet7 TLS) landed; Wave 2 (C&S Creator soft-fail) landed + re-verified; Wave 3 (GNOME app-folder gsettings empty-typed-array crash) landed.** W1: live reproducer was "Error: 2 / Couldn't connect to update server" on Ubuntu + wine-11.8. Two-layer root cause: (a) `patch.net-7.org` now serves Let's Encrypt + only accepts TLS 1.2+, (b) LaunchNet7.exe is .NET 2.0/3.5 whose `ServicePointManager.SecurityProtocol` defaults to TLS 1.0. Fix: rewrote LaunchNet7.cfg `http://` → `https://`, added `SchUseStrongCrypto`+`SystemDefaultTlsVersions` reg keys for .NET 2.0/4.0 (native + Wow6432Node), `winecfg -v win7` after the N7 installer (existing `winetricks winxp` for vcrun2008/dotnet20 stays). W2: after the user's W1-fixed install, start-menu "Earth & Beyond" still ran the wine-default `Earth & Beyond.lnk` → `e&b.exe` (bypassing Net-7). Root cause was *not* winemenubuilder clobbering — it was the C&S Creator rc 71 hard-failing at upstream line ~865, exiting the script before the Net-7 proxy launcher generation (line ~1199), `~/.local/bin/enb*` symlinks (line ~1287), and `.desktop` Exec/Path rewrites (line ~1372) could run. Fix: demoted C&S install `\|\| { exit }` to a warn-and-continue (the main game + launcher don't depend on C&S), and guarded the post-install `mv ${CSC_LINUX_PATH_EXE}` so it no-ops cleanly under `errexit` when the source was never created. User directed local-fork-first on 2026-05-26 — `LOCAL_MODIFICATIONS.md` updated; upstream PR back to `ciphersimian/enb-linux-installer` is now a follow-up (W1.7 / W2.5), not gating. **W2.4 verified on user re-run 2026-05-26** — C&S warned-and-continued, proxy launcher + `~/.local/bin/enb*` symlinks created, start-menu rewrites started. W3: same re-run then crashed at GNOME app-folder gsettings — `gsettings get folder-children` returned `@as []` (typed empty array), upstream's strip-and-concat produced the invalid gvariant `[@as , 'enb']`. Fix: detect emptiness after stripping `@as` + whitespace; emit `['enb']` rather than `[, 'enb']`. Also `\|\| true` the three `gsettings set` calls — this entire block is cosmetic Activities-overview folder organisation, not install-critical. **Still open:** W1.6 fresh-prefix re-verify, W3.3 user-re-run-after-Wave-3 verify, W1.7 / W2.5 / W3.4 upstream PRs, plus original U.1–U.5 for the **C&S Creator rc 71** root cause (avatar customisation tool only; not blocking the main game). | 2026-05-26 | 2026-05-26 |
| V | Client isolation, launcher hardening, neutral hostnames | [22-phase-v-client-isolation.md](22-phase-v-client-isolation.md) | complete (single wave 2026-05-27): launcher version-checks authlogin.dll patches, mkcert flow replaces root-store install, /CLIENT path quoted, PatchRegistry64 added, one-shot backup helper for in-place client patches, NET7_UPSTREAM_HOST env + /UPSTREAM: CLI plumbed through proxy, all `local.net-7.org` references swapped for `localhost` across launcher/proxy/login-server/deploy/client-config, new docs/17-traffic-and-ports.md covers ports/TLS/OpenSSL3/mkcert end-to-end | 2026-05-27 | 2026-05-27 |
| W | Proxy Win32 cross-build (proxy runs under WINE next to the client) | [23-phase-w-proxy-win32-crossbuild.md](23-phase-w-proxy-win32-crossbuild.md) | complete (single wave 2026-05-27): MinGW-w64 toolchain + static OpenSSL 3 prefix at proxy/third_party/openssl-mingw64; new mingw-w64-x86_64.toolchain.cmake; CMakeLists Win32 branch (links ws2_32/crypt32/iphlpapi/pthread, static stdlib); Net7.h split into _WIN32 vs POSIX shim branches; WIN32→NET7_LEGACY_WIN32 rename across proxy/* to dodge windows.h re-defining WIN32; shared common/include/net7/Mutex.h made unconditional pthread (winpthreads); WSAStartup added to Net7.cpp main(); Net7Proxy.exe (5.1 MB, PE32+ statically linked) runs under WINE with banner + TCP 3801/3805 LISTEN + both UDP planes bound; Linux native build still clean | 2026-05-27 | 2026-05-27 |
| Y | Reference-data import (NPCs, skills, buffs, sector maps, items, quests) | [25-phase-y-data-import.md](25-phase-y-data-import.md) | in progress -- 2026-06-02: **Y1+Y4+Y5 import landed**. Source: the external reconstruct dataset (JSONL reconstruct dataset, NOT a Net-7 dump). Importer: `tools/dataimport-jsonl/generate_seed.py` -> `db/postgres/seed_phase_y.sql` (5888 lines, applied at boot by the `schema-init` service via a canary gate on `starbase_npcs.npc_Id=100000`; idempotent on re-apply via top-of-seed DELETE-WHERE-id>=100000 across all synth-touched tables). **Y1 dedup**: importer queries live PG for pre-existing `(first_name, last_name, starbase_id)` and SKIPS wiki NPCs that match schema.sql roster at same starbase; 349 of 505 wiki NPCs dedup'd out as schema duplicates. **Y1 placement**: JSONL `station` fuzzy-matched against `starbases.name` (52/65 exact + 5 aliases = 57/65). Vendors -> Bazaar (room_type=2), others -> Lounge (3), with fallback chains. Per-room `npc_index` ordinal. Final synth rows: 156 NPCs (83 placed + 73 ghost with NULL room_id), 28 factions, 50 manufacturers, 26 starbase_vendors + 26 vender_groups + 508 vender_inventory; pre-existing 556 vendors / 8608 inv untouched. **Y5**: 5003 item_base, 12+38 categories/subcategories. Synth IDs live at 100000+ so a future authoritative dump won't collide. **Does NOT yet close the Friendship 7 4-vs-77 ItemBase byte-deficit** -- still missing per-NPC avatar templates and item-type-specific tables (item_beam/item_engine/etc.); 73 ghost NPCs sit in DB with NULL room_id until their 8 starbases get wired into `starbases`. **Y4 (2026-06-02): 48 sector navigation markers imported** -- by-name + by-position diff of the reconstruct `sectors/json/*.jsonl` against the authoritative runtime surfaced 48 nav-point/hidden-nav-point markers absent from `sector_objects` (9382 existing). Importer: `tools/dataimport-jsonl/generate_seed_navs.py` -> `db/postgres/seed_phase_y_navs.sql`, synth ids 100000..100047, gated on `sector_objects.sector_object_id=100000`, idempotent. Coords are the dataset's /1000 values * 1000 back to the runtime frame (APPROXIMATE, not byte-accurate); the ~15 non-positional fields (type/asset/nav_type/radar/orient/etc.) are derived from each sector's own navs by visibility class. Gates/stations/planets/moons excluded (need relational rows the dataset lacks). **2026-06-02 Y9 importer BUILT (overturns the earlier Y9 "blocked" call):** prospecting nodes ARE importable by NAME. `tools/dataimport-jsonl/generate_seed_prospecting.py` -> `db/postgres/seed_phase_y_prospecting.sql` emits 529 type-38 harvestable-field sector_objects (synth ids >=100000, gated+idempotent) + 2537 oretype links + 1339 restype rows. resource names -> item_ids (339/342=99% via base_ore_list/item_base); (sector,nav) -> runtime nav-point object (93% of triples) supplying field position. The prior "no resource_id -> non-importable" verdict was an id-diff mistake (resource_id IS a type-38 sector_object_id; a node is constructable from name+position like Y4 navs). APPLIED + verified live (2026-06-03: 529 type-38 nodes present at sector_object_id>=200000; wired into schema-init, gated+idempotent, task #40). **2026-06-02 Y8 importer BUILT (overturns the earlier Y8 "blocked" call):** mob loot/drop links ARE importable by NAME. `tools/dataimport-jsonl/generate_seed_drops.py` -> `db/postgres/seed_phase_y_drops.sql` emits **2582 mob_items rows** (synth ids >=100000, gated on `mob_items.id=100000`, idempotent, wired into schema-init) for drop links where both mob+item resolve to runtime ids but the pair is absent from `mob_items` (8450 existing). The drop RATE is owner-approved generated (drop_chance=10.0/usage_chance=0/type=1/qty=1, grounded in the runtime distribution); the LINK is the real gap. The prior "0 of 2415 rows carry a numeric drop_chance -> non-importable" verdict was the same field-presence mistake as Y9 (it counted a field instead of diffing the (mob,item) links). APPLIED + verified live (2582 rows, joins resolve, idempotent). **551 links** whose mob/item lacks a runtime row -> `tools/dataimport-jsonl/phase_y8_unresolvable_drops.csv`, deferred to **Y10** (owner-confirmation-gated; never auto-started). **Y6/Y7 still blocked: missions + mob catalog have no importable gap -- the absent reconstruct tail lacks the load-bearing field each runtime row requires (base_asset_id / mission_XML); injecting fabricated-id rows is forbidden by the CLAUDE.md server-integrity rule absent a primary-source dump. See plans/25 "Finding 2026-06-01..02" + decisions-log. **Cleanup DONE 2026-06-03 (task #60): renamed the MySQL-named server DB layer to Postgres-neutral (server/src/mysql/ -> server/src/db/, mysqlplus -> sqlplus, USE_MYSQL_* -> USE_PG_*, g_MySQL_* -> g_DB_*); green build + clean boot. Follow-up DONE 2026-06-03 (task #61): deleted the dead #ifndef non-SQL fallback path + USE_PG_*/SQL_ENABLE toggles (348 lines, byte-identical binary, green build + clean boot). Follow-up DONE 2026-06-03 (task #62): renamed the login-server/Net7SSL + proxy MySQL-named copies to g_DB_*, renamed the MYSQL_*->DB_* env contract in lockstep across LinuxAuth.cpp + docker-compose.yml, deleted dead USE_MYSQL_*/SQL_ENABLE/SQL_ACCOUNT_STRING/LogMySQLMsg/proxy externs; green login build + clean boot + 3 live TlsLoginTests pass. The MySQL-name cleanup across all three binaries (server + login + proxy) is now complete.** | -- | 2026-06-02 |
| X | Password hash modernization (MD5 -> Argon2id, no back-compat) | [24-phase-x-password-hash-modernization.md](24-phase-x-password-hash-modernization.md) | **done 2026-05-29.** All 5 waves + W6 follow-up landed. W1: libsodium vendored via Debian apt (`libsodium-dev`/`libsodium23`) in both `server/Dockerfile` + `login-server/Dockerfile`; pkg-config-driven CMake link in both targets. W2: `accounts.password varchar(40)` -> `accounts.password_phc text` (Postgres) / `varchar(128)` (MySQL source); dev admin row carries Argon2id PHC for `'devadmin'`; destructive migration documented in `db/postgres/README.md`. W3: `LinuxAuth.cpp::ValidateAccountLinux` + `AccountManager::{ValidateAccount,AddUser,ChangePassword}` all read `password_phc` via parameterized `$1`/`?` then call `crypto_pwhash_str_verify` in-process — plaintext never crosses the Postgres wire. Static `EnsureSodiumReady()` / `EnsureAccountSodiumReady()` lazy-init. Both targets build clean. W4: `TestAccounts.cs` inserts a precomputed Argon2id PHC constant for `SharedPassword "testpw"` (no C# Argon2 dep needed); dead `EnsurePgcryptoAsync` deleted; `convert.sh` now appends a `DO $reset_identity$` setval block (fixes pre-existing PK-collision bug exposed by clean rebuild). `TlsLoginTests` (Valid/Wrong/Nonexistent) all pass against rebuilt stack. W5: legacy `login-server/Net7SSL/AccountManager.cpp` (1028 lines, 100% `#ifdef WIN32`) deleted; `pg_stat_statements` redaction audit verified LinuxAuth uses `exec_params` with `$1`; decisions-log entry appended. **W6 (2026-05-29):** Added `Argon2idChangePasswordRoundtripTests.SlashChangepassword_RewritesPhc_OldPasswordRejected_NewPasswordAccepted` -- drives a real sector handshake, fires `/changepassword <newpw>` via 0x0033 CLIENT_CHAT, pins the 0x001D success reply byte-exactly, then re-runs AuthLogin and asserts the OLD password is rejected and the NEW password is accepted. End-to-end coverage of `MatchOptWithParam` -> `AccountManager::ChangePassword` -> `HashPasswordToPhc` -> parameterised UPDATE -> separate login-server process's `crypto_pwhash_str_verify`. Wave 189 already pinned the missing-arg ERROR fork; this is the matching success-path pin. +0 Phase K ratchet (0x0033 + 0x001D already covered); meaningful Phase X regression coverage. Verified: passes in 8s. Wire protocol untouched; server-internal change only -- preservation rules intact. | -- | -- |
| Z | Server-emitter fidelity (retail-vs-emulator wire divergences) | [26-phase-z-emitter-fidelity.md](26-phase-z-emitter-fidelity.md) | in progress -- 2026-06-03: durable register for places where OUR server emits bytes that disagree with the retail capture, surfaced by the Phase-S CLI Tier-2 decode work. NOT a license to loosen the server: every entry either keeps the server as-is pending primary-source proof at the "near-irrefutable confidence" bar, or records a fidelity-tightening fix that already shipped with a capture citation. Confirm-and-resolve workflow + classification (A wire-value mismatch / B id-allocation / C capability gap / D by-design). **8 findings catalogued; Z-1 + Z-2(b) + PB-4 resolved via 11 live-retail captures (2026-06-15), Z-R1 resolved.** Z-1 `[x]` 0x00BD CTA_RESPONSE field@4: RESOLVED NOT-a-divergence -- cap7/cap9 paired 0xBC->0xBD frames show field@4 is the echoed GroupAction selector + field@8 Success, exactly what our server emits; the earlier {13,14,15,17} "beacon/feature-broken" theory was false and is withdrawn. Z-2 `[~]` 0x005F: (b) RESOLVED -- emote byte@2=0x01 is correct (cap5's 3 emotes confirm); (a) RE-SCOPED -- station local chat is 0x001D MESSAGE_STRING color 2, NOT a 0x5F broadcast (old capture_3 reading was wrong); our SendMessageString already emits that shape, remaining is a two-client cross-delivery repro. Z-3 `[!]` 0x0005 START emits CharacterID vs retail's sector-local id (id-allocation; internally consistent, low pri, high-risk to change). Z-4 `[~]` 0x0034 ServerSent equal vs retail +1 tick (cosmetic, accept). Z-5 `[~]` 0x0097 GALAXY_MAP: PB-4 FIXED via proxy serve (UDPClient::SendCachedGalaxyMap streams GalaxyMap.dat on the 0x2010/0x0097 request, CV-MAP); server-side Type-5..9 streaming stays a lower-pri gap. Z-6 `[gap]` 0x0021 PUSH_MESSAGE never emitted (capability gap, feature work). Z-7 `[~]` 0x003A ticket ASCII vs retail opaque binary (by design, decisions-log 2026-05-23). Z-R1 `[x]` 0x0036 SERVER_REDIRECT sector_id LE -- RESOLVED (proxy assigns host-order, cites capture_1 222/656/1062). Z-8 `[~]` 0x000B OBJECT_TO_OBJECT_EFFECT: the two server emitters disagree above bit 0x04 -- `Object::SendObjectToObjectEffectRL` (ObjectClass.cpp ~850-928) is the authoritative capture-validated layout, the single-player `Player::SendObjectToObjectEffect` (PlayerConnection.cpp:1394) is wrong above 0x04 by its own admission; LATENT because every live caller sets only bits 0x01/0x02/0x04 where they agree, so the shipped proxy 0x2012->0x000B beam + the CLI decoder are correct. Server alignment of the twin deferred (no live caller exercises the wrong region; high-risk for nil gain). Surfaced by Phase AB §5. | 2026-06-03 | 2026-06-03 |

| AA | Proxy translation fidelity & single-source consolidation | [27-phase-aa-proxy-fidelity.md](27-phase-aa-proxy-fidelity.md) | in progress -- 2026-06-03: **Wave 1 landed.** Established the proxy is ONE cross-platform source tree, two build targets (Linux-native docker + Win32-PE under WINE = how players run it), NOT two implementations -- deleted 5 dead `NET7_LEGACY_WIN32`-walled twin files (`UDPProxyToClient.cpp`/`UDPProxyToGlobal.cpp`/`UDPProxyMVAS.cpp`/`ClientToSectorServer.cpp`/`ClientToGlobalServer.cpp`; the macro is never defined in any build, so they were empty TUs); both build targets re-verified clean after deletion. S->C opcode parity Tier-1: 0x1b AUX_DATA undersize-drop guard (len<8), forward-gate tighten 1..0x0FFE -> 1..0x00FE (highest game opcode is 0x00DD, nothing in 0xFF..0xFFE -- pure fidelity tighten), consume the 0x2025..0x202e gate-cache band (was stalling the sequence). C->S audited: matches reference except 0x4e starbase-exit handoff (`[!]` deferred -- needs launcher handoff). Tier-2 deferred (forward-unmodified is the safe direction): 0x09/0x0b conditional drop, 0x34 gate-cache timestamp inject, gate-cache subsystem. C# .NET 10 proxy rewrite recorded as a FUTURE idea (owner-confirmed not-now: C++ proxy already builds Win32-PE for WINE, rewrite buys players nothing, high-risk to replace working+tested code). Docs: CLAUDE.md "proxy is NOT a dumb relay" section + docs/02 §1 + docs/03 §1 + README pcap-replay warning -- captures are NOT directly client-applicable. Sourcing kept neutral (clean-room stream observation + committed pcaps). **Wave 3 (2026-06-03):** found Wave 2's "S->C consume set matches the reference" claim was WRONG for the fabrication band -- the reference EXPANDS the compact control opcodes the server emits (0x2012/0x2013/0x2014, latent 0x2018/0x2019) into full client packets and we were drop-consuming them = active regression (no beam/tractor/object-spawn visible). Corrected the misleading code+plan comments, reconstructed the fabrication spec in plans/27 §3a, and shipped the first expander: **0x2012 -> 0x000B prospect/mining beam** (commit da906490), grounded entirely in citable in-repo server code (Player::SendObjectToObjectEffect + ActivateProspectBeam), Bitmask 0x0007 + client-self-expiring Duration. The REMAINING fabrication (0x2013/0x2014/0x2018/0x2019) has un-citable fields that crash the real client if wrong, so its verification harness + implementation move to **Phase AB** (the C# CLI byte-pin suite must precede it -- see plans/28 §4). Follow-up fidelity fix (commit dae171ea): the shipped beam's Duration was uncapped (`drain_ms & 0xFFFF`); the client reads it signed so a long mine (>32.7s) wrapped negative and the beam vanished -- now capped at 32000 to match the authoritative range-list emitter (ObjectClass.cpp:884-885). **Wave 4 (2026-06-03): two in-sector regressions fixed from the live `net7-live-2026-06-02-login-undock-clicknavs-warp-logout.pcap` capture.** (a) **Empty Luna** -- the capture shows the server emitting 51x `0x2018` STATIC_OBJECT_CREATE (navs/stations/gates/decos) + 34x `0x2019` RESOURCE_OBJECT_CREATE (asteroids) in the Luna session; our proxy was drop-consuming both, so the sector rendered empty. Proxy now EXPANDS them (`UDPProxyToClient_linux.cpp` `CreateObject`/`CreateResource`): 0x2018 -> 0x04 CREATE + 0x89 RELATIONSHIP(only the ObjectID is BE/ntohl) + 0x40 CONSTANT_POS + 0x1b AUX-name + 0x99 NAVIGATION; 0x2019 -> 0x04 CREATE(Type=38) + 0x40 position + 0x1b resource-name. Byte-pinned by `StaticContentFabricationSpecTests` (CLI). (b) **Ships vanish after idle** -- root-caused to the server's 2-minute idle reaper (`PlayerManager.cpp` `SendUDPOpcodes`/`SendUDPPlayerOpcodes`: `LastAccessTime + 2*60000 < now` -> `DropPlayerFromGalaxy`); live server log proved it (clienta reaped at 2m11s, no refresh). The upstream Win32 `UDPProxyMVAS.cpp` keepalive was never ported (absent from repo; `MVASThread` defined nowhere), so neither proxy build sent any 0x3005/0x1004 to MVAS port 3806. Fix: `UDPClient::MVASKeepaliveThread`/`SendServerKeepalive` (`UDPClient_linux.cpp`, both build targets), started once per session in `FixedClientComm()`, sends a bare 12-byte 0x3005 PLAYER_COMMS_ALIVE carrying the live GameID every 30s to the server's MVAS port 3806 (capture, re-parsed: the real proxy runs TWO synced ~30s 0x3005 streams -- a bare keepalive to udp/3806, frame `0c 00 05 30 2a 99 03 40 01 00 00 00`, plus a 0x3005 echo to the live sector port; we replicate the 3806 stream only, the server's authoritative refresh path). **Port correction (same wave):** first cut used `UDP_Send(0,...)` = `send()` to the connected peer, but this UDPClient is connect()'d to `UDP_MASTER_SERVER_PORT`/3808 (no comms-alive handler) so it refreshed nothing; fixed to `UDP_Send(MVAS_LOGIN_PORT,...)` = explicit `sendto(server:3806)`. Runtime-proved: raw 0x3005 w/ bogus id 0xDEADBEEF to live server:3806 returned `0x100A` echoing the id, confirming 3806 dispatches 0x3005 -> `HandleKeepCommsAlive`. play-cli covered by the CLI's own direct 0x3005 (`StartCommsAlive`). CLI byte-pin: `MvasClientTests.BuildCommsAlive_MatchesRetailCaptureFrame`. Real-client confirmation tracked in plans/29 CV-01/CV-02/CV-04 (async, non-blocking). | 2026-06-03 | 2026-06-03 |
| AB | Port packet-structure knowledge into the C# CLI (+ keep server/proxy/CLI in sync) | [28-phase-ab-cli-packet-structures.md](28-phase-ab-cli-packet-structures.md) | in progress -- 2026-06-03: §1/§2/§5 + §3-spec DONE; §3-live `[!]` blocked behind Phase K. **Honest scope correction (plans/28 §0):** NOT a from-scratch port -- the CLI already field-decodes ~95 opcodes via `PacketRecordRegistry`. Landed: **§1** fabrication-band ratchet `FabricationBandCoverageTests` (11 opcodes pinned to exact record types, commit 0e2c16cd); **§2** the two named-but-unparsed compact decoders 0x2012 START_PROSPECT + 0x2013 TRACTOR_ORE + `CompactMineRecordTests` byte-pin (commit 3fcb7973); **§5** dual-0x000B-emitter drift resolved as latent (Z-8), PacketStructures.h struct comment reconciled to the authoritative RL layout (commit dbe93970), and the proxy beam Duration capped at 32000 to match `Object::SendObjectToObjectEffectRL` (client reads Duration signed; >32767 wraps negative/invisible; commit dae171ea); **§3-spec** `ProspectBeamFabricationSpecTests` pins the 0x2012->0x000B fabrication contract incl. the cap (commit 9276d632). 621/621 unit tests green. **§3-live BLOCKED `[!]`:** the live mining round-trip needs an in-space start; fresh chars start docked at a starbase, but the dock->space undock transition is fixed and works (owner-confirmed 2026-06-04, tasks #64/#65), so an in-space session IS reachable -- what is missing is the proxy-routed in-space mining harness (not yet built) to drive + byte-pin 0x000B against the real client. **§4 gate stands:** the remaining Phase-AA fabrication (0x2013/0x2014 tractor/loot, 0x2018/0x2019 spawn) has un-citable fields that could break the Win32 client if guessed wrong, so it stays blocked on §3-live until those fields are captured/observed -- a CV-gate, not a client crash. The C# spec alone is NOT sufficient to unblock it. | 2026-06-03 | 2026-06-03 |
| AE | CLI full-field dump + REPL semantic event narration | [32-phase-ae-cli-dump-and-narration.md](32-phase-ae-cli-dump-and-narration.md) | complete -- 2026-06-04: PURE CLI TOOLING (no server/proxy/login changes -- fully unblocked, no Phase K dependency). Two features: **(1) REPL event narration** -- inbound frames now emit one-line human notices, e.g. `* Mob (L17, hostile, Mbonae Whatever) appeared in scanner range, d=50`. **AE-W1 DONE:** `SectorWorld.Ingest` returns `WorldEvent`s (Appeared on an entity first getting a name via 0x0004+0x001B/0x0061, announce-once latch; Departed on 0x0007 REMOVE of an announced entity); new `EventNarrator` formats them via `Describe`/`ReactionName`/`TypeName`; wired into `OnPacketReceived` + `ReplayInboundFrame` (so the UDP move/in-space path is covered too) behind `NarrateEnabled` (default on). **AE-W2 DONE:** opcode-derived notices -- damage-to-self (0x0064), warp progressed/ended (0x009C; "ended" not "interrupted" since the server sends -1 for both arrival and boundary-interrupt), loading-sector (0x003A handoff), system message (0x001D, covers leave-group "You have left the group"). Group-invite was already narrated (`AnnounceGroupInvite`). **AE-W3 DONE:** `narrate-on`/`narrate-off` REPL commands. 16 new `EventNarratorTests` + full 701-test unit suite green; App + Core build clean. **AE-W4 DONE:** full-field dump audit -- the emit-diff is EMPTY (all 79 distinct `SendOpcode(ENB_OPCODE_00xx)` literal sites in server+login map to one of the 109 registered records) and NO concrete record uses the legacy non-marking `Field*` emitters, so every frame the server actually sends already parses all its fields; remaining GenericRecord fallbacks are exactly the outbound-only request builders + the 6 server-never-emitted KnownUnimplemented opcodes, intentionally left record-less to avoid dead code (plan 31/AD Q2). No speculative records added. | 2026-06-04 | 2026-06-04 |
| AD | Protocol parity scorecard (opcode mapping / CLI decode / proxy fidelity) | [31-phase-ad-protocol-parity-scorecard.md](31-phase-ad-protocol-parity-scorecard.md) | in progress -- 2026-06-04: consolidated "are we at parity?" surface, created in answer to "100% mapped / CLI decodes all / proxy 100% parity?". **Honest verdict:** (Q1) opcode mapping/naming = 100% (210 header defines, 211 CLI names). (Q2) CLI decode = never throws (NamedOpaqueCodec fallback); 107/145 client-wire opcodes structured-decoded + 7 dedicated codecs; the 38 "opaque-only" are ~26 OUTBOUND request opcodes the CLI emits (not receives), 6 server-never-emitted (KnownUnimplemented), and a marginal inbound tail (`GLOBAL_ERROR 0x75`) -- effectively complete, NOT a real gap. (Q3) proxy fidelity = NOT 100%: Tier-1 S->C/C->S + fabrication 0x2012 beam / 0x2018 / 0x2019 / MVAS keepalive DONE; **0x2013 tractor + 0x2014 loot fabrication BLOCKED**, Tier-2 gate-cache + 0x4e handoff deferred. **Keystone:** the blocked fabrication has un-citable wire fields that could break the real client if guessed wrong, so it CANNOT ship on the C# spec alone (CV-gate) -- it needs a capture or first-hand layout first. This is NOT a client crash: the dock->space undock path is fixed and works (owner-confirmed 2026-06-04, tasks #64/#65), so an in-space session is reachable. "Continue" resolves to AD-66 = build the proxy-routed in-space mining harness to pin 0x000B live, which then unblocks AD-2/AD-3 once their fields are captured; AD-1 (structured-decode 0x75) is the only unblocked side-task. **AD-66 DONE (2026-06-04, commit 4a7712c0):** `SectorMiningTests` drives the full retail prospect/mine trigger end-to-end (Progen Explorer + seeded SKILL_PROSPECT, undock to sector 1030, MVAS-feed onto Asteroid Field 6, lock roid, MineResource) and byte-pins the proxy-fabricated 23-byte 0x000B beam; +`SectorWorld` 0x2019 ingest +2 unit tests; NON-flaky (tries every in-range roid; depleted ore stack surfaces its 0x0022 PUSH_MESSAGE). NO server change. **Remaining (AD-2/AD-3 = task #49) is genuinely BLOCKED on a capture I cannot produce:** the loot/tractor OUTPUT chain needs 0x04 CREATE (article wire Type byte -- Type=38 right for ore, UNKNOWN/crash-risky for a looted weapon via 0x2014) + 0x46 positional-interp (field order is a guess); those bytes ride the ENCRYPTED client<->proxy leg that no AF-5 cleartext capture covers, and a wrong 0x04 CREATE crashes the Win32 client (CLAUDE.md Trap-1). Unblock needs a decrypted client<->proxy tractor/loot capture OR owner real-client iteration. AD-4/AD-5 explicitly deferred (do not implement on spec). The safely-implementable parity work is therefore COMPLETE. | 2026-06-04 | 2026-06-04 |
| AC | Editor-tool usability: Postgres backend, zero-friction login, change-tracking, never-hang UI, Item Editor port | [30-phase-ac-editor-tools.md](30-phase-ac-editor-tools.md) | complete -- 2026-06-03: all six sub-tasks landed. **AC.1** common DB layer MySQL->Npgsql on the `net7` content DB. **AC.2** zero-friction `just` login + enlarged resizable window. **AC.3** DB-write-boundary ChangeTracker -> re-appliable `.sql` changeset, inherited by all editors. **AC.4** every editor's DB I/O now off the UI thread (item/mob/faction + sector & effect 15 sites each) so the X button always closes; `Timeout=5`/`Command Timeout=30` second-line guard (commit 12cfff6b). **AC.5** Item Editor ported to `tools/item-editor-avalonia` on the AC.1-AC.4 base, launcher flag flipped (commit 2d5e6374). **AC.6** the real bulk: the AC.1 migration had silently broken EVERY per-editor write path (MySQL-only `?word`/`INSERT..SET`/`LAST_INSERT_ID()`/`information_schema.Auto_increment`); audited+fixed all four editors, quoted mixed-case/reserved identifiers, fixed the `EquipEffect` bytea-vs-int landmine (string literal `'1'`, no schema retype), and found a HIDDEN PRODUCTION BUG only by running live -- every IDENTITY sequence stranded at `last_value=1` (bulk loads never advanced them) so every `INSERT..RETURNING` would have collided; fixed with `db/postgres/sync_sequences.sql` wired into schema-init. Live-verified all write paths + the changeset re-apply loop against `net7` (commit 7fd01446). TOOLING-ONLY: no server/proxy/login-server changes. Remaining: a manual GUI pass by the owner (DB-down X-close + live-server-reflects-edit), non-blocking. Original planning note: The Avalonia editor suite is near-unusable against the live stack: its `commontools-avalonia` DB layer is MySQL (`MySql.Data.MySqlClient`, `Login.axaml.cs` -> host `net-7.org`:3307 = the `mysql-legacy` container) but the server has read **Postgres** since Phase N, so editor edits land in a DB nothing runtime reads. Six tasks: **AC.1** migrate the common DB layer MySQL->Npgsql targeting the `net7` content DB (mind the two-DB topology + Postgres mixed-case identifier folding); **AC.2** zero-friction login when launched via `just` (auto-populate/skip the dialog from the local stack; enlarge the cramped ~290x195 window + make it resizable); **AC.3** change-tracking that records mutated records and generates a re-appliable `.sql` changeset (mechanism in `commontools-avalonia` so ALL editors inherit it; start with Sector Editor; lean toward capturing at the DB-write boundary in `DB.cs`); **AC.4** the window X button must ALWAYS close (root-cause the `Error within DB.executeQuery()` hang -- sync DB I/O on the UI thread blocking on the unreachable MySQL host -- move DB I/O off the UI thread, surface errors as dismissable, never wedge); **AC.5** port the lone un-ported editor `tools/itemeditor` -> `tools/itemeditor-avalonia` ONTO the AC.1-AC.4 base, flip the launcher `Ported` flag; **AC.6** verify across the suite + re-apply a changeset to close the "edits a dead DB" loop. TOOLING-ONLY: does NOT touch server/proxy/login-server; editors adapt to the DB the server uses, not the reverse. | 2026-06-03 | 2026-06-03 |
| AH | DTLS + per-packet auth for the proxy<->server UDP leg | [35-phase-ah-dtls-proxy-server.md](35-phase-ah-dtls-proxy-server.md) | **OWNER-AUTHORIZED 2026-06-07, in progress.** Two-layer design: (1) DTLS both directions (OpenSSL `DTLS_method()`; cert strategy DECIDED -- reuse the existing Let's Encrypt PKI, connect-by-IP/verify-by-NAME via `SSL_set1_host`, NO self-signed, NO SPKI pin; per-peer in-memory BIOs keyed by `(ip,port)` in the shared `net7::DtlsTransport`; asymmetric handshake -> symmetric AEAD) for confidentiality + channel integrity; (2) per-packet C->S auth token (sent only INSIDE the established DTLS channel) to bind each datagram to an account -- DTLS authenticates the channel, not the account (same proxy binary ships to everyone). DTLS is default-ON / fail-closed; the LOUD explicit opt-out `NET7_DTLS_ALLOW_PLAINTEXT=i-accept-unencrypted-udp` is wired into the dev/test compose only. Threat model is NOT thin: the Linux port dropped the Win32 ticket handoff, so the game server never validates the ticket suffix (impersonation hole) -- see plans/35 + the Phase AJ audit. **AH-0..AH-4 DONE** (CSPRNG ticket both generators; server + proxy DTLS wired, fail-closed, validated end-to-end at idle; opt-out wired into dev/test compose). **AH-9 keystone DONE** (the always-on DB ticket-suffix-validation half, closing the AJ-1 impersonation hole, task #89): shared `net7_user.login_ticket` table, both issuers UPSERT, `ProcessTicketInfo` -> `ValidateTicketSuffix` parameterized SELECT + constant-time `sodium_memcmp` + expiry, reject G_ERROR_TICKET_INVALID; accept + forged-reject tests green; real-client check CV-14. **AH-8/AH-9-token/AH-10 DONE** (commit 265ec481, branch phase-ah-dtls-auth-wrapper, NOT pushed): DTLS-gated 17-byte per-packet C->S auth wrapper -- proxy prepends inside DTLS (UDPClient_linux.cpp), server strips+validates at the recv edge via shared `net7::CheckAuthWrapper` (AuthWrapper.h) against the token bound to the Player at char-select (GetLoginTokenBinary->SetAuthToken), constant-time compare, rate-limited drop log that never logs the token; inner bytes byte-identical so plaintext path + CLI/integration suite unchanged; also fixed the first-dash username misparse (strtok->strrchr) in server ProcessTicketInfo + proxy HandleGlobalConnect. CLI byte-pin AuthWrappedPacket.cs (6 tests) + auth_wrapper_test.cpp (9 tests, incl. forged-token-rejected-and-logged) green; clean-DB integration 131 pass / 2 pre-existing wedge fails (proven not introduced via stash-baseline) / 7 skip; gtest 16/16. Real-client check CV-17. AH-5 (capture-workflow preserve) + AH-6 (firewall/key-perms note) still remaining. | 2026-06-07 | -- |
| AJ | Server-side auth & input-validation hardening (security audit) | [37-phase-aj-security-hardening.md](37-phase-aj-security-hardening.md) | **in progress 2026-06-07.** Aggressive security audit of the server-native code surfaced by the DTLS work. AJ-0a/AJ-0b DONE: replaced both live `rand()` ticket generators (login-server `LinuxAuth.cpp` + game server `AccountManager.cpp`) with libsodium kernel-CSPRNG draws. **AJ-1 DONE** (impersonation hole closed via AH-9 ticket-suffix validation, commit 2c5d9876, CV-14). **AJ-2 DONE** (`HandleSkillAction` OOB index bounds-checked + CLI OOB-rejection test). **AJ-6 DONE** (ticket expiry now enforced in `ValidateTicketSuffix`, folded into AH-9). **AJ-4 DONE** (chat sender spoofing: `HandleClientChat` now passes the authenticated `GameID()` to Guild/Local/Broadcast chat instead of the wire `chat->GameID`; two-player spoof-defeat test landed `[Fact(Skip)]` blocked by proxy single-tenancy; CV-15). **AJ-3 DONE** (inventory-slot OOB: `HandleInventoryMove` 0x0027 now bounds-checks FromSlot/ToSlot against real per-container slot counts via `InventorySlotCount` -- Cargo 40/Equip-Ammo 20/Vault 96/Vendor 128/Trade 6; OOB-rejection test + 5 inventory tests green; CV-16). Reported/needs-verify: AJ-5 create/delete no-auth, plus MEDIUM/LOW (account-in-use race, banned-IP TODO, starbase loc). Each fix is a server **tightening** (rejects input the real server/client never produced) gated on primary-source citation + CLI parse/test first + plans/29 CV entry. **Phase AK** (in the same file) is the adversarial final audit, done LAST after AH + AJ complete. | 2026-06-07 | -- |
| AL | WASM-in-browser proxy + REPL (investigation only) | [38-phase-al-wasm-browser-investigation.md](38-phase-al-wasm-browser-investigation.md) | **investigation complete 2026-06-07. Verdict: NOT feasible as asked; no code.** Q1 (proxy in WASM making real UDP to the sector servers): impossible -- a browser has no raw UDP; its only network primitives are fetch/WebSocket (TCP), WebRTC DataChannel (P2P SCTP/DTLS), and WebTransport (QUIC), none of which emit a raw datagram to an arbitrary host:port. Emscripten "sockets" tunnel over a WebSocket relay, not real UDP. So any browser proxy needs a server-side relay -> UDP, and the AH DTLS + per-packet auth token make that relay a trust boundary (dumb-L4 breaks the (ip,port) peer map; terminating-relay is a privileged MITM), not a dumb bridge. Q2 (REPL on ConsoleEx->WASM): moot, and independently dead -- SharpConsoleUI (nickprotop/ConsoleEx) "targets real terminals only, not Blazor/WASM" (NetConsoleDriver / raw libc / Win Console API / embedded PTY). Feasible alternatives if an in-browser client is wanted later, both keep the proxy NATIVE server-side: (A) Blazor-WASM REPL + xterm.js over a WebSocket relay; (B) web-PTY (ttyd/wetty) to a server-side CLI container. Neither is "proxy compiled to WASM." | 2026-06-07 | 2026-06-07 |
| AI | On-demand sector lifecycle (lazy start + idle teardown) | [36-phase-ai-sector-lifecycle.md](36-phase-ai-sector-lifecycle.md) | Stage 1 + Stage 2 DONE + PUSHED (2026-06-06, commit a009f1a8). Stage 1 (lazy start): nav skeleton loaded galaxy-wide at boot; MOBs/fields/resources + UDP bind + thread deferred to first handoff (EnsureSectorStarted). Stage 2 (idle teardown): m_SectorIdleTeardownMs poll PARKS empty sectors -- family-scoped on the SHARED station/space obj_manager (a docked player counts toward the station mgr, so teardown collects the whole family, quiesces every member's event+recv thread, then frees deferred objects once); manager stays mapped, restart-in-place on next entry. Thresholds env-configurable (NET7_SECTOR_IDLE_POLL_MS / _TEARDOWN_MS). Two adversarial audit rounds cleared CRITICAL UAFs + a HIGH purge-abort double-load. AI-11 verified live: park freed 111 objects in sector 1015, re-entry restarted cleanly, no crash. Pre-phase wins: MALLOC_ARENA_MAX=2 (975->910 MiB) + lazy temp pools (->861 MiB). CV-10/CV-11 await real-client confirm. | 2026-06-06 | -- |
| AF | Live reference capture corpus (parse + byte-pin the live Net-7 server) | [33-phase-af-live-reference-corpus.md](33-phase-af-live-reference-corpus.md) | done -- 2026-06-04: the owner captured 6 traces against the **live Net-7 production server (the live reference server)** and directed we treat it as a reference implementation to copy (= canonical primary source per CLAUDE.md). Parsed all 6 on the cleartext proxy<->server UDP leg via a scratch dumper (`proxy/local-debug/opdump/`) that reuses the production `SectorStreamReassembler` so offsets match the CLI. **Banked a full per-opcode decode table** (fabrication / combat / vendor / gate-jump bands -- plans/33). **AF-5 DONE: pinned the fabrication band against live bytes** -- 5 real-capture fixtures (`live_start_prospect_2012.hex`, two 0x2013 tractor + two 0x2014 loot, ProspectRun #190/#197/#251 + KillLoot2 #91/#99) + `LiveReferenceFabricationTests` (5/5 green): the existing CLI records (`StartProspectRecord`/`TractorOreRecord`/`LootItemRecord`) decode every byte of the live frames with no gap. **AF-4 finding:** the 0x2012 id fields are AsteroidGID@4 (obj->GameID, STATIC node, low 0x000188xx band) + EffectUID@8 (GetSectorNextObjID, DYNAMIC 0x0025xxxx); the records were already correct (server PlayerSkills.cpp:691-695), only a scratch note had the two swapped. **AF-6:** fixed a pre-existing integration build break -- `SectorMvasMoveTests.cs` passed `onInbound: world.Ingest` as a method group, which stopped compiling when 5c991f84 changed `SectorWorld.Ingest` to return events; wrapped in a discarding lambda. **Unblocks the field-layout half of AD-2/AD-3 + AB §4** (0x2013 tractor / 0x2014 loot fabrication fields are now captured/proven; only the real-client render confirmation remains, still a plans/29 CV-gate). **AF-7..AF-10 DONE.** AF-8: 0x0064 @16/@20 are BOTH entity GameIDs (player id flips by damage direction) -- the "@20 effectDesc" suspicion was wrong; pinned both directions. AF-9 vendor (0x0054/0x0056/0x006A) + AF-10 gate-jump (0x003A/0x0034/0x0097/0x009C) pinned clean (`LiveReferenceDialogGateTests`); Z-4 data point: live 0x0034 = +0 tick, agreeing with our server. AF-7 combat: 0x000B (35B string form) + 0x000E pinned clean, and **found+fixed a real Trap-1 defect** -- 0x008B ATTACKER_UPDATES mob id is BIG-ENDIAN on the live wire but record+server both used LE (server handed the client a byte-reversed attacker id). Fixed CLI (`ReadI32BE`) + server (`htonl(mob_id)`, commit d58ac8ea), pinned both start/stop frames; plans/29 CV-08 for real-client check; docker server build green. This also satisfies the #66 live 0x000B beam pin. NO `.pcapng` is ever committed (gitignored, contains a login leg); committed artifact is the decoded-hex frames. | 2026-06-04 | 2026-06-04 |
| AM | External status events (server -> chat/dashboard relay) | [39-phase-am-external-status-events.md](39-phase-am-external-status-events.md) | **built 2026-06-07; AM-0..AM-5 + AM-7 done, AM-6 partial.** Emits 5 server events as fully-rendered lines: login (`level: Cx/Ty/Ez, class: TE` + `(N online)`), logout (+ `(N online)`), level-up {Combat/Trade/Explore}, broadcast `[player] message`, server started. **Naming is consumer-agnostic** (owner directive 2026-06-07 -- Discord is just the first consumer): func `EmitExternalStatusEvent`, table `external_status_events`, NOTIFY `external_status_event`, flag `NET7_EXTERNAL_STATUS_ENABLED`, sidecar `freya/status-notifier/` + `DISCORD_BOT_TOKEN`/`STATUS_CHANNEL_ID`. **Architecture:** server rides the existing single-threaded `SaveManager` queue -> parameterized `INSERT external_status_events` + `NOTIFY` on the net7_user connection it already holds (NO new outbound socket / HTTP client in the server) <- Go sidecar (`freya/status-notifier/`, pgx/v5) `LISTEN`s + drains + posts each line into a Discord channel via the bot. Durable outbox (sent_at=NULL until 2xx), startup catch-up drain, 429-aware (discordgo limiter), default-OFF both ends. **AM-9 (built 2026-06-08):** admin `/notify` slash command (Manage-Server-gated) toggles which kinds the sidecar relays at runtime via the `status_notification_settings` table -- a sidecar-side delivery filter the game server NEVER reads (no inbound control path). AM-9g removed the webhook transport entirely: the bot is now the sole Discord touchpoint (relay + `/status` + `/notify`). Server change is **emit-only** -- alters no EnB client wire byte, so the wire-fidelity gate (CLI byte-pin + plans/29 CV) does NOT apply; does not weaken security. Prod pipeline updated: `enb:status-notifier-<v>` built/pushed (Build-And-Push.ps1) + threaded into the droplet `.env` (Update-Stack.ps1). Docs: `docs/18-external-status-events.md`. **AM-6 partial:** sidecar end-to-end + live server `server_start` emit proven; login/logout/levelup/broadcast NOT yet driven by a real client (shared-env risk) -> **CV-AM-1** in plans/29. **AM-8 (built 2026-06-07):** read-only `/status` Discord bot (`freya/status-notifier/bot.go`, discordgo) + emit-only `server_status` heartbeat (singleton row UPSERTed every ~30s by `SaveManager::WriteServerStatusHeartbeat`, same flag). `/status` shows uptime, in-memory player + warm-sector counts, and a per-player table (name, class name+code via race/`prof`, floored C/E/T levels, sector name from net7). Bot is OUTBOUND-only (Discord gateway, no inbound port) and pure `SELECT` -- no path into the game server. Heartbeat + bot SQL verified live; bot's Discord round-trip needs an owner bot token -> **CV-AM-2** in plans/29. | 2026-06-07 | 2026-06-07 |
| AN | FreyaLauncher auto-update (patcher) + login `/updateCheck` + S3/CloudFront delivery | [40-phase-an-launcher-autoupdate.md](40-phase-an-launcher-autoupdate.md) | **C1-C5 CODE COMPLETE 2026-06-07; deploy owner-gated, NOTHING deployed (no terraform apply, no S3/CF/ACM/WAF created, no artifacts pushed).** C1 launcher updater (`tools/LaunchFreya/Update/*`, 34 unit tests incl. the path-traversal guard) + C2 login `/updateCheck` + startup `manifest.json` hash cache (`login-server/Net7SSL/PatcherManifest.{h,cpp}` via libcurl; route lives in `LinuxAuth.cpp`, NOT `SSL_Connection.cpp` which is WIN32-dead on Linux) are built and verified end-to-end against a local `file://` manifest stub -- UP_TO_DATE / conditional UPDATE_NEEDED (launcher exe+cfg vs proxy-only) / case-insensitive hash match / cache-miss=503-DOWN all confirmed; login docker image builds clean. C3 opt-in delivery infra (terraform `patcher.tf` + `providers.tf` us_east_1 alias; env threaded operator `.env` -> `Update-Stack.ps1` -> droplet `.env` -> `docker-compose.prod.yml` login service; blank `ENB_PATCHER_PRIVATE_S3_BUCKET` = inert, login fail-closed-DOWN). C4 push pipeline (`Push-ClientPatch.ps1` + `just push-client` + `package-client-windows` writes manifest.json/zip; explicit `-p:CheckForUpdates=true`; FLAT bucket, binaries-first/manifest-last, CF `/*` invalidation; `just push` then `just update`). C5 retargeted to the **Windows** zip + self-update (owner 2026-06-07: NO installer program -- the `package-client-windows` zip IS the Windows install; Linux installer untouched, it keeps the upstream WINE patcher which double-duties as game-data delivery Freya never replaces). Self-updater for the Windows FreyaLauncher build: on launch (before Play enables) it SHA-512s its own `FreyaLauncher.exe` + `FreyaProxy.exe` and POSTs both to a new login-server HTTPS `/updateCheck` (sibling of `/AuthLogin` in `SSL_Connection.cpp`); UP_TO_DATE -> status `Online`; UPDATE_NEEDED -> prompt, download from CloudFront to `updates/`, verify hashes, atomically self-replace (Windows rename-self + relaunch), auto-restart. **Only TWO hashes are checked** -- `FreyaLauncher.cfg` has no own hash check, it rides with the launcher whenever the launcher EXE mismatches (owner 2026-06-07); the server builds the `files` list conditionally per mismatch. Local `just play-local` build compiles the updater OUT (`checkForUpdates=false`). Login server caches the authoritative hashes from a private S3 bucket via a credential-free `manifest.json` GET over CloudFront **at startup only** (no TTL); `just update` restarts login to pick up new hashes, so the operator runs `just push` then `just update`. Cache-not-yet-populated -> server reports DOWN (fail-closed). Infra (owner-gated): private S3 + CloudFront-via-OAC serving it + ACM cert in us-east-1 + one WAFv2 per-IP rate rule (~20 req/5min, block; relies on CF caching for cost) + Route53 `dl.<domain>`, all folded into the idempotent `deploy/do` `just up` terraform converge; `just push` builds the Win artifacts, overwrites S3, invalidates CF `/*`. linux-installer switches off the upstream Net-7 patcher to FreyaLauncher. `/updateCheck` is a new HTTPS surface, NOT an EnB game-protocol change -- wire-fidelity gate N/A; gets its own security review + CV-AN-1..4. **All §9 open questions resolved 2026-06-07** (Q1 manifest-GET, Q2 reload-on-update, Q3 cfg-bound-to-launcher, Q5 cache-miss=DOWN, Q6 per-IP rate rule, Q7 `just up`; Q4 rename-self self-replace = recommendation, no objection). | 2026-06-07 | -- |
| AO | `/capitalize` chat command (class-suffix case toggle) | [42-phase-ao-capitalize-command.md](42-phase-ao-capitalize-command.md) | **built 2026-06-08.** New non-retail player slash command: toggles the case of the trailing 2-letter class suffix (TE TS TT JE JS JD PW PS PP) on the CALLER'S OWN avatar name; both-upper -> lower, else -> upper. Operates only on the caller's own `m_Database` (no other-avatar access, no new auth surface), persists through the existing server-authoritative full-DB save (`Player::SaveDatabase()` -> `SAVE_CODE_DATABASE` -> `HandleDatabase`) -- the same path the server already owns; the client never sends a full-DB blob, so no clobber. Live updates `SetName` + ship name/owner; the client's rendered nameplate refreshes on relog (it caches the login name). Server-emit-only behaviour, alters NO retail EnB client wire byte (it is a server-internal name edit + an existing save opcode), so the wire-fidelity gate (CLI byte-pin + plans/29 CV) does not strictly apply; CV-CAP-1 tracks the cosmetic real-client nameplate check anyway. `server/src/PlayerConnection.cpp` case 'c'. | 2026-06-08 | 2026-06-08 |
| AP | Rolling Postgres backups to private S3 (sidecar) | [43-phase-ap-pg-backup-s3.md](43-phase-ap-pg-backup-s3.md) | **built 2026-06-08, code-only, NOTHING deployed.** AP-1..AP-6 done. `db-backup/` sidecar (`postgres:16-alpine` + aws-cli + bash): hourly `pg_dump -Fc` (zlib-compressed) of `net7` + `net7_user`, keep newest 24 hourly + promote every 6th hour into a six-hourly tier kept 28 (7d x 4), count-pruned, into a PRIVATE S3 bucket via a BUCKET-SCOPED IAM token. Default-OFF: empty `BACKUP_S3_BUCKET` -> the loop idles (no crash-loop, smoke-proven); `manage_db_backup=false` -> terraform creates zero resources. Wiring: `deploy/do/terraform/backup.tf` (+5 opt-in vars), `_Common.ps1`/`Update-Stack.ps1`/`Build-And-Push.ps1`, both compose files, `.env.example`, `db-backup/README.md`. Read-only DB consumer, no inbound port, no wire change -> CV gate N/A. `docker build` green. **AP-7 (owner deploy) left undone -- this phase deploys NOTHING** (no bucket, no token, no apply). | 2026-06-08 | 2026-06-08 |
| AQ | Freya Online -- Go login server + Auction House + Mailbox + React UI | [44-phase-aq-freya-online.md](44-phase-aq-freya-online.md) | **in progress 2026-06-09.** New self-contained Freya/MIT service under `freya/online/` (Go + React/TS SPA), no Net-7 compile dependency. **AQ-1 Go login server runtime-verified:** byte-exact legacy wire (`/AuthLogin` -> 13B `Valid=False\r\n` / 63B `Valid=TRUE\r\nTicket=<user>-<32hex>\r\n` + `login_ticket` UPSERT; `Server: AuthServer/2.5`), Argon2id PHC verify in pure Go (`x/crypto/argon2`), `/sectorserver.cgi` `/touchsession.jsp` `/certificate.html` `/updateCheck` `/who.cgi` + 404 fallback; two pgxpool pools (net7_user save-state + net7 content -- cross-DB single-conn reads would fail). **AQ-2 AH engine + AQ-3 Mailbox built** (PostListing/PlaceBid/Buyout/sweepExpiredAuctions; accountMail/MarkRead/LootAttachment/sweepExpiredMail; opaque low/med/high band NOT ordered by expiry; 10% deposit up front, 95% unsold return; scs/v2 session middleware; bootstrap JSON matches SPA types). **AQ-5 SPA** ported to React 18+TS+Vite, built dist served from the Go binary (spa.go + SPA fallback), runtime-verified GET /. Runs as container-root to read the 0600 TLS key (rootless Docker = unprivileged host user). **AQ-5 write-path wired + AQ-9 tests done (2026-06-09/10):** the SPA now drives the REAL API for every mutation (bid/buyout/list/mark-read/loot) and re-pulls `bootstrap` as source of truth (VITE_MOCK now opt-in, real by default); offline-guard IS enforced on write paths and surfaces as HTTP 409 (`TestIT_API_OfflineGuard409`); test suites green (Go: 20 integration gated on FREYA_TEST_DB + 8 unit; web: 18 vitest). **AQ-10 Account tab -- password reset (2026-06-09):** third logged-in-only tab; `POST /api/account/password` requires session acctID, re-verifies current pw, enforces min length 8, writes a libsodium-compatible Argon2id PHC (same store the C++ game login reads), rotates the session; runtime-verified (new bundle served, unauth -> 401). **AQ-7 cutover done in code (2026-06-10, UNCOMMITTED -- owner review):** the net7ssl-derivative game-auth was NOT folded into freya/online; instead it was extracted to a SEPARATE standalone binary `login-server/net7go/` (own go.mod, CC BY-NC-SA 3.0 -- it is a derivative/rewrite of Net7SSL, NOT MIT, hence it lives under login-server/ not freya/). net7go speaks PLAIN HTTP on :8085; `freya-online` terminates TLS on :443 and RAW-RELAYS the legacy auth URIs to it byte-for-byte (legacy.go/ticket.go deleted from freya/online, replaced by legacy_proxy.go; byte-exactness pinned by legacy_proxy_test.go). net7go owns the AF_UNIX server-liveness keepalive. The C++ `login` (net7ssl) is REMOVED from every deploy path -- docker-compose.yml, the justfile recipes (play-local/play-cli/run-stack/dev/rebuild/package/push), and deploy/do (prod compose + Build-And-Push + cloud-init cert hook now use net7go + freya-online). The C++ login SOURCE is retained (inherited reference); only its deployment is gone. net7go + freya-online both build (go test green; docker images build). Pending real-client confirm: **CV-NET7GO-1** (plans/29). **NOT done:** AQ-4 bots; AQ-6 in-game unread-mail notify (server-side, integrity-gated); commit (held for owner review); browser smoke-test against the live stack. NOTE for AR-1: game-auth Argon2 now runs in net7go (no rate limiter yet); website /api/login Argon2 runs in freya-online -- the flood-hardening must cover BOTH binaries now. **AQ-11 (2026-06-10, UNCOMMITTED):** Vault transfer SPA page + Send-mail compose, both SERIALIZABLE + offline-guarded + ownership-checked (acctID from cookie); AH-sell & mailbox-loot engines converted to the same serialTx. `VAULT_SLOT_COUNT=96` / `MAX_MAIL_ITEMS=6` consts client+server. C++ in-game vault moves made crash-atomic: `sql_query_c` gained begin/commit/rollback, new `SAVE_CODE_MOVE_INVENTORY` persists a move's two slot writes in ONE transaction (cargo<->vault + vault<->vault), closing a dupe/loss window between the two former autocommit writes. HONEST: the C++ exposure was crash-atomicity, NOT a concurrency race (single FIFO save thread, one client per char). NOT a wire change (no opcode/packet/DB-content changed) -> CV-AQ-VAULT-1. Tests: server builds 167/167 -Wall -Wextra; 4 new C++ tx gtests + 18 new Go integration tests (incl. concurrent same-slot dup-safety) + web 20 vitest all green. Committed 43e12965. **AQ-11b (2026-06-11):** closed the residual -- the vault->cargo auto-stack path (ToSlot==-1, N-slot CargoAddItem) is now atomic too via a variable-length N-record SAVE_CODE_MOVE_INVENTORY (cap SAVE_MOVE_MAX_RECORDS=15); CargoAddItem optionally collects touched slots, SaveInventoryMoveSlots commits them + the emptied vault source in one tx. 3 more C++ tx gtests green (11 total). **AQ-12 Profile tab (2026-06-11, UNCOMMITTED):** new first tab -- per-avatar character sheet (identity + stylized class via race*3+prof + overall level = combat+explore+trade + teal/blue/purple discipline bars + sector + starship card with hull/cargo/thrust/warp + fitted equipment hover-tooltips + learned skills as filled/empty level dots). Three split, ownership-scoped, indexed-only read APIs (`/api/avatar/{name}/profile|ship|skills` in store_avatar.go), each joining the two pools in Go (no cross-DB statement), all values bound; equipment excludes -1/-2 slot sentinels. 4 new Go integration tests + API auth/ownership test; gofmt/vet/`go test ./...` + web vitest 20/20 + build all green. Website-only read feature -- no wire change. Committed 3b56ca3c. **AQ-13 Galaxy tab (2026-06-11):** new DEFAULT/first tab -- live sector map. Every named sector (130) grouped by star system (25), linked by the gate graph, each lighting up + glowing by current player count, RELATIVE-max normalized (1-vs-2 == 50-vs-100). Two split read APIs (store_galaxy.go): `/api/galaxy` static topology (systems w/ normalized faction token from `systems.notes`, sectors, deduped/self-loop-free gate edges from `sector_objects.gate_to` w/ dock-gate interior->parent remap) cached 5min; `/api/galaxy/occupancy` live per-sector counts cached 15s. Live counts derived EXACTLY like the Discord status bot (account+avatar `last_login>last_logout`, sector = `avatar_info.sector`), docked players (sector = starbase interior id) remapped to parent via `starbases.sector_id` -- no C++/wire change, fully website-derivable. React Galaxy.tsx: deterministic radial cluster layout (no real coords exist -- galaxy_x/y all 0), SVG gate edges, faction-colored nodes whose radius+glow scale with relative occupancy, hover info panel, total-online header, faction legend; topology fetched once, occupancy polled 15s. 3 new Go integration tests + `TestIT_API_Galaxy` + `TestGalaxyNormalizeFaction` + 4 web api.test.ts cases; gofmt/vet/`go test ./...` + web vitest 23/23 + build all green. Website-only read feature -- no wire change. | 2026-06-09 | 2026-06-11 |
| AR | Freya Online auth/abuse hardening (login flood + minor findings) | [45-phase-ar-online-hardening.md](45-phase-ar-online-hardening.md) | **AR-1/AR-2/AR-5 landed 2026-06-14 (UNCOMMITTED).** Post-AQ security review of `freya/online/server/`. **AR-1 DONE on BOTH auth binaries** (the AQ-7 split put game-auth in net7go, website login in freya-online): a dependency-free per-IP token bucket (`ratelimit.go ipLimiter`, refill=elapsed*rate capped at burst, GC-evicts buckets idle>15m) + a global Argon2id concurrency semaphore (`argonGate`, N buffered slots, acquire tries non-blocking then waits 2s then sheds -- never queues unbounded) in front of `/AuthLogin` (net7go) and `/api/login`+`/api/account/password` (freya-online). `golang.org/x/time/rate` deliberately avoided (not in module cache; CI builds Go only via docker). net7go keys per-IP on a relay-injected, edge-OVERWRITTEN `X-Freya-Client-IP` (`legacy_proxy.go` sets it from the relay's own RemoteAddr, so a client header can't spoof it -- net7go only ever sees the relay as peer); throttle returns the BYTE-IDENTICAL `Valid=False` failed-login response (no new wire surface). freya-online keys on RemoteAddr, returns 429/503. All knobs env-tunable (`NET7GO_AUTH_RATE_PER_MIN`/`_BURST`/`NET7GO_ARGON_MAX_CONCURRENT`; `FREYA_LOGIN_RATE_PER_MIN`/`_BURST`/`FREYA_ARGON_MAX_CONCURRENT`), defaults 60/min burst 20, Argon N=4 (4*64MiB peak). `ratelimit_test.go` in both modules (burst-throttle, disabled-when-rate<=0, stale eviction, gate caps under flood, IP precedence/IPv6 port-strip) green. **AR-2 DONE**: checked multiply guards the int64 overflow in `vendor*int64(stack)` (`store_ah_write.go`), rejects the listing instead of wrapping. **AR-5 DONE**: prod compose sets `FREYA_HTTP_ADDR=""` (never published 8080); required adding `lookupOr()` first -- `env()` treated blank as "use default" so the empty value was unreachable (latent bug). **Deferred (by design):** AR-3 in-memory sessions (operational, fine as-is), AR-4 Vite dev-server advisory (no prod exposure; routine bump), AR-6 SSH accept-new (intentional cattle-droplet tradeoff). Real-client check: plans/29 CV-AR-1 (low risk -- wire-identical throttle). | 2026-06-14 | -- |
| AS | Client UI overhaul (enbmod Lua) -- replace native bottom-bar HUD | [49-phase-as-client-ui-overhaul.md](49-phase-as-client-ui-overhaul.md) | **code done 2026-06-12; AS-3 live values + real-client visual check pending owner.** Owner ask: remove the six circular hotkey buttons + the circular warp control; restack the green/red/blue stat bars vertically at equal length; new 12-button `1..9 0 - =` square shiny rounded action bar along the bottom; Lua APIs to disable native UI element classes + handle keybinds. DONE: AS-1 (`enb.screen`/`measure`/`on_input` swallow, fail-open), AS-2 (procedural `rect_grad`/`rrect`/`rrect_grad` + alpha), AS-4 (`freya_ui.lua` cover+swallow HUD + 12-button action bar), AS-5 (`xp_overlay.lua` reposition), AS-7 (clean -Wall -Wextra build + README). AS-3 PARTIAL: scaffolding (`autocalib.lua` find/probe/save, `calib_data.lua` gitignored + init.lua pcall-load) done; live offset values need owner in-game. AS-6 (Tier B true widget hide): mechanism shipped, OFF by default -- `enb.patch_ret(addr,pop)` overwrites a draw routine's entry with `ret`, with a commented init.lua HIDE block off the address book; correctness is real-client-only (CV-AS-HIDE-VITALS/-XP/-SKILL). **AS-9 (2026-06-12): ported the `Earth & Beyond HUD.html` glass design** -- `freya_hud.lua` shared toolkit (palette + `H.glass` gradient/gloss/hairline/corner-tick panel + outlined text), `freya_ui.lua` = PlayerCard (name+LV header, 3 vitals track+gradient fill+cur/max+pct) + 12-slot glass hotbar, `xp_overlay.lua` = bottom-left DiscCard (C/E/T letter + xp bar + LV badge). Visibility-gated on new `enb.state()` (space=cards+hotbar, station=cards only, login/charsel/load=nothing; CV-AS-STATE) and `enb.cursor()` draws our pointer on top (CV-AS-CURSOR). Specs rewritten (freya_ui 20, xp_overlay 8, init 4, screenshots 6 incl. station/login), all green; screenshots visually verified vs design. All in `freya/client-injection/enbmod/` (MIT); no binary assets -- procedural primitives. | 2026-06-12 | -- |
| AU | Client login automation skill (agent-driven EULA->login->charsel->ingame) | [51-phase-au-login-automation.md](51-phase-au-login-automation.md) | **complete 2026-06-18.** Born out of Phase AS: a wedged/relaunched client blocked all live HUD iteration until the owner re-logged in by hand. `.claude/skills/login-to-client` -- reliable, repeatable building-block scripts (kill -> start-stack -> seed/in-space -> autoplay -> EULA Accept -> login devuser/devpass -> first-char enter -> wait-ingame), each polling a real signal (window mapped / PIL+numpy region match / enbmod `enb.inspace()` heartbeat), no blind sleeps, screenshot-on-stuck. Input via xdotool XTEST (raise+abs-click+type); detection PIL+numpy (no OCR/cv2 on this box). Play became the in-app `FREYA_AUTOPLAY=1` hook (MainWindow.axaml.cs). Start-in-space (AU-12): 02-seed `ensure_slot0_in_space` divides slot-0 `avatar_info.sector` by 10 when docked (server reads it at AccountManager.cpp:694, docks when >9999 at PlayerManager.cpp:209); idempotent, opt-out `ENB_START_IN_SPACE=0`, NOT in the shared seed (would break undock/mining tests). Proven end-to-end: `INGAME inspace=true state=space`. | 2026-06-18 | 2026-06-18 |
| AT | Code quality -- formatting rules, linting, warning cleanup, god-class DI | [50-phase-at-code-quality.md](50-phase-at-code-quality.md) | **complete 2026-06-16.** CLI-enforced formatting across every language + warning cleanup + managed-code god-class DI splits. Owner scope: format everything EXCEPT `server/src`; C++ gets formatting+warnings only (god-class/DI deferred to the server rewrite, NO boost-ext/di); Go=gofmt tabs (4-space N/A); 4-space everywhere else; preserve CRLF + Latin-1 © headers (.editorconfig leaves EOL unpinned, clang-format `ReflowComments:false`). **AT-1** config/tooling (.editorconfig 4-space + StyleCop severities, `stylecop.json`, `.clang-format`, prettier tabWidth=4); **AT-2** apply via CLI (dotnet format / clang-format -i / prettier / gofmt); **AT-3** CI format-check gate + `just fmt`/`fmt-check`; **AT-4** proxy/launcher compiler warnings -> 0 (no behaviour change; live-integration-proven); **AT-5** C#-only god-class (>1200 LOC) audit -- 4 of 5 candidates false positives (xUnit fact-collections + the Vector3 math struct), only MainWindow.axaml.cs was real and its logic was already extracted, so pulled the last pure helpers (HostResolver, LauncherLogFiles) out under test (+28 tests); **AT-6** final audit -- 503 files (mostly pure whitespace), caught + fixed one mixed-line-ending defect (Vector3.cs UTF-16->UTF-8 left CRLF/LF mix), all CI green incl. live integration suite, no wire change so no CV entry. | 2026-06-16 | 2026-06-16 |

Decisions log: [99-decisions-log.md](99-decisions-log.md).

Status markers:
- `not started` — no work yet
- `in progress` — at least one item ticked off; not all items done
- `complete` — every item in the phase plan is `[x]` or `[!]` with justification
- `blocked` — waiting on external dep / user input

## Iteration rules

1. On startup read this file; pick up at the in-progress phase.
2. **Do not stop at phase boundaries.** Finish current → roll directly into next.
3. As items finish, flip `[ ]` → `[~]` → `[x]` (or `[!]` blocked) in the per-phase file.
4. Update this status table whenever a phase opens or closes.
5. Append meaningful decisions to `99-decisions-log.md`.
6. Legitimate stops: context exhausted, unrecoverable external block, all phases done. Otherwise keep going.
7. If reality and plans diverge, update plans first, then continue.

## Bootstrap log (one-time setup work done before Phase A)

- [x] `plans/` directory and all 11 plan files written
- [x] `CLAUDE.md` created with plans-workflow rules
- [x] Repo skeleton directories created with `.gitkeep`
- [x] `LICENSES/` populated (CC BY-NC-SA 3.0 project default; Net7, Tada-O, enb-linux-installer; README)
- [x] Initial `.gitignore`
- [x] First git commit
