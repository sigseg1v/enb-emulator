<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>N7.CliClient</RootNamespace>
    <AssemblyName>CliClient.Core</AssemblyName>
    <IsPackable>false</IsPackable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>
  <ItemGroup>
    <InternalsVisibleTo Include="CliClient.UnitTests" />
  </ItemGroup>
  <ItemGroup>
    <!-- Westwood RSA/RC4 handshake crypto, kept in its own (non-freya) assembly
         for clean licensing separation. See proxy/WestwoodCrypto/. -->
    <ProjectReference Include="..\..\..\..\proxy\WestwoodCrypto\WestwoodCrypto.csproj" />
  </ItemGroup>
</Project>
